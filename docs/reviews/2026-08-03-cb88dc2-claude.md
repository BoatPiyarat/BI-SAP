# Review — RQ-20260803-1918-rcb-onetime-change-order-rcl-label

Reviewer: Claude Code
Artifact: `docs/FINDINGS_RCB_ONETIME_CHANGE_ORDER_MISROUTED_RCL_20260803.md` (commit `ce09d9a`),
`sql/adhoc/20260803_l80482368_rcb_creditshell_misroute.sql`, and the fail-closed delta in
`sql/ddl/058_v3_unit5_newpayment_shadow.sql` (commit `cb88dc2`).
Verdict: **PASS**

## Checklist 1–12

1. **Traceability** — pass. Governance pull timestamped `2026-08-03T12:04:11.971Z` with normalized
   full-definition hashes for both live objects; population job
   `codex_l80482368_exactpop_20260803_121114` at `2026-08-03T12:11:14.139Z` with dry-run at
   `12:11:12.405Z`, bytes processed/billed against the 20 GiB ceiling; registry-guard job
   `codex_onetime_rcl_mapping_guard_20260803_124344`.
2. **Provenance over repetition** — pass. Cites the three 2026-07-20 job IDs as root cause instead
   of re-deriving them; cites live vs. repo hashes rather than re-pasting bodies.
3. **NULL-safety** — pass. `posted_latest` filters `SAFE_CAST(DocEntry AS INT64) IS NOT NULL`
   (not raw equality); the label match wraps both `PaymentMethod`/`PaymentChannel` in `IFNULL` before
   `REGEXP_CONTAINS`. The 058 assertion (lines 159–163) does the same `IFNULL(...)` pattern.
4. **Ordering** — pass. `posted_latest` dedup orders `UpdateDate DESC, SAFE_CAST(DocEntry AS INT64) DESC`
   — a real tiebreak, not a string-sorted date.
5. **Column order** — N/A, no interface-file column list touched by either artifact.
6. **Grain stated** — pass, and worth a note. Diagnostic grain is order/order_item, not order_item+period.
   The join `posted_latest` to `source_rcb_change` is scoped by `U_OrderItem` only, not `U_Period`; this
   is safe here only because the population is restricted to `FULL_PAYMENT` + `installments=1` +
   `number_of_installment=1` orders, which by the doc's own stated rule are always single-period. If
   this query is ever reused for a population without that guarantee, add the period join explicitly —
   flagging as a note, not a blocker, since `COUNT(DISTINCT order_item)` protects the reported numbers
   from inflation even if a future reuse introduced multi-period fan-out.
7. **Distribution, not row count** — pass. Reports a four-stage funnel (source → posted → wrong-label →
   wrong-label-and-M1) with both order and order_item counts at each stage, not a single aggregate.
8. **No contradiction with knowledge** — pass. Uses `SAP_LIVE_FULL`-derived `posted_latest` as the
   truth source per the canonical SAP-truth rule; correctly distinguishes "recorded on RCL" (payment
   label) from `CompanyDB` (company database) rather than conflating them. Explicitly scoped as
   separate from INCIDENT-002a/002b and the 224 unknown-cause orders.
9. **Scope** — pass. Diagnostic lives in `sql/adhoc/`; the fix is a source-only `ASSERT` addition
   inside the existing V3 procedure body, not a new deploy or a legacy-view edit. Doc explicitly
   states "Do not patch a drifted live legacy view from its repo baseline."
10. **Rollback** — N/A, no deploy occurred; doc states this explicitly.
11. **Cost hygiene** — pass. Dry-run before run on the diagnostic; single query returns all funnel
    metrics via `COUNT(DISTINCT IF(...))` rather than four separate scans; registry-guard query
    processed 1,256 bytes/billed 10,485,760 bytes, well under the cap.
12. **Honest labelling** — pass. "Nine is the population of this exact posted cause, not every
    possible one-time/RCL mechanism" and "Correction eligibility remains separate and is not approved
    by this finding" both explicitly bound the claim; the 058 delta is labelled source-only pending
    review.

## Assertion placement (delta-specific ask)

Confirmed: in `058_v3_unit5_newpayment_shadow.sql`, the `ONETIME row resolved to an RCL payment
mapping` assertion (lines 159–163) runs against `_resolved`, which is built (lines 131–149) after
the `payment_mapping_registry`/`insurance_group_registry` joins, and strictly before
`_candidate_target` is materialized (line 165). The regex `r'(^|\|)RCL'` correctly anchors on
string-start or the `method|channel` delimiter, so it also catches a channel-only match when method
is empty. The registry-guard job confirms zero currently-approved mappings would trip it, so the new
gate is defense-in-depth, not a live regression risk.

Checklist 1–12 reviewed; one non-blocking note (item 6, join scope reuse risk).
