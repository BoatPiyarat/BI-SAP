# Review — RQ-20260817-second-daily-recon-mtd-report-delta

Reviewer: Codex
Fixed point: `25e6fa0`
Artifact: commit `9722688`
Verdict: **BLOCK**

## Standards

The corrective delta closes the deterministic-clock, recipient-trimming, and diff-hygiene defects.
The embedded Node runtime executed all 30 contract assertions successfully, the Apps Script parsed,
and `git diff --check 25e6fa0...9722688` passed. No actionable Fowler smell or new secret exposure
was found.

The mandatory exact-query safe-wrapper dry-runs remain unavailable. A live attempt on 2026-08-17
failed during dry-run, before query execution, with `bq 2.0.92` `ReauthUnattendedError`. The runbook
also still contains `<APPS_SCRIPT_PROJECT_ID>` and lacks a verified manifest, deployed source hash,
captured trigger ID, rehearsal result, and exercised rollback evidence. The artifact is not ready
for rehearsal or installation.

## Spec

The 35-day freshness lookback improves the empty-current-month case but does not fully separate
refresh evidence from business-row presence. `MAX(recon_checked_at)` is still computed only over
rows whose `first_paid_time` falls in that arbitrary lookback. A freshly rebuilt but genuinely
quiescent table therefore still produces no freshness evidence and fails instead of delivering a
valid zero-count report. The test mocks a freshness row independently, so it does not exercise this
case.

The requested terminal sequence is also incomplete by explicit admission: exact SQL dry-runs,
concrete Apps Script target/manifest/hash, acceptance rehearsal, installed trigger ID, and verified
rollback remain absent.

Required correction: use refresh evidence that remains observable when the business population is
empty (for example a dedicated refresh-control record), add the quiescent-table test, restore the
BigQuery dry-run gate, and replace deployment placeholders with verified target and rollback
evidence. Request one focused delta review. Do not rehearse or install the trigger before PASS.
