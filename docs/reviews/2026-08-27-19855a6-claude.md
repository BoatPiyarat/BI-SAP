# Review: promoter legacy-SA exception disclosure delta — commit `19855a6`

Reviewer: Claude Code
Scope: promoter proposal, default-Compute workaround, delivery checker, and session authorization
Verdict: **PASS**

This was a read-only delta review against the three required corrections in
`docs/reviews/2026-08-27-344c9eb-claude.md`. No IAM, deploy, build, GCS, Workflow, or Scheduler
action was taken.

## Closed findings

1. **True permission ceiling:** both decision documents now state that project
   `roles/storage.objectAdmin` permits read/create/overwrite/delete across project buckets and
   direct `roles/storage.legacyBucketOwner` carries legacy bucket/ACL control. Exact-generation
   reads and create-only writes are application controls, not IAM controls.
2. **Durable narrow exception:** the binding workaround records Boat's dated promoter-only
   exception and keeps the workflow caller, Scheduler OAuth, dispatcher, watchdog, and all other
   unattended components on default Compute. No-IAM-mutation constraint remains unchanged.
3. **Shared blast radius:** both documents disclose that the legacy identity also runs
   `sap-interface-pipeline` and `demo-nonmotor-bucket`; neither legacy resource may be changed.

## Checker and command

The checker now requires the legacy promoter runtime and emits it in JSON while retaining the
default-Compute workflow/Scheduler gates, public-principal gate, and all other fail-closed checks.
The single proposed `gcloud run deploy` contains no IAM-binding command or IAM-mutation flag.

## Conclusion

All prior required corrections are closed and consistent. Under Boat's recorded 2026-08-27 13:34
ICT approval, the private promoter proposal is actionable as documented. Deployment remained
outside this review and had not yet occurred at verdict time.
