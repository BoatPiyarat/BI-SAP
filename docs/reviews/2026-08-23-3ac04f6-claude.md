# Review: RQ-20260823-2229 v3-creditshell-deploy-evidence

Reviewer: Claude Code
Artifact: `3ac04f6`; V3 CreditShell deployment evidence and post-deploy verifier
`sql/adhoc/20260823_verify_deployed_v3_creditshell_router.sql`
Verdict: **PASS**

## Verification performed

Two checks, both independent of trusting the commit's own numbers:

1. **Deployment-fidelity check (metadata-only, free — `bq show`, exempt from the query wrapper).**
   Pulled the live `view.query` text for all four objects
   (`vw_creditshell_flow_classification`, `vw_creditshell_routing_hold`,
   `vw_creditshell_routing_ready`, `vw_creditshell_routing_violation`) and diffed each against the
   PASSed source `sql/ddl/081_v3_creditshell_flow_router.sql` (commit `d2b6e63`). All four match
   byte-for-byte (aside from the `CREATE OR REPLACE VIEW ... AS` wrapper and trailing `;`, which
   `bq show` naturally strips) — no drift between reviewed and deployed. `creationTime` on all four
   views is `2026-08-23T15:04:20Z`–`15:04:23Z` UTC, a single 3-second sequential deploy, consistent
   with one `CREATE OR REPLACE VIEW` script run once, not a repeat/partial deploy.
2. **My one targeted live query** — ran the exact cited verifier
   (`sql/adhoc/20260823_verify_deployed_v3_creditshell_router.sql`) myself: dry-run
   131,551,761 bytes (~0.123 GiB), matching the cited estimate exactly; real run at
   `2026-08-23 15:57:09 UTC` returned `route_rcb_items=11069, route_rcl_items=4143,
   hold_items=19241, onetime_routed_to_rcl_violations=0, known_case_passes=4` — an exact match to
   the commit's claimed figures, reproduced independently roughly 50 minutes later (stable, not a
   one-off).

## Review focus items

- **Job/query/timestamp provenance** — PASS. Both job IDs are cited; I independently re-ran the
  verifier and got identical output, and confirmed live view `creationTime` corroborates a real
  deploy at the claimed time (not a fabricated or backdated claim).
- **Aggregation grain** — PASS. `hold_items` (19,241) equals the sum of the three held buckets I
  measured in the prior classifier review (`HOLD_PAYMENT_OPTION_NULL` 8,158 +
  `HOLD_RCL_SPINE_INVALID` 837 + `HOLD_SCHEDULE_MISSING` 10,246 = 19,241) — the roll-up in this
  verifier is consistent with the finer-grained distribution already reviewed in `d2b6e63`, not a
  new/different count.
- **Known-case predicate** — PASS. `known_case_passes=4` checks all four order_items
  (`L80569331-M1/-V1`, `L80482368-M1/-V1`) individually rather than an aggregate that could mask a
  partial failure; I independently confirmed the same 4/4 result.
- **Honest legacy-V2 boundary** — PASS. Both the changelog and the findings-doc addendum explicitly
  state this deployment does not patch the legacy V2 generator and no legacy consumer reads these
  new views yet, so legacy-error prevention remains open. No overclaiming.

## Checklist 1–12

1 traceability PASS (job IDs + independently reproduced query, not just described); 2 provenance
PASS (cites `d2b6e63` and the prior review by hash); 3 NULL-safety N/A (no new logic, same view
already reviewed); 4 ordering N/A; 5 column order N/A (not an interface file); 6 grain PASS (item-
level, matches prior review); 7 distribution PASS (no row-count-as-correctness claim beyond the
already-verified known-case predicate); 8 knowledge PASS (no v2 DDL, no GCS write, consistent with
`AGENT_RULES.md`); 9 scope PASS (`sql/adhoc/**` + `docs/knowledge/**` + one findings doc, all within
domain); 10 rollback PASS — new objects with no existing consumers, so `AGENT_RULES.md`'s DEPLOY
GATE approval step correctly did not apply, and rollback is simply dropping four newly-created
views nothing yet reads; 11 cost hygiene PASS (0.123 GiB verifier, matches my own dry-run exactly);
12 honest labelling PASS (legacy-boundary disclaimer present and accurate, no backfill/interface/GCS
claim).

Summary: Checklist 1–12 reviewed; no gap found. Deployment matches reviewed source exactly, and
every cited number reproduces independently.
