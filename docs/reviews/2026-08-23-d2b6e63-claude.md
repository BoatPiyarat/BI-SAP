# Review: RQ-20260823-2133 v3-creditshell-flow-router

Reviewer: Claude Code
Artifact: `d2b6e63`; `sql/ddl/081_v3_creditshell_flow_router.sql`, read-only V2 preview
`sql/adhoc/20260823_preview_v2_creditshell_routing_fix.sql`
Verdict: **PASS WITH NOTES**

## Verification performed

Source-only artifact, nothing deployed (confirmed by the request's own dry-run evidence: V3 DDL
0 bytes). Per protocol, ran one targeted live query re-implementing the classification CTE chain
as a plain SELECT (not creating the view) — 131,551,761 bytes (~0.123 GiB) dry-run,
`2026-08-23T~22:0x+07:00`, `scripts/bq_safe_query.sh`:

- **Both known live-misroute trigger cases now resolve correctly.** `L80569331-M1`/`-V1` (the
  second confirmed misroute, `docs/knowledge/20_SAP_PROGRESS.md` 2026-08-23 20:06 entry) and
  `L80482368-M1`/`-V1` (the original 2026-08-03 finding's known-answer case) both classify as
  `ROUTE_RCB_CREDITSHELL` under the new logic. This is the actual defect the artifact claims to
  fix, independently reproduced rather than trusted from the commit message.
- **Live routing distribution** (population = all `cancelled_change_orders` links,
  2025-01-01+ orders): `ROUTE_RCB_CREDITSHELL` 11,069, `ROUTE_RCL_CREDIT_SHELL` 4,143,
  `HOLD_SCHEDULE_MISSING` 10,246, `HOLD_PAYMENT_OPTION_NULL` 8,158, `HOLD_RCL_SPINE_INVALID` 837,
  all other HOLD/ROUTE buckets zero. No crash, no unexpected NULL routing_decision.
- **NULL-safety check on `ANY_VALUE(payment_option)`**: `transaction_dim` groups
  `carepay_transactions` by `id` and takes `ANY_VALUE(payment_option)`, guarded by
  `COUNT(DISTINCT payment_option)`. `ANY_VALUE` does not guarantee it skips NULL rows when a
  non-NULL value also exists in the same group, which could non-deterministically flip a resolvable
  id into `HOLD_PAYMENT_OPTION_NULL`. Checked directly: 0 transaction `id`s currently have
  `COUNT(DISTINCT payment_option)=1` together with `COUNTIF(payment_option IS NULL)>0` and more than
  one raw row — the risk doesn't manifest against live data today.

## Prior BLOCK items (`docs/reviews/2026-08-23-f25af4f-codex.md`) — closed or not applicable

1. Prohibited v2 DDL target — **closed**. All `CREATE OR REPLACE VIEW` targets are
   `sap_integration_v3.*`; nothing touches `sap_integration_v2`/`careos`, consistent with
   `AGENT_RULES.md` line 64.
2. Numeric provenance incomplete — **not applicable to this artifact**. This artifact makes no
   specific row/item-count behavioral claim (unlike the blocked `f25af4f`); it only claims the five
   structural corrections, which I verified directly above.
3. Duplicated routing logic (note, non-blocking) — **still present** (`target_payment_method` and
   `target_payment_channel` CASE blocks are identical), but no longer a live risk on its own: the
   new `vw_creditshell_routing_violation` view is the durable equality/regression guard the original
   note asked for.
4. Unknown/NULL flow fail-open — **closed**. `payment_option IS NULL` → `HOLD_PAYMENT_OPTION_NULL`;
   anything outside the three known values → `HOLD_PAYMENT_OPTION_UNKNOWN`. No unconditional
   fallback to either route remains.
5. Missing durable regression assertion — **closed**. `vw_creditshell_routing_violation` flags any
   row where `schedule_flow='ONETIME'` and the resolved target starts with `RCL`. By construction
   this returns zero rows today (the classification CASE structurally cannot produce that
   combination), which is correct for a tripwire view — it exists to catch *future* logic drift, not
   to report a current count.
6. CREDIT_CARD_INSTALLMENT missing `TotalPeriods=1` gate — **closed**. `HOLD_ONETIME_INVARIANT`
   requires `s.flow='ONETIME' AND s.total_periods=1` (plus a complete single-row spine) for both
   `FULL_PAYMENT` and `CREDIT_CARD_INSTALLMENT` before either can route.

## Checklist 1–12

1 traceability PASS (dry-run evidence timestamped; my own verification numbers stated above with
query cost); 2 provenance PASS (cites `f6fe914` BLOCK and the 2026-08-03 finding by name); 3
NULL-safety NOTE (see `ANY_VALUE` risk above — zero current incidence, not a blocker, worth a
follow-up using `ARRAY_AGG(... IGNORE NULLS ... LIMIT 1)` instead of `ANY_VALUE` for determinism);
4 ordering N/A (no date-sorted output); 5 column order N/A (classification view, not an interface
file); 6 grain PASS — explicitly item-grain (`order_id, order_item`), confirmed by both trigger
cases correctly producing separate M1/V1 rows sharing one order-level `payment_option`; 7
distribution PASS (no row-count-as-correctness claim made); 8 knowledge PASS (v3-only DDL, matches
`AGENT_RULES.md`); 9 scope PASS (`sql/ddl/**` + `docs/knowledge/**`, no v2 touch); 10 rollback PASS
— these are brand-new v3 objects with no existing consumers, so `AGENT_RULES.md`'s DEPLOY GATE
paragraph-and-approval requirement (which applies to *replacing* something existing consumers read)
does not yet apply; 11 cost hygiene PASS (0 bytes DDL dry-run stated; my verification query
0.123 GiB, one query, multiple metrics via UNION ALL); 12 honest labelling PASS — the artifact does
not overclaim: `docs/INPUTS_NEEDED.md`/progress notes are explicit that this view does not stop the
still-active live V2 misrouting (root cause in `RCL_04_new_order_credit_shell_all.sql` remains
unfixed per the 20:06 ICT entry) and requires a separate deploy decision.

## Required note (non-blocking, does not stand in the way of PASS)

Deploying these new v3 views does **not** fix the live production defect — `L80569331-M1`'s class
of order will keep posting `RCL-Credit Shell` in the real SAP export until either the legacy V2
generator is patched (separately blocked by the v2-DDL prohibition) or V3 takes over production
interface delivery. Confirm this is understood before treating this PASS as "the misroute is fixed."

Summary: Checklist 1–12 reviewed; one non-blocking NULL-safety note on `ANY_VALUE` determinism
(zero live incidence today); all six prior BLOCK items from `f25af4f` are closed or inapplicable;
both real trigger cases independently reproduced correct.
