# Review: RQ-20260824-1338 installment InvoiceNo NULL-safety resubmission

Reviewer: Codex  
Artifact: `a3d0305` + `0ac324f`  
Verdict: **BLOCK**

## Standards

Both prior implementation blockers are substantively closed. The cumulative SQL diff is surgical:
only the explanatory header and two NULL-safe `COALESCE` predicates remain. Scoped evidence shows
`L77833033-V1` paid period 1 unchanged and pending periods 2–6 converted to blank InvoiceNo. The
full-population arithmetic is internally exact: 542,947 paid + 453,708 pending = 996,655 total;
453,709 original NULLs = 453,708 pending rows + the one disclosed paid residual.

**Remaining BLOCK — evidence provenance.** The queue gives job IDs only for the scoped checks. It
does not give the full-population before/after job IDs (or a saved reproducible query), source-object
list, or execution timestamps for the 996,655/190,504/542,947/453,708 parity claims.
`AGENT_RULES.md` Verification discipline and protocol checklist 1 require named source object,
query, and timestamp for every reported number. Add that provenance; do not substitute the queue
Opened time. Dry-run estimates are below the cap, but checklist 11 remains incomplete because the
parity executions cannot be independently identified.

The residual `L80174076-M1` paid NULL is disclosed as pre-existing in untouched compulsory logic
and is correctly outside this surgical pending-period fix. It must remain blocked by the pre-export
gate and receive a separate task. Any eventual deploy must patch a freshly captured live definition
and preserve its outer `SELECT * REPLACE` wrapper.

No actionable Fowler smell found.

## Spec

**PASS.** The change implements the pending NULL→blank requirement, paid behavior remains unchanged,
row/item/status parity is claimed consistently, unrelated formatting is gone, and the one paid
residual is credibly pre-existing and out of scope. The live-wrapper deployment condition remains
explicit.

## Checklist 1–12

1. **Traceability — BLOCK:** full-population jobs/query/source timestamps absent.
2. **Provenance over repetition — PASS:** commits and prior review are cited.
3. **NULL-safety — PASS:** both changed comparisons are NULL-safe.
4. **Ordering — N/A:** no ordering/date change.
5. **Column order — PASS:** expressions changed in place; live wrapper must remain intact.
6. **Grain — PASS:** row/item parity is internally consistent; no join change.
7. **Distribution — PASS:** total, item, paid, pending, NULL, and blank distributions reconcile.
8. **Knowledge consistency — PASS:** pending blank InvoiceNo matches canonical rules.
9. **Scope — PASS:** whitespace churn reverted; paid compulsory residual deferred.
10. **Rollback — N/A:** source only; not deployed.
11. **Cost hygiene — BLOCK:** dry-runs are under 20 GiB, but parity executions lack reproducible IDs.
12. **Honest labelling — PASS:** residual and no-deploy boundary are explicit.

Per the protocol's one-round rule, do not start another agent-to-agent review round. Supply the
missing provenance and escalate the final disposition to Boat with both positions if needed.
