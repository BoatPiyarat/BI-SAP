# FINDINGS — empty `carepay_transaction_snapshot_installment_details` strands items silently, no interface error

Status: **Not yet Class-A reviewed.** Investigation + design proposal only. No SQL deployed, no
production mutation. Quantification below should not be cited as final until reviewed.

## Origin

Boat asked: why has `L78753909` never reached SAP, with no interface-error history at all?
Traced end-to-end 2026-08-25 (read-only).

## Root cause (confirmed on `L78753909-V1`)

1. The order's `carepay_transaction_snapshots` row declares `number_of_installment = 10`, but
   `carepay_transaction_snapshot_installment_details` has **zero rows** for that snapshot — the
   per-period breakdown was never generated by CareOS. This is an upstream data gap, not a SAP-side
   SQL defect.
2. The customer actually paid in full with one `ONLINECARD` charge, `service_provider = 'RCB'`,
   `SUCCESSFUL`, on 2026-08-11 — a genuine one-time payment, consistent with `payment_option =
   'CREDIT_CARD_INSTALLMENT'` being deliberately classified `flow='ONETIME'` in `stg_schedule`
   (a credit-card installment is force-mapped to ONETIME because the bank pays in full up front,
   `sql/ddl/012_stg_schedule.sql`).
3. Because `number_of_installment=10` (>1), the item is excluded from the legacy
   `sap_dashboard_carepay_fully_paid` view (`sql/production/sap_dashboard_carepay_fully_paid.sql:53`
   hard-filters `number_of_installment = 1`) and only surfaces in
   `sap_dashboard_carepay_installment` instead.
4. But that view derives `Period` from `transaction_snapshot_installment_details.period`
   (`sql/production/20260824_deploy_installment_invoiceno_null_safety.sql:198`, the current live
   definition) — with zero detail rows, `Period` is `NULL` for every row, so the view falls back to
   one placeholder row: `TransactionStatus='Pending'`, `Period=NULL`, blank `InvoiceNo`. (The
   `charges.service_provider = 'RABBIT_LENDING'` join condition on the same view,
   `line 249`, is a second, independent reason the successful `RCB`-provider charge would never
   join even if detail rows existed — compounding, not the primary cause here.)
5. A row with `Period=NULL` cannot pass the pre-export spine-completeness gate
   (`docs/design/SAP_INTERFACE_PRE_EXPORT_GATE.md` §"RCL installment checks" — needs the exact
   integer set `1..N`). It is therefore never built into an interface candidate and never reaches
   the SAP submission step — which is exactly why `sap_errors_logging` (the SAP-side interface
   error table), `sap_excluded_records`, and `sap_validation_error` all show **zero** rows for this
   item. It didn't fail; it never left the building.

## Does V3 already cover this? — Partial. ONETIME is rescued; RCL is not.

V3's Unit 5 NEWPAYMENT shadow (`sql/ddl/058_v3_unit5_newpayment_shadow.sql`) sources ONETIME-flow
items from its own V3-owned `vw_onetime_payload_source`
(`sql/ddl/050_v3_onetime_payload_source.sql`), and RCL-flow items straight from the same legacy
`sap_data_engineer.sap_dashboard_carepay_installment` view (line 105 of 058):
```
WHERE EXISTS (SELECT 1 FROM _target t WHERE t.order_item=OrderItem AND t.flow!='ONETIME')
```

`050`'s `onetime_master` CTE joins `charges` directly to `transactions` by `transaction_id` alone —
it never touches `transaction_snapshot_installment_details` or filters on
`number_of_installment`, and it explicitly excludes `RABBIT_LENDING` (`service_provider <>
'RABBIT_LENDING'`), the opposite of the legacy view's restrictive `=` filter. Confirmed empirically:

```
SELECT * FROM sap_integration_v3.vw_onetime_payload_source WHERE OrderItem='L78753909-V1';
```
→ `TransactionStatus='paid'`, `Period=1`, `TotalPeriods=1`, `InvoiceNo='chrg_68n72hf0z7zd3m8ddja'`,
`ActualReceived=6100.07` — a correct, complete row. **V3 already resolves this exact item for the
ONETIME flow, independent of the CareOS data gap.**

**RCL flow has no equivalent independent path.** If a genuinely multi-period installment order
(`flow='RCL'`, `payment_option='RABBIT_CARE_INSTALLMENT'`) hits the same empty-`installment_details`
condition, V3 inherits the identical blind spot as V2, because Unit 5 reads the same broken legacy
view for that flow with no fallback.

## Population scope (read-only scan, 2026-08-25, ~0.3 GiB)

Every transaction whose latest snapshot declares `number_of_installment > 1` with zero
`installment_details` rows, bucketed by `stg_schedule.flow`:

| flow | payment_option | affected items | already in SAP |
|---|---|---:|---:|
| ONETIME | CREDIT_CARD_INSTALLMENT | 20,793 | 20,485 |
| NULL (not in schedule) | CREDIT_CARD_INSTALLMENT | 861 | 832 |
| **RCL** | **RABBIT_CARE_INSTALLMENT** | **3** | **3** |
| ONETIME | FULL_PAYMENT | 2 | 0 |

Reading this:
- The large ONETIME bucket is expected and mostly benign — `number_of_installment>1` is apparently
  a common artifact for credit-card-installment orders regardless of how they were actually paid,
  and 20,485/20,793 are already invoiced. The **308** ONETIME + **29** flow=NULL items not yet in
  SAP (including `L78753909-V1`) are candidates that should resolve correctly once run through
  `vw_onetime_payload_source` (for the flow=NULL rows, `stg_schedule` itself needs checking
  separately — a missing schedule row is a different, prior gap).
- **The RCL row is the one that matters for design purposes**: 3 items hit this exact defect under
  a genuine installment flow, and — by luck, not by design — all 3 are already in SAP (likely
  posted before this specific empty-detail condition arose for them, or reconciled by some other
  path; not investigated further here as it's not blocking). **Zero RCL items are currently stuck**,
  but nothing in V3 would catch a *future* one — it would silently strand exactly like
  `L78753909-V1` did, with the same total absence of error/exclusion/validation trail.

## Design proposal — close the RCL blind spot

Two independent, non-conflicting fixes; either alone helps, both together is more robust:

**1. Fail-closed detection gate (highest priority — makes the failure visible instead of silent).**
Add an explicit hold check to the RCL sourcing branch of DDL 058 (or a pre-export gate assertion in
`docs/design/SAP_INTERFACE_PRE_EXPORT_GATE.md`'s RCL section) that flags — by name — an order_item
whose declared period count is known (`stg_schedule.total_periods > 1`, or the legacy view's own
`TotalPeriods` column) but whose actual row(s) show `Period IS NULL` or a row count that doesn't
match. Something in the shape of:
```sql
-- illustrative only, not for deployment as-is
COUNTIF(Period IS NULL AND SAFE_CAST(TotalPeriods AS INT64) > 1) AS declared_installment_no_detail_rows
```
routed to a distinctly named hold (e.g. `HOLD_EMPTY_INSTALLMENT_DETAILS`), not the generic
`INCOMPLETE_RCL_SPINE` bucket — so this specific, actionable CareOS-side data gap is distinguishable
from ordinary in-progress spines (an item still mid-payment legitimately has an incomplete spine;
this is different — it has *zero* real period data at all despite a >1 period declaration).

**2. V3-owned RCL source, mirroring `050`'s pattern (larger, addresses root cause structurally).**
Longer-term, an RCL-flow V3 source view that joins `charges` to `transactions` directly (like `050`
does for ONETIME) rather than depending on
`carepay_transaction_snapshot_installment_details`/`number_of_installment`, would make V3's RCL path
as resilient to this CareOS data gap as its ONETIME path already is. This is a larger design (a new
DDL, its own review cycle) and shouldn't block shipping fix #1 first.

Neither fix requires touching V2/legacy objects — both are additive V3-side changes, consistent with
`AGENT_RULES.md`'s DDL scope rule.

## What this finding does not claim

- Does not claim `L78753909-V1` or any of the 339 not-yet-in-SAP ONETIME/FULL_PAYMENT items are
  ready to invoice today — each still needs to pass every other pre-export gate check
  (56-column contract, InvoiceNo immutability, master/exclusion checks) before submission.
- Does not claim the 3 already-posted RCL items need correction — they're already in SAP; this
  finding is about the *class of risk*, not those specific rows.
- Does not propose deploying anything. Both design options above are proposals for a separate,
  reviewed, Boat-approved unit of work.
