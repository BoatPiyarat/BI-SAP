# Handover — Boat away 2026-07-26 to 2026-07-30

Written 2026-07-27. Covers everything built today plus what's genuinely still open. Per Boat's
own rule for this window: nothing here is claimed "done" unless verified against real data;
anything not verified says so plainly.

---

## 1. What to check every morning from your phone (3 things, with direct links)

1. **Did the extract run?** You're pressing this by hand this week (the scheduler is broken,
   IAM grant still pending - see §4). Console:
   https://console.cloud.google.com/run/jobs/details/asia-southeast1/sap-extract-job/executions?project=pacific-plating-282708
   Press **EXECUTE**. Takes 15s-2min normally.
2. **The 07:00 ICT digest email** (subject "SAP daily digest - <date>", or "⚠️ NO DATA ACCESS -
   ..." if the cloud routine lost GCP access). Read the "FAILED / still open" section first.
3. **Anything from `data@rabbit.co.th`'s inbox** (or wherever that forwards) — that's where the
   automated BigQuery alerts land, not your own inbox directly (see §4 for why, and the fix options).

If you only have 2 minutes: #2 covers #1 and most of the rest already.

## 2. What each alert means and what to do

| Alert (subject/source) | What it means | Do you press something, or wait? |
|---|---|---|
| "SAP daily digest" (07:00 ICT, your Gmail) | Normal daily status | Read it; act only on the "FAILED" section |
| "⚠️ NO DATA ACCESS - SAP daily digest" (your Gmail) | The cloud routine itself lost GCP credentials - **no real status was checked that day** | This needs someone to fix the routine's credentials - not a pipeline problem, don't assume the pipeline is fine just because this fired |
| `sap_dead_mans_switch` failure email (`data@rabbit.co.th`) - message starts "extract ไม่ได้รันคืนที่..." | SAP_LIVE hasn't had a fresh load in >26h | **Press EXECUTE** on the extract job (link in the message and in §1) |
| `sap_column_contract_guard` failure email (`data@rabbit.co.th`) | A `sap_view.*` interface view had a column reordered/renamed/moved - **a real exported file would very likely be rejected by SAP** | Do NOT let tonight's legacy export run if you can stop it; this needs code investigation (which view, which column) - can wait for you unless it's already past export time, in which case it's genuinely urgent |
| `sap_validation_regression_alert` failure email (`data@rabbit.co.th`) | `sap_validation_error` jumped past 60 total rows (baseline ~22-24) | Worth a look when convenient, not necessarily urgent - check the breakdown in the message |
| `sap_interface_daily_status_alert` failure email (`data@rabbit.co.th`) | Either `interface_daily_status` hasn't refreshed in >12h (chain may not have run), or a real `PAID_AFTER_CANCEL` case exists (a payment recorded in SAP after CareOS cancelled the order) | Staleness: check if the nightly chain ran. PAID_AFTER_CANCEL: worth investigating, genuinely rare (7 baseline cases) so any new one stands out |

## 3. What runs entirely on its own (no action needed unless it alerts)

- The legacy interface pipeline (`sap-order-payment`/`-non-motor` → the two Cloud Functions →
  real files to SAP) - automatic, 01:30 ICT nightly. Known open risk: Motor's function had a 300s
  timeout dropping its RCL newpayment step for 5+ days as of 2026-07-26 - **UNVERIFIED whether a
  540s fix has landed**, check via the daily digest or `gcloud functions describe
  rcb-motor-order-payment-sap-bucket-1 --region=asia-southeast1 --format="value(timeout)"`.
- The real SAP_LIVE loader (extract → GCS → Eventarc → `sap-order-payment-initial-phase`) -
  automatic and healthy, confirmed live 2026-07-27, as long as the extract itself produces a file
  (which needs your manual press this week).
- The full V3 nightly chain (`sap_state_and_recon_refresh`, now correctly firing 21:00 ICT after
  today's timezone-bug fix) - staging tables, `sap_mirror_doc`/`state`, `expected_state`,
  validation, `delta_export`, `interface_daily_status`. Diagnostic only - writes no interface file.
- All 4 new email alerts (§2 table) - each genuinely tested this session (see §5).

## 4. What's locked, waiting for you specifically

- **Attila's IAM grant** for `sap-extract-schedule` (exact command in `docs/INPUTS_NEEDED.md`) -
  you chose to press EXECUTE manually this week instead, so this isn't blocking, just still open.
- **`data@rabbit.co.th` vs your own inbox**: every BQDTS failure-email alert (§2 table, the 4
  `data@rabbit.co.th` rows) goes to that identity, confirmed via `ownerInfo.email` on each transfer
  config - not to `piyaratt@rabbit.co.th`. If you don't check that inbox this week, those 4 alerts
  are effectively silent to you. Only the 07:00 digest (Gmail-connector-based) reaches you directly.
- **Q3a** (Aware): which document is authoritative when a period has multiple SAP documents - now
  has 2 parts (original + the new NULL-BatchRunDate sub-question added today). Nothing downstream
  is blocked on this - `sap_mirror_state`'s picking rule handles it provisionally and tags every
  affected row, now visible all the way through to `delta_export`/`interface_daily_status`.
- **`sap_integrety_2025_RCL` and 5 related views** (FINDINGS §14): confirmed real double-counting
  bug, but the view itself is dormant (no consumer in 90 days) - `audit_010_careos_missing_in_sap_detail`
  IS actively used and has the same pattern, not yet individually checked. **Not touched, not
  told to anyone outside the team**, per your explicit instruction - waiting for your return.
- **4 standing decisions** (ProcessingFee divisor, C2 quarantine policy default, EDC channel
  matrix, B2B confirmation) - listed in `docs/INPUTS_NEEDED.md`, not blocking anything this week.
- **3 accepted fixture files** for PHASE B's real acceptance test (golden-file diff) - see §6.

## 5. What was verified today, and how (not just claimed)

- **2 real bugs found and fixed** in `sap_mirror_doc`/`sap_mirror_state` (string-sorted dates,
  missing tiebreak) - verified via a live before/after `delta_type` distribution comparison:
  `NEEDS_PAID_UPDATE` dropped 48,335 → 3,585 after the fix, confirming it mattered for real data,
  not just in theory.
- **The email-alert mechanism itself** - this project had only ever tested "stays fresh" before;
  today a throwaway test config was forced to genuinely fail with `enableFailureEmail` on,
  confirmed `state: FAILED` with the real error message. Whether the email actually landed in an
  inbox can't be checked from here - that's the one link in the chain only you can confirm.
- **Column-contract guard** - tested against a deliberately reordered scratch view (2 columns
  swapped), confirmed it correctly flagged both positions, before being scheduled for real.
- **`interface_daily_status`** - the first version had a real bug (`PAID_AFTER_CANCEL` fired on
  1,898 rows that were just the normal "paid then later cancelled" pattern); caught it by sampling
  real rows, fixed by comparing payment date against cancel date directly (now 7 genuine cases).

## 6. What's NOT done - genuinely open, not attempted to compress into a stub

**PHASE B (4.1/4.2) — not started this session.** Extending `expected_state` to the full 56-column
contract across all 5 flows (including Credit Shell, currently excluded entirely) and building the
legacy-file diff harness is a substantial rebuild of the L3 engine in its own right - sections 1-3
above (unattended hardening + finishing PHASE A + the carryover items) took the whole of this
session's available time and were prioritized first per your own explicit ordering ("ทำก่อน").
**Do not read anything above as PHASE B progress** - `expected_state` is unchanged from what
existed before today (3 flows: ONETIME, RCL, RCL_CMI; no Credit Shell; no diff harness). Needs its
own dedicated session. Acceptance for this phase remains the golden-file test against the 3
accepted files you'll bring back 2026-07-30 - not something to shortcut with parallel-run-only
comparisons in the meantime, per your own explicit instruction not to lower the acceptance bar.

**PHASE C (5.1-5.4) — not started.** Shadow-mode export (`sp_export_delta` writing to a shadow
prefix, the C2 quarantine-vs-atomic default, the cancel branch) depends on PHASE B's expected_state
being interface-complete first (can't export what isn't built yet) - correctly sequenced as
not-yet-reachable rather than skipped.

## Reporting discipline followed this session

Every completed unit (§ sections above) has its own `git commit` and its own `30_SAP_CHANGELOG.md`
entry with the real verification numbers, not just a description of intent. `20_SAP_PROGRESS.md`
reflects current state. Everything genuinely uncertain is logged in `docs/INPUTS_NEEDED.md` rather
than guessed at or quietly dropped.
