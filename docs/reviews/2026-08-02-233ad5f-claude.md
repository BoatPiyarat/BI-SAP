# Review — 233ad5f (change-order preflight hardening + first-run evidence)

**Queue entry:** RQ-20260802-change-order-hardening · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — both review notes implemented correctly; evidence arithmetic exact.

- **Grain preserved:** `_links_raw` stays DISTINCT pairs; the correlated degree counts are computed
  against the deduplicated raw set, so no inflation, and `_links` remains one row per pair.
- **CASE precedence — both placements correct:** `HOLD_LINK_AMBIGUOUS` is first (every downstream
  gate assumes a clean pair, so ambiguity must trump); `HOLD_SAP_TOTAL_PERIODS_CONFLICT` sits after
  the per-row range check and *before* the spine check — right, because
  `distinct_periods != MAX(total_periods)` is only meaningful once a single TotalPeriods version
  exists (the exact masking case NOTE 3 described).
- **Correlated link-degree counts:** valid form; hardened full script re-validated by dry-run.
- **Evidence:** the seven status counts sum to exactly 28,341 ✓; full job provenance (job ID,
  query timestamp, processed/billed). Distribution is plausible: 94% `HOLD_OLD_NOT_IN_SAP` is what
  an all-history link table joined against SAP should look like; 1,367 already-terminal pairs are
  the correctly-cancelled history; **93 READY** is small and — as required — explicitly
  provisional pending the hardened rerun, with the completed job retained as provenance rather
  than overwritten.
- Authority boundaries restated intact: Aware owns the explicit-cancel decision + CreditShell
  literals; FA/Boat own batch approval; item-level mapping still unproven, so no payload from the
  93 yet.

Next evidence expected: hardened rerun (same provenance discipline) — the 93 may shrink if any
pair trips the two new holds; then the item-level mapping proof before any payload work.

Queries used: 1 full-script dry-run (0 bytes).
