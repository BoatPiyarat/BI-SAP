# Review — RQ-20260811-1845-period-cutoff-calendar-delta

Reviewer: Codex  
Artifact: commit `efd6de0`; `sql/ddl/075_v3_period_cutoff_calendar.sql` and
`sql/adhoc/20260811_verify_period_cutoff_calendar.sql`.  
Verdict: **BLOCK**

## Findings

1. The verification script is not executable. Its `DECLARE rehearsal_failures` occurs after
   executable statements. Safe-wrapper dry-run at 2026-08-24 17:5x ICT failed at line 116 with
   `Variable declarations are allowed only at the start of a block or script`. DDL 075 itself
   dry-ran successfully at 0 bytes.
2. The live-test section unconditionally deletes year-2099 rows from the production calendar
   before and after the test. Those keys are not proven unowned, cleanup is not failure-safe, and
   destructive production cleanup lacks approval.
3. T2–T6 accept any exception as PASS without matching the intended assertion/error message, so
   unrelated authorization, schema, or runtime failures could produce false-positive evidence.
4. The required test matrix remains partial: exact-at-cutoff, timezone, rerun-after-transition,
   actual adjacent-period calculation/delegation, and real procedure transition are not exercised.
   The TEMP rehearsal copies selected predicates rather than verifying the deployed procedure.
5. Finance's correction-policy gate remains open. The 2026-08-24 correction from 14:00 to 16:00
   demonstrates that a reviewed audited correction path is now required before calendar deployment.
6. The `MERGE` delta is surgical and follows the established insert-only idiom, but no enforced key
   or concurrency test proves one immutable row per `period_start` under simultaneous calls.

## Checklist 1–12

1. Traceability: **PASS WITH NOTE** — artifact paths and prior review are cited; validation failed.
2. Provenance: **PASS** — reuses the prior BLOCK and established DDL 071 idiom.
3. NULL-safety: **PASS** — required procedure inputs fail closed.
4. Ordering: **BLOCK** — invalid procedural declaration order prevents parsing.
5. Column order: **N/A** — no interface payload columns changed.
6. Grain: **BLOCK** — monthly grain is stated but concurrent uniqueness remains unproven.
7. Distribution: **BLOCK** — claimed test outcomes cannot execute and errors are not distinguished.
8. Knowledge consistency: **BLOCK** — Finance correction policy remains unresolved.
9. Scope: **PASS WITH NOTE** — V3-only, but the test performs unsafe live cleanup.
10. Rollback: **BLOCK** — live-test cleanup is not failure-safe.
11. Cost hygiene: **BLOCK** — DDL dry-run 0 bytes; verifier dry-run fails parsing.
12. Honest labelling: **BLOCK** — “safe to run live” and executable-test claims are unsupported.

Standards: 4 hard findings; worst is the non-runnable verifier plus destructive live cleanup.  
Spec: 4 findings; worst is that the executable gate matrix and Finance correction-policy gate are
not satisfied.

