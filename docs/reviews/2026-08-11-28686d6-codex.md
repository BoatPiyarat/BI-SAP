# Review — RQ-20260811-1015-daily-completeness-dispatch-wiring

Reviewer: Codex  
Artifact: commit `28686d6`; `sql/ddl/067_v3_daily_completeness_snapshot.sql` and
`infra/v3_nightly_orchestrator.workflows.yaml`.  
Verdict: **BLOCK**

## Finding

The outcome split, terminal status set, original-run binding, and four-column payload-identity
join agree with the reviewed finding. `HUMAN_ACTION` residuals do not reach snapshot dispatch.
However, the two new lookup subworkflows are not on the required persist→alert→raise path:
`count_export_runs_for_pipeline` dereferences `rows[0]` without checking `jobComplete` or row
shape, and `derive_original_pipeline_run_id` checks cardinality but not `jobComplete`. API/query,
asynchronous-completion, or malformed-result failures therefore bypass the explicit `fail_closed`
contract. The post-import record is also marked `SUCCEEDED` before origin resolution and snapshot
dispatch, so a later failure can leave contradictory control state.

The mandatory dry-run was attempted through `scripts/bq_safe_query.sh` on 2026-08-11; the wrapper
self-test passed 7/7, but the DDL dry-run failed with `ReauthUnattendedError`. This is not
deploy-ready.

## Checklist 1–12

1. Traceability: PASS — artifact and prior finding are named; dry-run failure reproduced.
2. Provenance: PASS — prior reviewed finding and commits are cited.
3. NULL-safety: PASS for the changed SQL predicates.
4. Ordering: N/A.
5. Column order: N/A.
6. Grain: PASS — `(order_item, period, charge_id, payload_hash)` parity confirmed.
7. Distribution: PASS — no row-count parity offered as correctness proof.
8. Knowledge consistency: PASS — terminal set is exactly ACKNOWLEDGED/PARTIAL_REJECT/REJECTED.
9. Scope: PASS.
10. Rollback: N/A — source only, never deployed.
11. Cost hygiene: BLOCK — mandatory live dry-run not obtained.
12. Honest labelling: PASS — the request disclosed the missing dry-run.

Required correction: give both lookup subworkflows explicit synchronous-completion and result-shape
gates routed through `fail_closed`; ensure snapshot failure cannot coexist with a terminal-success
post-import record; then obtain a clean safe-wrapper dry-run and request one delta review.

