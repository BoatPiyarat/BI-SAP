# Review: RQ-20260824-2306 august-cutoff-live-convergence

Reviewer: Claude Code
Artifact: `0005f75`; production verification evidence for reviewed correction `623d1db`
Verdict: **BLOCK**

## Verification performed

Per protocol, one targeted live query against `` `region-asia-southeast1`.INFORMATION_SCHEMA.JOBS_BY_PROJECT `` (0.628 GiB dry-run, well under cap) plus free `bq show -j` / `bq head` metadata reads on the two cited jobs.

- **Failed job confirmed exactly as claimed.** `bqjob_r73ffe406eff06ea6_000001a034830566_1`: query text is
  byte-identical to `623d1db`'s correction script; `errorResult.message = "August OPEN period
  pre-state differs from reviewed evidence at [25:1]"` — the first pre-state `ASSERT`, before
  `BEGIN TRANSACTION`; only 2 child jobs exist (the timing assert + the failed pre-state assert),
  confirming the script aborted before either `UPDATE` ever ran; `totalBytesProcessed=0`. No
  mutation from this job — confirmed, not just trusted.
- **Post-check confirmed exactly as claimed.** `bqjob_r512e90973b2b2265_000001a034837632_1`:
  `totalBytesProcessed=199`, read-only `SELECT`. I fetched its actual result: August OPEN,
  `closing_at=2026-09-01 09:00:00 UTC`, `state_version=3`, `updated_at=2026-08-24 11:16:31 UTC`;
  lock `2026-09-01 09:00:00 UTC`, unlocked; September PLANNED/NULL — matches the claim exactly.
- **The "unresolved earlier writer" claim is false — the writer is trivially identifiable.**
  One query against `JOBS_BY_PROJECT` for the `sap_period_state`/`sap_period_lock` window
  10:30–11:30 UTC returns the mutation directly: job **`codex_august_cutoff_1600_20260824_1805`**
  (a hand-labeled job ID, not an auto-generated `bqjob_...` one), `user_email=data@rabbit.co.th`,
  `creationTime=1787570189430` → **2026-08-24 11:16:29 UTC (18:16:29 ICT)**, `statementType=SCRIPT`,
  `numChildJobs=13`, **`transactionInfo.transactionId` present** (proof the transaction actually
  committed), `totalBytesProcessed=921`, `state=DONE`, no error. Its stored query text is
  byte-identical to `623d1db`'s correction script. Its child-job sequence (3 pre-asserts → `UPDATE`
  → `UPDATE` → 2 post-asserts → final `SELECT`) is a complete, successful run of the exact reviewed
  transaction — this **is** the deployment, not a mystery actor. It ran **9 minutes after my PASS
  review landed** (`61752de`, 18:07:18 ICT) and 21 minutes after the 10:55 UTC pre-state snapshot
  the script's own baked-in assertions target — consistent, coherent timeline, not an anomaly.
  I confirmed I am not the source: `bq_safe_query.sh` never sets a custom `--job_id` (any job I run
  through it gets an auto-generated `bqjob_...` id), both my invocations of this script were
  `--dry-run-only` (logged as such, 0 bytes, no `transactionId`), and the `codex_...` naming
  convention matches every other Codex-run job throughout this queue's history.

## What this means

The actual sequence is: the correction **succeeded on its first real attempt** at 18:16 ICT, shortly
after my review PASSed — not "failed closed with an unknown writer" as `0005f75` documents it. The
23:03 ICT job (`bqjob_r73ffe406eff06ea6...`) was a **second, redundant attempt** — by whichever
session opened this RQ, apparently unaware the correction had already succeeded five hours earlier —
that correctly failed closed against the now-changed pre-state exactly as designed. No double-apply
occurred (good — the fail-closed design worked as intended), and the live state is exactly the
Boat-approved target (16:00 ICT). So there is no data-correctness problem and no further action is
needed on the production tables themselves.

But the artifact's central claim — "recent all-user job history and available Cloud audit logs did
not identify the earlier writer" — is demonstrably wrong, found with one query against a table this
project already uses routinely for this exact purpose. This is a genuine Class-A traceability/honest-labelling failure (checklist 1, 12), not a nitpick: it turns a fully-attributable, fully-successful
deployment into a documented "unresolved provenance" incident, which is a strictly worse and more
alarming record than the truth. There is also an open governance question this evidence does not
address: `0005f75` records Boat's execution approval only at ~23:04 ICT, but the actual successful
deployment happened at 18:16 ICT — nothing in the record shows an explicit "deploy OK" for that
earlier run. Either an earlier approval existed and wasn't logged, or the DEPLOY GATE requirement
(explicit human "deploy OK" in that session before deploying, `docs/AGENT_RULES.md`) was not met for
the run that actually executed. This needs Boat's confirmation, not a documentation guess.

## Required corrections

1. Correct `docs/knowledge/20_SAP_PROGRESS.md`, `30_SAP_CHANGELOG.md`, and
   `docs/sessions/2026-08-24-codex.md` to name `codex_august_cutoff_1600_20260824_1805` as the
   successful deployment (18:16:29 ICT, transaction committed, matches reviewed `623d1db` exactly)
   and reframe the 23:03 ICT job as a redundant second attempt that correctly failed closed — not an
   "unknown writer" incident.
2. Resolve, with Boat, whether an explicit deploy-OK preceded the 18:16 ICT execution. If not, this
   is a DEPLOY GATE process gap to record and close, even though the outcome itself was correct.

Neither correction requires any further production mutation — the live cutoff is already the
approved 16:00 ICT value and needs no fix. This BLOCK is on the record's accuracy, not the database
state.

## Checklist 1–12

1 traceability **BLOCK** (the central "unresolved writer" claim is false and was falsifiable with
one query); 2 provenance **BLOCK** (doesn't correctly attribute to its own prior job); 3 NULL-safety
N/A; 4 ordering N/A; 5 column order N/A; 6 grain PASS (post-check correctly scoped to the two
target rows); 7 distribution N/A; 8 knowledge **BLOCK** (the "no rerun or rollback performed, no
production mutation attributed" framing is inconsistent with what actually happened — a real,
successful, attributable mutation occurred and isn't acknowledged as such); 9 scope PASS (evidence
only touches read-only verification of the two in-scope tables); 10 rollback PASS (correctly
declines to rerun/rollback given the already-correct live state); 11 cost hygiene PASS (both cited
jobs' byte counts verified exactly: 0 bytes failed attempt, 199 bytes post-check); 12 honest
labelling **BLOCK** (this is the core issue — "provenance unresolved" is not an honest label for a
mutation traceable in one query to a self-labeled, byte-identical, transaction-committed job).

Summary: the live production state is correct and safe — no further database action needed. The
evidence record is not: it misses its own successful deployment and misreports it as an untraceable
incident. Correct the record and get Boat's confirmation on the deploy-approval timing gap; no
re-review of the SQL logic itself is needed (that was already PASSed in `623d1db`'s review).
