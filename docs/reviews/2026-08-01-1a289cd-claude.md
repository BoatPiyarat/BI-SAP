# Review — 1a289cd + b6bc1c3 (E1–E3 / F1–F3 implementation)

**Queue entry:** RQ-20260801-1200-e1-e3-f1-f3 · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS WITH NOTES** — all three Boat-required confirmations verified against the
actual files; four non-blocking notes. Deploy of 037 proceeds per Boat's chain-① instruction.

## Boat's three explicit confirmations

1. **Tier field = OrderDate alone — CONFIRMED from the file** (not the commit message):
   `DATE(d.order_create_time) AS order_date` (line 132) → `EXTRACT(YEAR FROM e.order_date) AS
   order_year` (line 150) drives every tier decision (register inserts + population WHERE).
   `date_basis = GREATEST(order_create_time, policy_start_date)` is retained separately and used
   only as the processing basis — the header states it explicitly ("it is not the tier field").
   The demanded tier-delta evidence was supplied (job `bqjob_r7ce7c676734cde22…`), and it passes
   the structural sanity check: OrderDate ≤ GREATEST always, so transitions can only move records
   to earlier tiers — exactly and only the three downward transitions are reported
   (≥2026→≤2024: 75/68; ≥2026→2025: 9,719/6,176; 2025→≤2024: 9,318/6,542; per-transition counts,
   not globally unique — correctly labelled).
2. **Rule-code taxonomy — reconciled; Codex's no-active-consumer claim independently verified.**
   My own grep over `sql/**` + `scripts/**` at HEAD: old codes appear only in (a)
   `sql/adhoc/20260731_gap_quantification…` — a dated one-shot audit whose result (373,044) is
   explicitly superseded; (b) comments in 032/033; (c) `034`, which is marked
   historical/do-not-apply. **No active monitor, script, or view hardcodes the old codes**; the
   new `046` morning report consumes `rule_code` dynamically (`GROUP BY rule_code`). New taxonomy:
   `DATE_BASIS_MISSING · YEAR_OUT_OF_SCOPE · YEAR_2025_NON_CANCEL_EXCLUDED · TEST_CUSTOMER ·
   INSURER_NOT_IN_MASTER` (kept).
3. **2025 dual-condition — preserved and exact.** Register excludes
   `NOT (already_in_sap AND IFNULL(is_cancelled_effective, FALSE))`; population admits
   `already_in_sap AND IFNULL(is_cancelled_effective, FALSE)`. Complement is an exact partition of
   2025 rows — no gap, no overlap; NULL cancellation state fails closed to excluded. The named
   failure mode (cancel-only without the SAP-existence check) is absent.

## Rest of the RQ scope

- **No silent drops:** every population WHERE predicate has a matching register write
  (NULL order_date → DATE_BASIS_MISSING; ≤2024 → YEAR_OUT_OF_SCOPE; 2025-non-cancel; test name;
  insurer). Register rows may overlap across rules — consistent with the established
  per-rule-counts-overlap convention.
- **E3:** master seeded from `SAP_LIVE_FULL` valid positive DocEntry with the **same**
  canonicalization expression used at match time in 037 — both sides consistent, which is the
  failure mode that matters.
- **E2:** exact-match `['test','test div']` after LOWER/TRIM; no substring matching.
- **F1:** `insured_id` normalized at the shared staging source; added mid-list to
  `expected_state`'s output — safe: all consumers (017/018/030/035/046) select named columns
  (verified in the earlier blast-radius pass).
- **F2:** detail carries `flow` + `LENGTH(PolicyNo)` only — no PolicyNo value, no truncation. PII-clean.
- **F3:** 12 flows enumerated by name with named columns (comment correctly distinguishes
  ordinal CSV consumption from semantic validation); date value in detail is not PII.
- **Guards intact:** `b881fa3`'s full active-period guard survives the rewrite (active-row filters,
  `active_period_count = 1`, non-future ASSERT) **plus** a new config-pin
  `ASSERT year_no_touch_max = 2024 AND year_cancel_only = 2025` — good addition.
- **MISSING measurement:** 5,712 + 32 = 5,744 ✓ against the stated live input, with full job
  provenance; old 373,044 superseded.

## Notes (non-blocking)

1. `DATE_BASIS_MISSING` fires on `order_date IS NULL` — the code name says date_basis but the
   condition (and reason text) is OrderDate; rename to `ORDER_DATE_MISSING` at next touch to keep
   the register self-describing. (Structurally harmless: date_basis NULL ⟹ order_date NULL, so
   coverage is complete either way.)
2. `TEST_CUSTOMER_PHONE` is dropped by design ("no phone-based E2 exclusion"). It was report-only
   (never removed rows), so the population is unaffected — but the register line (341 rows in the
   pre-change capture) goes silent. Anyone watching that count should be told once.
3. RULE-09 is formally superseded (`old_year_rescued` hardcoded FALSE, schema-compatible; RULE-09
   runbook marked DO NOT RUN). My `aba1aad`/`1f3e14a` review content about RULE-09 semantics is
   superseded to the same extent; `CURRENT_STATE` RULE-09 row needs the annotation (Codex lane).
4. Stale comments: 032/033 still name the old code taxonomy in prose.

Queries used: 0 (artifact + grep verification; tier-delta and MISSING evidence carried their own
job provenance).
