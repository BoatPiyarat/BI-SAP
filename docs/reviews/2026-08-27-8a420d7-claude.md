# Review: daily-completeness fail-closed lookups, delta review

Reviewer: Claude Code
Artifact: commit `8a420d7`; `infra/v3_nightly_orchestrator.workflows.yaml`
Prior BLOCK: `docs/reviews/2026-08-27-efd6de0-codex.md` (RQ-20260811-1845, delta of `efd6de0`)
Verdict: **PASS**

## Session limitation, stated up front

No live BigQuery/Workflows execution available in this session — this is a static trace of the
YAML control flow against the prior BLOCK's two required corrections, not a live run.

## Verifying both required corrections

I read `count_export_runs_for_pipeline` and `derive_original_pipeline_run_id` in full (not just
the diff hunks) to confirm the fall-through path after each new gate.

1. **Query/connector exceptions.** Both `googleapis.bigquery.v2.jobs.query` calls are now inside
   `try:`/`except:`, routing to `fail_closed` with a distinct `step` label
   (`DAILY_COMPLETENESS_EXPORT_COUNT` / `DAILY_COMPLETENESS_ORIGIN_LOOKUP`) and the caught
   exception serialized into `reason`. Resolved.
2. **Malformed nested cell dereference.** Both subworkflows now gate, in order, before any
   dereference: `jobComplete` (pre-existing) → `len(rows) == 1` (pre-existing) → **new**
   `len(f) == 1` on the single row → **new** read the cell's `v` via
   `default(map.get(...), "")` → **new** reject blank/non-numeric (count) or blank (origin) before
   `int()`/return. `default()` treats BigQuery's JSON-null cell the same as an absent key, so a
   real SQL NULL is caught by the same blank check, not just a missing field. Resolved.

`fail_closed` (line 1353) ends in an unconditional `raise`, confirmed by reading its definition —
so every one of these new gates actually terminates the workflow rather than falling through to
the `return` step on a matched failure condition. This is the same pattern the two pre-existing
gates (`jobComplete`, row-count) already used and were previously passed on, so the new gates
inherit already-reviewed control-flow semantics rather than introducing a new pattern.

## Checklist 1–12

1. **Traceability — PASS.** Exact commit, exact prior review, exact two required corrections
   named and traced line-by-line above.
2. **Provenance — PASS.** Reuses the prior BLOCK verdict and the already-reviewed `fail_closed`
   subworkflow rather than re-deriving anything.
3. **NULL-safety — PASS.** Field presence, cardinality, and value-blankness are all gated before
   dereference in both subworkflows; this was the exact BLOCK.
4. **Ordering — N/A.** No ordering/date logic in this delta.
5. **Column order — N/A.**
6. **Grain — PASS.** No join or aggregation grain changed; only added gates on the same two
   single-row/single-field results.
7. **Distribution — N/A.** No distribution or count-parity claim made.
8. **Knowledge consistency — PASS.** Matches the fail-closed convention already used by the
   sibling `gate_complete` steps and by `fail_closed` itself.
9. **Scope — PASS.** Changes are confined to the two named subworkflows; the SQL query strings
   are byte-identical to before (verified via diff — only surrounding YAML structure changed).
10. **Rollback — N/A.** Source-only; not deployed by this artifact.
11. **Cost hygiene — PASS.** No new query was introduced; the two existing `SELECT` strings are
    unchanged, so no new dry-run is needed for them. The claimed DDL 067 dry-run (0 bytes,
    2026-08-27) is for an unrelated, unchanged file cited only as evidence nothing else drifted.
12. **Honest labelling — PASS.** The claim ("closes both BLOCK findings... do not deploy or mutate
    a scheduler") matches exactly what the diff does — no deploy, no CALL, source only.

No BLOCK, no notes requiring another round. Both required corrections from `efd6de0-codex.md` are
verifiably closed by tracing the full step sequence, not just the changed hunks.
