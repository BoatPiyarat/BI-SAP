# Review: RQ-20260824-1333 v3-ddl058-deploy-evidence

Reviewer: Claude Code
Artifact: `bd5331b`; deployment evidence for reviewed DDL 058 source `5afb5ff`
Verdict: **PASS WITH NOTES**

## Verification performed

Per protocol, metadata-only reads (`git hash-object`, `bq show -j`, `bq ls -j --parent_job_id`) plus
one small `INFORMATION_SCHEMA` query for the live routine body/timestamp and one for the identity
table's creation time (both ~0.01 GiB dry-run, well under cap) — not counted against the one-query
budget per the metadata-read precedent in prior reviews.

- **Reviewed-source identity: exact.** `git hash-object sql/ddl/058_v3_unit5_newpayment_shadow.sql`
  on the current working tree and on `5afb5ff` both produce `0e8fab0a52e10d7e27fd4d6c058f0f81ff8a3ecc`
  — byte-identical, and `git diff 5afb5ff -- sql/ddl/058_v3_unit5_newpayment_shadow.sql` is empty.
  Matches the claimed blob hash exactly.
- **Deployment job: found and verified.** `bqjob_r4b8f99fa9291b2_000001a0327782de_1` (under
  `location=asia-southeast1`) is a `SCRIPT` job, `totalBytesProcessed=0` / `totalBytesBilled=0`,
  `creationTime=1787553088688` → `2026-08-24 06:31:28 UTC` (13:31:28 ICT) — matches the claimed
  06:31:29 UTC to within the same second. **Its stored `configuration.query.query` text is
  byte-for-byte the same DDL 058 script** — I read the full stored text and it ends at the
  procedure's closing `END;` with no trailing `CALL`, matching the "definition only, no CALL"
  claim directly from the job's own record rather than trusting the commit message. Two child jobs
  (`script_job_..._0`, `script_job_..._1`) correspond to the `CREATE TABLE IF NOT EXISTS` and
  `CREATE OR REPLACE PROCEDURE` statements — both 0 bytes, consistent with pure-DDL statements.
- **Identity table genuinely untouched.** `INFORMATION_SCHEMA.TABLES` shows
  `v3_unit5_payload_identity` has `creation_time = 2026-08-02 15:25:52`, three weeks before this
  deploy job — proves `CREATE TABLE IF NOT EXISTS` was a real no-op skip, not a silent recreate.
- **Live routine body and timestamp: exact match.** `INFORMATION_SCHEMA.ROUTINES` for
  `sp_build_v3_newpayment_shadow` returns `last_altered = 1787553089663` (matches the claimed
  `lastModifiedTime` exactly, digit-for-digit) and `has_paid_mapping=true`,
  `has_pending_mapping=true`, `has_exact_status_assert=true` — all three match the claim exactly,
  verified against the live object rather than re-trusting the source-review from `5afb5ff`.
- **No runtime/delivery claim overreach**: nothing in `bd5331b`'s docs claims a fresh run, a
  candidate rebuild, or delivery — `docs/AS_BUILT_V3.md` and `20_SAP_PROGRESS.md` both explicitly
  state "has not yet been CALLed" / "Fresh Unit 1–5 execution remains the next separately gated
  step," consistent with what the deployment job itself contains (definition only).

## Note (non-blocking)

Checklist item 10 (rollback) is satisfiable but not explicitly stated in this record: the previous
`sp_build_v3_newpayment_shadow` definition (pre-`5afb5ff`, without the lowercase-status mapping) is
only recoverable by going to git history one commit back and re-running `CREATE OR REPLACE
PROCEDURE`. That is a real, fast (<5 min) rollback path, but the deployment record doesn't name the
prior commit or state the rollback command explicitly the way some other deploy-evidence entries in
this queue have. Suggest adding one line naming the pre-deploy commit for anyone who needs to roll
back without re-deriving it from `git log`.

## Checklist 1–12

1 traceability PASS (job ID/timestamp/blob-hash/routine-timestamp all independently re-verified
against live objects, exact matches); 2 provenance PASS (cites `5afb5ff` and its own review, doesn't
re-derive already-settled DDL logic); 3 NULL-safety N/A (deployment evidence, no new logic); 4
ordering N/A; 5 column order N/A (procedure definition only, no interface file emitted by this
artifact); 6 grain N/A; 7 distribution N/A; 8 knowledge PASS (consistent with the prior review and
Boat's recorded approval; no contradiction); 9 scope PASS (deploy touches only
`sap_integration_v3.sp_build_v3_newpayment_shadow`, docs updates match); 10 rollback NOTE (see
above — satisfiable via git history, not explicitly named in the record); 11 cost hygiene PASS (0
bytes dry-run and 0 bytes actual, confirmed directly from job metadata, not just the claim); 12
honest labelling PASS (explicitly states no CALL occurred and fresh execution is a separate gated
step — confirmed true from the job's own stored query text).

Summary: Checklist 1–12 reviewed; every load-bearing number in the claim (blob hash, job ID,
timestamp, routine last-altered, body-content flags, identity-table creation time) was independently
re-derived from the live objects rather than trusted from the commit, and all matched exactly. One
non-blocking note: name the pre-deploy commit explicitly for a faster rollback path next time.
