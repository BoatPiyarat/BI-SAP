# Review: RQ-20260823-2039 RCB onetime CreditShell fix

Reviewer: Codex  
Artifact: `f25af4f`  
Verdict: **BLOCK**

## Standards

1. **BLOCK — prohibited deployment target.** The artifact proposes `CREATE OR REPLACE VIEW` on
   `sap_integration_v2.RCL 04_new order credit shell new tunning`; `AGENT_RULES.md` permits DDL
   only in `sap_integration_v3` and explicitly forbids CREATE/ALTER/DROP in v2. Source preparation
   is non-mutating, but the stated deployment unit cannot PASS without a newer canonical exception.
2. **BLOCK — numeric provenance incomplete.** The new 59,494 rows / 12,084 items / 10,721 changed
   rows / 7,557 changed items claims name the view but not the reproducible query/job ID and exact
   source timestamp required by the verification and review rules.
3. **Note — possible duplicated routing logic.** The identical predicate/result is repeated in the
   PaymentMethod and PaymentChannel CASE expressions. This matches surrounding style but needs a
   durable equality assertion to prevent drift.

## Spec

1. **BLOCK — unknown/NULL flow remains fail-open.** The finding says to hold/report unknown flow,
   but both CASE expressions retain the unconditional carried-over fallback to `RCL-Credit Shell`.
   Lack of a hold mechanism does not satisfy the stated requirement.
2. **BLOCK — required durable regression assertion missing.** The spec requires no ONETIME row to
   have PaymentMethod or PaymentChannel matching `RCL%`; a one-off comparison is not that guard.
3. **BLOCK — CREDIT_CARD_INSTALLMENT scope is too broad.** The new branch does not enforce the
   specified ONETIME invariant `TotalPeriods=1`.

## Checklist 1–12

1 traceability BLOCK (missing job/query/timestamp); 2 provenance PASS; 3 NULL-safety BLOCK
(unknown/NULL defaults to RCL); 4 ordering N/A; 5 column order PASS (additive CTE field only);
6 grain PASS; 7 distribution PASS; 8 knowledge BLOCK (v2 DDL target); 9 scope PASS; 10 rollback
BLOCK (no permissible deploy/rollback path); 11 cost hygiene PASS (8.47 GiB dry-run stated, but job
provenance must be added); 12 honest labelling PASS (source-only and no backfill clearly stated).

Summary: Standards 2 blockers + 1 note; Spec 3 blockers. Worst standards issue is prohibited v2
DDL; worst spec issue is retaining fail-open unknown-flow routing.
