# Review: RQ-20260823-2304 post-import-activation

Reviewer: Claude Code
Artifact: `be60c7e`; inert post-import runtime identity/subscription/scheduler activation
Verdict: **PASS**

## Verification performed

Ran `scripts/check_post_import_activation.ps1` live myself (fully read-only: `gcloud
workflows/run/pubsub/scheduler describe|list|get-iam-policy` only, no mutating verb anywhere in the
script) — this is the one targeted check for this review, matching the precedent set in
`docs/reviews/2026-08-06-8e045ad-claude.md` ("ran both checkers live" rather than trusting output
described in the commit).

Result, independently obtained at `2026-08-23T16:23:12.9696141Z` (≈21 minutes after the cited
`16:02:57Z` checker run in the request):

- `dispatcher_revision: sap-post-import-dispatcher-00002-zjl` — **exact match** to the cited
  revision.
- `watchdog_image` digest `sha256:9d718275af3ea5c7b928777e52abd7b0855271cb211f05431b162dde6e8ab87e`
  — **exact match** to the cited digest.
- `dispatcher_environment` and `watchdog_environment` both show correctly separated key/value
  pairs (`PROJECT_ID`, `BQ_LOCATION`, `WORKFLOW_NAME`, `WORKFLOW_LOCATION`, `BQ_DATASET` for the
  dispatcher; `PROJECT_ID`, `BQ_LOCATION`, `EXECUTION_TIMEOUT_SECONDS=900`,
  `CLAIM_TIMEOUT_SECONDS=600`, `ALERT_TOPIC`, `CANCEL_WAIT_SECONDS=120`, `BQ_DATASET` for the
  watchdog) — consistent with the claimed comma-parsing defect (which would have collapsed all
  values into `PROJECT_ID`) being genuinely fixed, not just described as fixed.
- `safety_failures: []`, `readiness_blockers: []`, `safety_passed: true`, `rehearsal_ready: true` —
  reproduced bit-for-bit against the request's claim.

## Review focus items

- **Private ingress / no public invocation** — PASS. The checker's own failure conditions cover
  `ingress != internal` and `allUsers`/`allAuthenticatedUsers` membership; zero failures returned.
- **Exact identities/env/timeouts** — PASS. Both dispatcher and watchdog service accounts matched
  the approved default Compute identity (a mismatch is an explicit `safetyFailures` condition), and
  the watchdog's 600/900/120 timeout triple is visible correctly in my own output.
- **Push/DLQ contract** — PASS. `readiness_blockers` empty covers topic binding, push endpoint,
  OIDC service account + audience, DLQ topic + `maxDeliveryAttempts=5`, and
  `ackDeadlineSeconds=600` — all in one boolean, but the underlying script asserts each condition
  individually before folding into that list, and I read the script source line-by-line rather than
  trusting the aggregate flag alone.
- **PAUSED scheduler** — PASS. Same reasoning: the checker's own scheduler block requires
  `state == PAUSED`, exact cadence `*/2 * * * *`, `Asia/Bangkok`, correct run URI, and correct OAuth
  service account, all before it would report zero readiness blockers.
- **Honest no-execution boundary** — PASS. Cross-checked against
  `docs/design/DEFAULT_COMPUTE_SA_AUTOMATION_WORKAROUND.md` (already-PASSed design, `8e045ad`):
  its own required invariants — non-public Cloud Run, `delivery_enabled` false, scheduler PAUSED
  until rehearsal+cutover — are exactly what this commit claims to hold, no drift from the approved
  design. No IAM grant was claimed or observed; the checker script contains no `add-iam-policy-binding`
  or similar call.
- **Caught/corrected PowerShell environment-list error** — not independently reproducible (it
  describes a since-fixed defect in a prior run), but consistent with what I now observe: the
  current environment maps are correctly key-separated, which is what the described fix would
  produce and what the described bug (all values landing in `PROJECT_ID`) would NOT produce.

## Checklist 1–12

1 traceability PASS (job/revision/digest/timestamp all independently reproduced, not just quoted);
2 provenance PASS (cites the exact prior design docs and revision/digest identifiers instead of
re-describing them); 3 NULL-safety N/A (infra activation, not a data query); 4 ordering N/A; 5
column order N/A; 6 grain N/A; 7 distribution N/A; 8 knowledge PASS (checked against
`DEFAULT_COMPUTE_SA_AUTOMATION_WORKAROUND.md`'s required invariants — no contradiction); 9 scope
PASS (Cloud Run/Pub/Sub/Scheduler activation only, no BigQuery/SAP/GCS action, no IAM mutation); 10
rollback PASS — redeploying a Cloud Run revision is reversible by redeploying the prior revision,
and the scheduler stayed PAUSED throughout so nothing is currently running that would need
"rolling back" in the operational sense; 11 cost hygiene N/A (no BigQuery cost involved; the
checker itself is a handful of free `describe`/`list` API calls); 12 honest labelling PASS — the
commit explicitly lists what did NOT happen (no watchdog execution, no rehearsal, no Gmail
publication, no SAP action, no GCS write) and does not claim more than the checker actually proves.

Summary: Checklist 1–12 reviewed; no gap found. Every cited identifier and boolean reproduced
independently, live, ~21 minutes after the original check.
