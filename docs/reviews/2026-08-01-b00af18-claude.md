# Review — b00af18 (R1 un-retraction: loader retry commits amplify SAP_LIVE)

**Queue entry:** RQ-20260731-2345-r1-loader-retry-confirmation · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked: (1) all five rows of the LOAD-job table multiply out exactly (41×60,404=2,476,564;
70×60,385=4,226,950; 25×58,619=1,465,475; 12×61,133=733,596; 1×27), zero bad records — per-job
`statistics.load.outputRows` from `JOBS_BY_PROJECT` is the strongest evidence class available for
this claim, with full job/byte provenance. (2) The mechanism claim is precise and matches the
evidence: LOAD commits → container OOM → request 503 → Pub/Sub redelivery → identical LOAD again;
the 31-Jul live sequence (12 commits on revision 00019, success+delete at 16:37:43Z on 00020 after
the 4 GiB raise) is a real closed loop, not inference. (3) The epistemics are handled correctly:
this does **not** revive the *original* R1 wording (crash-loop re-reading the bucket / watermark
reset — still retracted, watermark advanced continuously); it establishes a different, evidenced
mechanism on the load side, Boat-confirmed, with A2/A3 interface-import churn explicitly kept as a
contributing factor. One-day offset between OOM storms (27–29) and amplified batches (26–28) is
consistent, including the exact 25×↔25.000× correspondence with A4's 07-28 ratio. (4) The 22–24 Jul
no-POST/no-JSON gap is flagged as possible permanent mirror loss with a concrete consequence —
(ก)/(ง) depend on absence-from-SAP, so affected OrderItems need separate measurement after the
post-load rerun. This caveat must ride along with any FA-facing (ก)/(ง) number.

Interaction flag: the finding names 043's idempotent MERGE as the destination-side prevention —
043 is currently **BLOCK** (`2026-08-01-043-merge-claude.md`, two CALL-time defects with fixes
proposed). This finding raises the priority of that fix; it does not change the BLOCK.

Knowledge follow-up (Codex lane): `CURRENT_STATE_20260731.md` §4's R1 retraction row now needs the
un-retraction annotation so the retraction table and this finding cannot be cited against each other.

Queries used: 0.
