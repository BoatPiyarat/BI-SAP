# Review — f74f605 (July InsurerCode exclusion risk quantification)

**Queue entry:** RQ-20260731-2225-insurer-exclusion-risk
**Class:** A (money-adjacent: THB 2.26M of July-PaymentDate records currently excluded)
**Reviewer:** Claude Code (review clone, committed refs only)
**Verdict:** **PASS**

| # | Result |
|---:|---|
| 1 | PASS — full provenance: job `p0_insurer_risk_20260731_152353`, result timestamp `2026-07-31 15:23:55 UTC`, creation/start/end times, dry-run vs billed bytes (371,716,873 / 372,244,480), ceiling stated, all 8 source tables named. |
| 2 | PASS — reuses procedure 037's own payment-event/compulsory-item logic rather than re-deriving a parallel definition; cites `sap_config.year_no_touch_max=2024` at the same timestamp. |
| 3 | PASS — "Amount NULL count was zero" is checked and stated, not assumed. |
| 4 | PASS — PaymentDate window is a half-open range `[2026-07-01, 2026-08-01)`; satang/100 conversion stated. |
| 5 | N/A — no interface-feeding object changed. |
| 6 | PASS — grain explicit throughout (300 records / 294 orders); the 4,810-unique vs 4,816-summed distinction is stated with the 6-order overlap, and the earlier G1 gross-vs-post-exclusion mischaracterization was **self-corrected with a targeted job** (`g1_exclusions_20260731_150736`, bytes and timestamps recorded) rather than left standing. |
| 7 | PASS — per-code breakdown (`30`, `46`, `48`, `49`) requiring individual Aware disposition, not one aggregate. |
| 8 | PASS — flags (rather than resolves) the real conflict: 037's exclusion `date_basis` is still `GREATEST(OrderDate, PolicyDate)` vs the locked PaymentDate basis (R13). EXCLUDED ≠ DELETED honored. |
| 9 | PASS — finding + canonical input updates only; the control improvement (extend `sap_excluded_records` with amount/date_basis) is explicitly deferred to reviewed DDL post-close. |
| 10 | N/A — no production change. |
| 11 | PASS — targeted jobs, both far under ceiling, byte counts recorded. |
| 12 | PASS — status OPEN, "Aware confirmation required"; no disposition inferred per code. |

**Named risk:** the 300/294/2.26M figures depend on reconstructing pre-exclusion PaymentDate with
037-equivalent logic; if 037's basis is later changed to PaymentDate (the open R13 alignment), the
exclusion population itself may shift and this finding should be re-run — worth one line in the
finding so the number isn't quoted past that change.

Queries used: 0 (evidence judged from artifacts; the author's jobs are fully identified and
re-runnable if Boat wants an independent reproduction).
