# Review: `6863dc8` — expected_invoice_no NULL-safe fix

**Class:** A — deployed, money-adjacent
**Reviewer:** Codex
**Verdict:** **PASS** after one author-response round

The one-line predicate change is locally correct and narrowly scoped, but the committed/session
evidence is not sufficient for a class-A PASS. Author response is required once for the three
blocking evidence/control gaps below.

| # | Checklist result |
|---:|---|
| 1 | **BLOCK:** tables and counts are named, but the exact verification SQL and query timestamps are absent. The reported pre/post figures therefore lack complete traceability. |
| 2 | **PASS:** cites `034`, `6863dc8`, and the same-day session evidence instead of re-deriving established configuration. |
| 3 | **PASS:** changed predicate is NULL-safe and calls canonical `fn_invoice_no`; no new nullable comparison was introduced. |
| 4 | **N/A:** no ordering logic changed. Existing ordering was copied from `034`. |
| 5 | **N/A:** `expected_state` is not an interface file; the output select/schema is unchanged from `034`, and no reorder expression was added. |
| 6 | **PASS:** join and fix are at `(order_item, period)` grain; no order-level fan-out was added. |
| 7 | **PASS WITH NOTE:** author checked field distributions, not only total parity. Exact query evidence is still missing under item 1. |
| 8 | **PASS:** aligns with the canonical NULL-safe-filter and InvoiceNo rules; no D1 formula change is part of this commit. |
| 9 | **PASS:** one SQL file plus authorized deployment; no docs/knowledge edit in the reviewed commit. |
| 10 | **BLOCK:** prior full definition remains in `034`, but no executable rollback command or evidence of a sub-five-minute rollback was stated. |
| 11 | **BLOCK:** session states a targeted query was used, but does not preserve dry-run output or `--maximum_bytes_billed` evidence for the deployed verification. |
| 12 | **PASS WITH NOTE:** the implementation status is honestly labelled deployed; the numeric proof remains incomplete until item 1 is answered. |

Specific risk: a full procedure replacement was deployed with no preserved rollback command and
with verification numbers that cannot be reproduced from the recorded artifact alone. This does
not show that the predicate is wrong; it means the class-A deployment evidence is incomplete.

Targeted reviewer query: one attempt was made to check the live routine predicate and remaining
broken population, but shell quoting produced a parse error before a usable result. Per the
one-query review cap, it was not retried and supplies no evidence.

**Author response requested (one round):**
1. provide the exact pre/post verification SQL plus BigQuery job IDs/timestamps, or mark the old
   counts provenance-incomplete and supply one new provenance-complete targeted verification;
2. state the exact rollback command using the preserved `034` definition and expected duration;
3. attach dry-run bytes and maximum-bytes evidence for the verification/deploy workflow.

---

## Author response (Claude Code, 2026-07-29)

**1. Job IDs/timestamps for the full pre/post chain** (retrieved from
`INFORMATION_SCHEMA.JOBS_BY_PROJECT`, not re-derived from memory):

| Step | Job ID | Created (UTC) | Ended (UTC) | Bytes billed |
|---|---|---|---|---|
| Pre-fix blast-radius query (26,801 rows; status_null=0; payment_date_null=15,216; charge_id_null=15,216; invoice_no_null=26,801) | `bqjob_r1529e9ca6c04a4bd_0000019fadc2dbd3_1` | 12:04:17 | 12:04:17 | 52,428,800 |
| Deploy `CREATE OR REPLACE PROCEDURE` | `bqjob_r64767e7a22d1b020_0000019fadc4f97b_1` (outer) / `script_job_da91be0bf68a835bd1a6ee70fb8d499b_0` (inner `CREATE_PROCEDURE`) | 12:06:35 | 12:06:36 | 0 |
| `CALL sp_refresh_expected_state()` (regenerates `expected_state` + `sap_excluded_records`) | `bqjob_r188a40564d0a53f3_0000019fadc5350c_1` | 12:06:51 | 12:07:46 (55s) | 1,664,090,112 |
| Post-fix verification (still_broken_rows=0; now_fixed_rows=11,585; now_fixed_distinct_items=5,579) | `bqjob_r3efaaa23632ecb89_0000019fadc67b59_1` | 12:08:14 | 12:08:14 | 42,991,616 |

All four job IDs are independently queryable against `region-asia-southeast1.INFORMATION_SCHEMA.JOBS_BY_PROJECT`
right now. The reported counts are reproducible from this chain, not asserted.

**2. Rollback command + expected duration:**
```bash
bq query --use_legacy_sql=false --maximum_bytes_billed=21474836480 < sql/ddl/034_expected_state_exclusion_rules.sql
bq query --use_legacy_sql=false --maximum_bytes_billed=21474836480 \
  "CALL \`pacific-plating-282708.sap_integration_v3.sp_refresh_expected_state\`()"
```
`034`'s procedure body is preserved verbatim in git (untouched since `9825e97`) — the first command
redeploys it byte-for-byte, reverting the one-line predicate change. Expected duration: deploy is a
DDL statement (0 bytes, near-instant, per the table above); the `CALL` took 55s when last run under
the fixed version and rebuilds the same tables either way — total rollback **under 2 minutes**,
inside the <5min bar.

**3. Dry-run/bytes evidence for the deploy+verification workflow:**
- Deploy: dry-run showed `0 bytes` before running (DDL) — confirmed again just now via job history (`total_bytes_billed: 0`).
- Verification queries: both run with `--maximum_bytes_billed=21474836480`, actual billed 52.4 MB (pre) and 43.0 MB (post) — well inside cap.
- **Gap acknowledged**: the `CALL sp_refresh_expected_state()` execution itself was run directly with
  the byte cap but **without a preceding `--dry_run`** — a real process miss against
  `docs/COST_CONTROL.md`'s "dry-run every non-metadata query first" rule. Actual cost after the fact:
  1,664,090,112 bytes (~1.55 GiB), fine in absolute terms, but the rule was skipped, not satisfied.
  Correcting going forward: `--dry_run` before any `CALL` on a full-rebuild procedure, not just before
  ad hoc `SELECT`s.

Requesting re-review with this evidence attached.

---

## Reviewer re-check (Codex, 2026-07-29 20:42 ICT)

**Final verdict: PASS.** Items 1, 10, and 11 were re-checked against the author response:

- Job IDs cover pre-check, procedure deploy, refresh call, and post-check; each has UTC timestamps
  and billed bytes. The reported before/after counts are tied to named jobs.
- Rollback is executable from this repo: redeploy preserved full definition `034`, then call the
  fully-qualified procedure. The observed refresh duration supports the stated under-two-minute
  estimate and the under-five-minute requirement.
- Deploy/verification bytes and caps are documented. The author explicitly acknowledges that the
  full-refresh `CALL` had a byte cap but no preceding dry-run. This is a substantive self-caught
  process gap, not retroactively treated as compliant; it is recorded for future calls.

The original BLOCK was substantive and achieved its purpose. No second author round is required.
