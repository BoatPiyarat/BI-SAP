# Review — 213626c (V3 July production runbook)

**Queue entry:** RQ-20260801-2243-v3-july-production-runbook · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **BLOCK** — inherits RQ-2241's two blocks and owns one of them: the runbook is the
document that must carry the missing UAT2 stage.

Grounds (shared with `2026-08-01-73f7c08-claude.md`):
1. Its scripts are blocked (048 UNION type failure — confirmed by reproduction).
2. **No UAT2 stage exists between "Shadow hard gate" and "Production delivery"** — G3 is mandatory
   (`ห้ามข้าม`), and the unverified physical-format surface (header, quoting, NULL-money rendering
   against an unknown legacy serializer) is exactly what UAT2 exists to catch. Insert a stage:
   export the identical payload to the UAT2 path, run the known-answer field-level compare on the
   22 drift positions, get Aware/Boat acceptance of the physical file, then production.
3. Add the post-write exact-byte archive step (object copy + generation + measured hash into
   `export_archive`) before DELIVERED is treated as evidence-complete.

Everything else in the runbook is right and should survive the fix round unchanged: deploy order
with per-step dry-runs and review gates; reusing the reviewed manual-sync orchestrator for the
refresh; the shadow hard-gate checklist (gap=0, 56 columns, July min/max, no delivered key,
BatchRunDate `31072026`, DDMMYYYY parse, PolicyNo ≤50); single-CALL delivery with job-ID capture;
and the no-blind-rerun instruction for the export/DELIVERED failure window.

One addition worth making while editing: the post-delivery verification list should include the
SAP import-result email (LogID + status) for this file, tying `export_archive.sap_log_id` /
`acknowledged_at` to real evidence — the schema already has the fields; the runbook never fills
them.

Queries used: 0 (shares RQ-2241's reproduction).
