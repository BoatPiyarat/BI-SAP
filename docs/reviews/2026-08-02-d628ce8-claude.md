# Review — d628ce8 (V3 change-order preflight + flow design)

**Queue entry:** RQ-CHANGE-ORDER-V3-20260802 (normalized to standard format on review) · **Class:** A
**Reviewer:** Claude Code · **Verdict:** **PASS WITH NOTES**

## The requested confirmations — all hold

- **Lane separation:** CANCEL_OLD / CREATE_REPLACEMENT / CREDIT_SHELL_PAYMENT are documented as
  independent lanes that are never unioned until each has an explicit eligibility result; the SQL
  matches — it produces pair-grain readiness only and deliberately stops before payload
  generation, with the honest stated reason (an order-level link does not prove item-level mapping).
- **Scope boundary:** INCIDENT-002b's 244 and the 224 unknown-cause orders are excluded in both
  files; the exporter's `CHANGE_ORDER_SEPARATE_FLOW` hold is explicitly *not* treated as cancel
  authority. Consistent with the D16 canon and D2.
- **Gates, checked against the SQL:** exactly-one active period ✓ (ASSERT); old-in-SAP ✓
  (`HOLD_OLD_NOT_IN_SAP`); terminal-state ✓ (any already-cancelled row holds the pair — rule 17,
  conservatively including mixed states); full spine ✓ (invalid-period rows first, then
  `distinct_periods != max_total_periods` — together with the range check this is exact-set
  equivalent, same logic family as 035's schedule check); immutable InvoiceNo ✓ (Paid-with-NULL
  invoice holds on both the SAP and replacement sides); replacement completeness ✓ (missing from
  expected_state, Paid missing invoice/date); July-only ✓ (`expected_payment_date >=
  open_period_start + 1 MONTH` — correct on the clamped output date, since the clamp can never
  push a July raw date into August).
- **`READY_FOR_AWARE_FA_REVIEW` implies no approval:** stated in the design, the SQL header, and
  the output comment; the required-next-evidence list keeps Aware's cancellation-document answer
  and FA/Boat batch approval as human gates. CreditShell literals correctly remain a mapping gate.
- **No production action:** DECLARE/ASSERT/TEMP/SELECT only — verified by read; full-script
  dry-run validates; the highest-risk live references probed standalone
  (`sap_mirror_state.U_OrderID/UpdateDate/UpdateTime` etc. — all exist post-RULE-03 deploy).

## NOTE 1 (process — fixed during this review): the entry was invisible to the queue tooling

The request used `# RQ-… — OPEN — Class A` instead of the machine-parseable schema
(`## RQ-…` + `Status:`/`Reviewer:`/`Class:`/`Opened:` lines), so `review_status.sh` reported
**0 OPEN while this Class A request was pending** — silent review debt, exactly what the parseable
format exists to prevent. The entry is normalized in the same commit as this review. Please keep
the protocol's exact field spelling; the parser is only as honest as the headers.

## NOTES 2–3 (cheap hardening for the next iteration, not blocking a source-only preflight)

2. Add `HOLD_LINK_AMBIGUOUS` when an `old_order_id` or `new_order_id` appears in more than one
   link pair — pair-grain ambiguity is detectable now, before the item-level mapping work.
3. Add `COUNT(DISTINCT total_periods) > 1 → HOLD` per old order: `MAX(total_periods)` can mask two
   conflicting spine sizes whose union happens to look complete ({N=2 rows, N=3 rows} with periods
   1..3 passes today). Same class as the TotalPeriods-versions check already added to 035.

Queries used: 2 dry-runs, 0 bytes billed (full-script validation + live-column probe).
