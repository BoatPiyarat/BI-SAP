# Review — a52b495 (cancel/change → credit-shell track + hardened rerun + RULE-02 supersession)

**Queue entry:** RQ-20260802-cancel-change-creditshell-track · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — and all three notes from the RULE-21–30 review are closed in this same commit.

## Evidence arithmetic — closes to the row, twice over

- Hardened rerun: 7,457 + 19,123 + 1,332 + 245 + 85 + 5 + 1 + 1 + 92 = **28,341** — exact
  conservation with the first run's total. Sharper still, the *redistribution* reconciles: the two
  new gates absorbed 7,457 (ambiguous) + 5 (TotalPeriods conflict) = 7,462, exactly matching the
  drains from the first run's buckets (26,541→19,123, 1,367→1,332, 253→245, 93→92) = 7,462 ✓ —
  the strongest possible evidence the hardening changed classification and nothing else.
- Item-map: all three strategies conserve on 482 old items (216+174+92 / 263+151+68 / 427+51+4).
  The conclusion is honest and correct: max 36% unique coverage means **no automatic business key
  exists**; suffix is a clue, not authority; auto-cancel prohibited.

## RULE-21 applied correctly to its own prior result

The 92 order-level READY pairs are downgraded — order-level linkage cannot prove that the
*replacement Paid for the same item* was accepted by SAP. The mapping SQL respects its own
boundaries: unambiguous links only (degree = 1 both sides, consistent with the hardened gate), old
items grounded in `sap_mirror_state` Paid/Pending, and a Paid-in-SAP acceptance measure per strategy.

## State machine — ordering is the contract, and it is right

ITEM_MAP_PENDING → APPROVED (with approver/timestamp/method provenance — the 042 evidence style) →
REPLACEMENT_PAID_ACK (from the *refreshed mirror*, rule 27) → CANCEL_SHADOW_READY (clone all 56
fields, change only TransactionStatus — rule 18) → CANCEL_DELIVERED → CANCEL_ACK (never inferred
from function/file status) → only then CREDIT_SHELL_PENDING → READY (approved literals, corrected
dedup, amount conservation) → ACK. The subtle distinction is the best line in the design: cancel
*construction* needs no CareOS mapping (it clones the SAP document); cancel *eligibility* does
(to prove RULE-21). Legacy tracks correctly inventoried as not-readiness-evidence (pre-RULE-21/22
cancel views; credit-shell generator's confirmed duplicate defect).

## Prior-review notes — all three closed

1. **RULE-02 supersession line** now in CURRENT_STATE §1: July artifact keeps `last_day`; normal
   nightly uses real-run-date capped to month-end per RULE-25/26 — exactly the split demanded.
2. **Error-count grain** stated: 17,760 instances across 15,843 rejected rows, not disjoint buckets.
3. **Mojibake root-cause owner** assigned (DE/V3 payload-source, Codex implementation + Boat
   approval) with the Unicode-trace instruction — rule 28 correctly repositioned as containment.

Boundaries intact: 002b/224 separate; Aware owns explicit-cancel + literals; FA/Boat own batch
approval; no deploy/export/legacy modification implied anywhere.

Queries used: 0 (arithmetic + cross-run reconciliation from recorded evidence).
