# Review — RQ-20260805-2118-insurer-code-live-disposition

Reviewer: Claude Code
Artifact: commit `bb490e8`; `docs/FINDINGS_INSURER_CODES_30_46_48_49_20260805.md`;
`sql/adhoc/20260805_verify_insurer_codes_30_46_48_49.sql`.
Verdict: **PASS**

## Checked against the request's own checklist

- **Normalization parity with DDL 032/E3 — verified line-for-line, not assumed**: compared this
  query's `COALESCE(REGEXP_EXTRACT(TRIM(U_InsurerCode), r'/(.+)$'), REGEXP_EXTRACT(TRIM(
  U_InsurerCode), r'^[^-]+-(.+)$'), TRIM(U_InsurerCode))` directly against
  `sql/ddl/032_exclusion_config_and_excluded_records.sql`'s own `sap_insurer_master`-seeding MERGE
  (lines 89-103): the normalization expression, the source table
  (`sap_integration_v2.SAP_LIVE_FULL`), and the acceptance predicate
  (`SAFE_CAST(DocEntry AS INT64) > 0`) are **identical**. This finding uses exactly the documented
  E3 rule, not an approximation of it.
- **Target completeness**: all four requested codes (`30`, `46`, `48`, `49`) are present in the
  `targets` UNNEST array — none silently dropped.
- **Join/aggregate accuracy**: `LEFT JOIN live`/`LEFT JOIN master` on `insurer_code` with
  `GROUP BY t.code` correctly preserves all four target codes regardless of whether they have any
  matching rows (a code with zero live/master rows still produces one output row with zero counts,
  not a missing row) — confirmed by re-running the query myself.
- **Job/result traceability**: confirmed the dry-run estimate (7,240,991,840 bytes) matches exactly
  what I got re-running it myself, and the real run stayed under the 20 GiB cap via the mandatory
  wrapper.
- **Conclusion scope**: the finding explicitly limits its claim to "SAP LIVE evidence... cannot
  support adding or releasing any of these codes" via the *documented* E3 source/rule, and
  explicitly declines to speculate about an alternative source or mapped code, instead requiring
  that to be named explicitly by Aware/Boat — appropriately scoped, not overreaching.
- **Absence of master/hold/export/workflow/GCS/SAP mutation**: the file is a single read-only
  `SELECT`; no `MERGE`/`INSERT`/`UPDATE`/`CALL` anywhere. Confirmed `sap_insurer_master`'s live
  schema (`insurer_code STRING, source STRING, added_at TIMESTAMP`) matches exactly what the query
  reads. `git diff --check`: clean.
- **Independently re-ran the exact query myself**: identical result — all four codes show
  `accepted_live_rows=0`, `accepted_doc_entries=0`, `master_rows=0`,
  `passed_live_and_registered=false`. Matches the finding's table exactly, row for row.

## Verdict

PASS. The methodology is provably identical to the project's own documented E3 acceptance rule
(verified against the actual DDL, not just described as consistent with it), and I independently
reproduced the zero-result finding for all four codes myself. The conclusion is honestly scoped to
what this specific evidence source can and cannot support. No mutation, hold change, export,
workflow, GCS, or SAP action occurred in this review.
