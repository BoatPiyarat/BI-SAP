# Review — 6ef690b (043 watermark/type fixes — response to the 2026-08-01 BLOCK)

**Queue entry:** RQ-20260801-0153-043-watermark-call-fixes · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — both BLOCK items resolved as specified; BLOCK on
`RQ-20260730-2230`/`docs/reviews/2026-08-01-043-merge-claude.md` is cleared by this commit.

Checked against the two BLOCK items:
1. **Analytic-in-aggregate watermark SET** → replaced with the grouped maximum-date selector,
   moved *inside* the `IF row_count > 0` guard (better than the proposed placement — an empty delta
   no longer even evaluates it). Construct independently re-verified with a standalone dry-run
   (2026-08-01, 0 bytes): validates clean, unlike the original which errored.
2. **TIMESTAMP→DATE mismatch** → `DATE(UpdateDate)` now projected in **all four** delta branches
   (verified by count: 4 projection sites), watermark table column, both DECLAREs, and the seed all
   moved to DATE — the whole chain is in one type domain, matching post-`2c96c53` 024's mirror
   schema. The `WHERE UpdateDate > wm_date` comparison (TIMESTAMP vs DATE) is dry-run-validated per
   the author's four-shard evidence.

Also verified: the strict late-arrival boundary is now documented in the header with the
row-for-row-vs-fresh-024 comparison as the explicit cutover gate ("no claim of losslessness before
that check passes") — the residual note from the BLOCK review, addressed. The author self-caught
the missing STRUCT alias via targeted dry-run and said so — correct behavior.

Standing condition (unchanged, author already states it): a meaningful full dry-run/CALL of 043 is
only possible after reviewed 024 is applied and refreshed; the cutover sequence in the trailing
comment governs. **No deploy authorized by this review** — 043 remains behind the 024 chain and
Boat's per-item approval.

Queries used: 1 standalone 0-byte dry-run (construct re-verification).
