# Review — RQ-20260804-1203-interface-status-increase-alert

Reviewer: Claude Code
Artifact: commit `3c32cf3`; `sql/ddl/066_interface_status_history_increase_alert.sql` and
`sql/adhoc/20260804_interface_status_increase_fixture.sql`.
Verdict: **PASS**

## Checklist 1–12

1. **Traceability** — pass. No job ID needed for a DDL-only dry-run at 0 bytes; reconfirmed the
   wrapper self-test offline (7/7).
2. **Provenance over repetition** — pass. Does not re-cite the 576/34,758 provisional numbers from
   `INPUTS_NEEDED.md` as a threshold or seed — see item 8.
3. **NULL-safety** — pass. Every current/prior count in the breach computation is wrapped in
   `IFNULL(...,0)`, so a status with zero rows on either side of the comparison (a real case: a
   status can have zero `interface_daily_status` rows some day, so no history group is written for
   it) computes cleanly instead of propagating NULL through the subtraction.
4. **Ordering** — pass. Prior-snapshot selection (`MAX(snapshot_date) WHERE snapshot_date <
   p_snapshot_date`) picks the most recent *actual* earlier snapshot, not a naive "yesterday,"
   correctly tolerating a gap if a day's snapshot was missed.
5. **Column order** — N/A, internal alert/history tables, not a positional SAP interface file.
6. **Grain stated** — pass. `interface_status_daily_history` logical key is explicitly documented
   as `(snapshot_date, status)`; the snapshot procedure's own conservation `ASSERT` (source row
   count = `SUM(record_count)` across statuses) proves the grain holds, not just declares it.
7. **Distribution, not row count** — pass. History is per-status record/order counts, not a single
   aggregate; the alert message reports both current and prior counts per status, not just a verdict.
8. **No contradiction with knowledge** — pass. Cross-checked `docs/INPUTS_NEEDED.md`'s prior
   MISSING 576 / STATUS_CONFLICT 34,758 snapshot numbers against this file: neither appears anywhere
   in `066` or the fixture — the file genuinely contains no seeded threshold or population number,
   matching its own header ("No threshold seed is included") and the request's explicit ask to
   confirm this.
9. **Scope** — pass. `sql/ddl/066`, `sql/adhoc/` fixture, plus same-session `INPUTS_NEEDED.md`
   update moving this from "not built" to "source ready, needs Boat's threshold."
10. **Rollback** — N/A pre-deploy; both tables are `CREATE TABLE IF NOT EXISTS` net-new objects.
11. **Cost hygiene** — pass. Fixture is literal-only (`INSERT INTO fixture VALUES ...`, no
    production reads); procedures query only the small history/config tables.
12. **Honest labelling** — pass. Header states plainly: "Existing absolute backlogs are never
    treated as a new incident by themselves" and "Boat must approve X before this checker can run" —
    doesn't imply the alert is live or usable yet.

## Fixture-to-implementation match

Re-derived all five fixture cases against the actual breach expression in `066`
(`IFNULL(c.record_count,0)-IFNULL(p.record_count,0) > cfg.max_record_increase OR ...order...`) and
confirm exact agreement, including the boundary case (`at_record_threshold_passes`: delta equals
the threshold exactly, strict `>` correctly does not fire) and the OR-not-AND semantics
(`order_increase_alerts`: records unchanged, only the order-count delta breaches, and that alone is
sufficient — unlike the AND-based magnitude gate reviewed under RQ-2112, this domain is
intentionally OR, and the file doesn't conflate the two).

## Config-completeness assertion

The combined `COUNT(*)=2` + `COUNT(DISTINCT status)=2` pair (lines ~130–142) correctly distinguishes
"duplicated" (extra row for one status) from "incomplete" (missing a status entirely) — both fail
closed, and traced that neither assertion alone would catch both failure modes.

Checklist 1–12 reviewed; no gap found.
