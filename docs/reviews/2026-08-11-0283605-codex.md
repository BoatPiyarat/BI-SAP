# Review — RQ-20260810-1222-delivery-checker-identity-fix

Reviewer: Codex  
Artifact: commit `0283605`; `scripts/check_v3_delivery_control_plane.ps1`.  
Verdict: **BLOCK**

## Finding

The one-line change fixes the observed project-ID and project-number resource paths, parses cleanly,
and does not change adjacent checker behavior. Local cases confirm that folders, suffixes, and a
wrong email remain rejected. However, `^projects/[^/]+/serviceAccounts/` strips any nonempty project
segment. Consequently `projects/evil/serviceAccounts/<approved-email>` normalizes to the approved
email and passes. The review request explicitly requires accepting every real project-ID/project-
number shape while rejecting shapes that should fail; the configured project or its numeric project
number must be validated, not discarded.

## Checklist 1–12

1. Traceability: PASS — live shape and rerun timestamp recorded.
2. Provenance: PASS — prior required note cited.
3. NULL-safety: PASS — empty/unmatched values remain unequal.
4. Ordering: N/A.
5. Column order: N/A.
6. Grain: N/A.
7. Distribution: N/A.
8. Knowledge consistency: PASS.
9. Scope: PASS — surgical checker-only change.
10. Rollback: N/A — read-only checker.
11. Cost hygiene: N/A.
12. Honest labelling: PASS.

Required correction: accept only `projects/<configured-project>/serviceAccounts/<approved-email>`
and the confirmed numeric-project form, require a full canonical resource match, add positive and
negative regex tests, and request one delta review.

