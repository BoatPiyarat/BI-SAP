# Review — d1310f9 (D16 incident split, FA-verification control requirement, SAP_LIVE append-only hold)

**Class:** A (money-adjacent incident taxonomy, affects what can be cited to FA)
**Reviewer:** Claude Code
**Verdict:** **PASS**

| # | Result |
|---:|---|
| 1 | PASS — `docs/knowledge/KNOWLEDGE_ADDENDUM_20260730_v3.md` (this commit's main content) tags every claim `[CONFIRMED]`/`[OPEN]`/`[RETRACTED]` and cites specific query timestamps where a real measurement exists (A8: 12:35:10/12:36:07 UTC); explicitly labels the "263 transactions" figure an **operational label, not a BigQuery measurement**, rather than presenting it as verified. |
| 2 | PASS — cites commit hashes (`5171adb`, `19c49cf`) throughout rather than re-deriving. |
| 3–7 | Mostly N/A (prose/knowledge synthesis, not new SQL); where distributions are given (A4's 10-day table) they're per-day, not a single rounded aggregate. |
| 8 | **PASS, and the standout strength of this commit**: §B (RETRACTIONS) explicitly walks back 8 prior conclusions, including two ("STOP nightly manual triggering," "`L80524847` ≈ pilot `L77828566` because ฿645.21 matches") that this same session's own Claude Code work had asserted and had to retract. Self-correction within the session, recorded plainly rather than quietly dropped — exactly what item 8 and the review protocol's whole premise want to see. |
| 9 | PASS — `docs/knowledge/**`/`docs/tasks/**` only; no `sql/ddl/**` touched. |
| 10 | N/A. |
| 11 | N/A — no new query in this commit itself (A4's query was already reviewed separately, see `2026-07-30-e02bd39-claude.md`). |
| 12 | PASS — extensive, consistent `[CONFIRMED]`/`[OPEN]`/`[RETRACTED]` tagging is itself the honest-labelling mechanism this checklist item asks for. |

Specific gap, flagged for the record rather than treated as this commit's fault: this session's
Priority 1 (`docs/sessions/2026-07-30-claude.md`) searched the entire repo for the "watermark 14
generations" / "297,413 vs 297,604 (+191)" figures Boat cited and found them **nowhere** —
including nowhere in this addendum, which doesn't make that specific claim either. The DocEntry
2345730 generation history in this addendum (§A8) was independently reproduced fresh and matches
(3 generations, not 14 — also flagged there). This isn't a defect in `d1310f9`; it's a separate,
still-open provenance gap for wherever that "297,413/297,604" figure actually originated (most
likely an uncaptured chat exchange in a session that predates this addendum) — noting it here so it
isn't lost a second time.

Checklist 1–12 reviewed; no gap found in this commit's own content.
