# Review — 03b0381 (July contract-failure item quarantine) + 393f9e3 (NOTE-1 declaration)

**Queue entry:** RQ-20260802-0528-july-contract-item-quarantine · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — and RQ-0515's NOTE 1 is closed by `393f9e3` (the wrapper omission is now
declared in 050's header with the correct ownership reasoning).

The four requested checks:
1. **Multi-reason key counting — correct.** A key failing multiple contract rules appears once per
   reason in `_contract_invalid` (register semantics, consistent with the established
   overlap-allowed convention), but the final conservation counts `DISTINCT (OrderItem, period)` —
   no double-count. `july_export_ready` excludes by key via NOT EXISTS over *any* reason.
2. **Conservation grain — consistent.** `_eligible_all` inherits `expected_state`'s verified
   key-uniqueness (one row per key incl. charge_id), so rows ≡ keys on every term:
   `eligible_all = ready + coverage-holds + DISTINCT contract-invalid keys` is exact, and it now
   runs as the *final* assertion after quarantine — the strongest form of the gate.
3. **MERGE idempotency — same proven pattern** as the coverage holds: keyed on (order_item, period,
   charge_id, hold_reason), matched rows only refresh `detected_at` — re-runs cannot duplicate.
4. **Invalid rows cannot enter ready — double-enforced.** The NOT EXISTS anti-join removes them at
   materialization, and the original zero-error assertions (Paid completeness, PolicyNo ≤50,
   DDMMYYYY UNPIVOT, 56-column, zero-August) still execute against the final `july_export_ready`
   afterward — verified present at file tail. If quarantine ever leaked, the old gates still fail
   closed.

Evidence quality: the shadow retry passing coverage/holds and then stopping on F2 with **exactly
three real PolicyNo-too-long records** is precisely the sequence the gates were designed for; the
item-level quarantine (hold the 3 rows in `july_export_hold`, export the rest, never truncate,
never silently drop) implements the confirmed policy and the canonical rule 10 consistently.

The July shadow is now structurally able to reach a clean build: coverage 831 + holds 222 + these
3 contract quarantines, all conserved and auditable. Remaining chain: clean shadow CALL → archive
export → UAT2 acceptance (human gate) → exact-byte promotion.

Queries used: 0 (evidence + file verification; prior probes reused).
