# Review — b881fa3 (037 active-period guard — response to the aba1aad-review NOTE 1)

**Queue entry:** RQ-20260801-0156-037-active-period-guard · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked against the guard spec issued in `2026-08-01-aba1aad-claude.md` NOTE 1 — all three failure
modes now fail closed:
1. **Expired/stale rows**: both the count and the value SELECT filter
   `lock_datetime > CURRENT_TIMESTAMP()` — `lock_datetime` is finally load-bearing.
2. **Sequencing (August row inserted before July locks)**: `ASSERT active_period_count = 1` makes
   two concurrent active rows a hard stop. This is *stricter* than the spec's ORDER-BY-LIMIT-1
   (which would have silently picked July) — the fail-closed choice is the better one; a human must
   resolve the overlap explicitly. Post-lock with no successor row → count 0 → also fails closed.
3. **Future-dated row**: `ASSERT open_period_start <= CURRENT_DATE()`.

With exactly-one enforced, `MAX(open_period_start)` over the filtered set is equivalent to
selecting the single row — correct. Declarations remain at the top of the block (BigQuery scripting
requirement), per the claim.

Note (cosmetic, no action needed for July): `CURRENT_DATE()` is UTC; between 00:00–07:00 ICT on a
period's first day, a same-day-ICT start would be rejected as "future". That errs strictly toward
fail-closed, so it is acceptable — if it ever bites an early-morning deploy, the fix is
`CURRENT_DATE('Asia/Bangkok')`, not a relaxation. Runbook step 5 (`RULE09_DEPLOY_RUNBOOK.md`)
remains the human counterpart of this guard; keep both.

The RULE-01/09 chain (044 → Boat's July row → 037 → refresh) now has machine enforcement at its
highest-risk point. Deploy authorization stays with Boat, per item.

Queries used: 0.
