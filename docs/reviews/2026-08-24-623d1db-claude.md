# Review: RQ-20260824-1756 august-cutoff-correction

Reviewer: Claude Code
Artifact: `623d1db`; `sql/adhoc/20260824_correct_august_period_cutoff_1600.sql`,
`sql/adhoc/20260824_rollback_august_period_cutoff_1400.sql`
Verdict: **PASS**

## Verification performed

Per protocol, one targeted live query re-derived the exact pre-state independently (215 bytes,
well under cap), plus a free `INFORMATION_SCHEMA.TABLES` metadata check and my own dry-runs of
both artifact files (not counted against the one-query budget, same precedent as prior reviews).

- **Live pre-state matches the claim exactly, independently reproduced** (not just trusted):
  `sap_period_state` 2026-08 = OPEN, `closing_at=2026-09-01 07:00:00 UTC`, `state_version=2`,
  `period_end=2026-09-01`; 2026-09 = PLANNED, `closing_at IS NULL`; `sap_period_lock` 2026-08 has
  `lock_datetime=2026-09-01 07:00:00 UTC`, `locked_by IS NULL`, `locked_at IS NULL`. Every value the
  correction script's three pre-flight `ASSERT`s check is currently true against production.
- **Time conversion correct**: 14:00 ICT (old) = 07:00 UTC, 16:00 ICT (new) = 09:00 UTC
  (Asia/Bangkok is UTC+7, no DST) — both literals in the script match.
- **`sap_period_cutoff_calendar` confirmed not deployed** (`INFORMATION_SCHEMA.TABLES` returns zero
  rows) — the artifact correctly avoids the still-BLOCKed DDL 075 path
  (`docs/reviews/2026-08-24-efd6de0-codex.md`, same day) and does a scoped direct correction
  instead, which that same BLOCK review explicitly says is now the required interim path.
- **Both scripts dry-run clean at 0 bytes**, verified myself independently. Notably this means
  neither has the statement-ordering defect Codex just caught in the sibling calendar-delta
  verifier (`DECLARE` after an executable statement) — these files declare all variables first.
- **Atomicity and fail-closed behavior are correct**: each `UPDATE` is followed immediately by
  `ASSERT @@row_count=1` inside the same `BEGIN TRANSACTION ... COMMIT TRANSACTION` block — the
  established safe idiom already reviewed and PASSed for `071_v3_post_import_refresh_outbox.sql`'s
  `MERGE`+`ASSERT @@row_count=1` pattern, adapted here to `UPDATE`. An uncaught `ASSERT` failure
  inside an open BigQuery transaction aborts the script and rolls the transaction back automatically
  — if either row-count check fails, nothing partial is left committed. The exact-value `WHERE`
  predicates (not just primary key) also make a second accidental run of the same script a safe
  no-op-then-fail (0 rows match, `@@row_count=1` fails) rather than a double-apply.
- **Rollback is exact and symmetric**: reverses `state_version` 3→(4 after rollback), `closing_at`
  09:00→07:00 UTC, with its own pre-state asserts gated on the *post-correction* state — cannot run
  before the correction succeeds, and cannot silently re-apply if state has moved on since (e.g. a
  period close).
- **Timing guard**: `ASSERT CURRENT_TIMESTAMP() < v_old_cutoff` in both files prevents either script
  from running after the originally-registered 07:00 UTC cutoff, which is a sensible extra
  fail-closed bound even though cross-month auto-transition isn't wired up yet.

## Required precondition before execution (not a defect — already self-disclosed)

Both files are correctly headed "DO NOT RUN WITHOUT REVIEW PASS + BOAT DEPLOY APPROVAL." Per
`AGENT_RULES.md`'s DEPLOY GATE and SINGLE DEPLOYER rules, the relayed 16:00 confirmation from Mo/
Boat authorizes the *cutoff value*, not by itself an explicit "run this transaction now" — Codex
(the only permitted deployer) still needs Boat's explicit deploy OK on this specific artifact before
executing it, same as any other Class-A production mutation. Flagging this as confirmation of the
gate that's already built into the artifact, not a new requirement.

## Checklist 1–12

1 traceability PASS (pre-state query timestamp/bytes cited and independently reproduced, exact
match); 2 provenance PASS (cites the human relay, durable Sheet link, and the same-day BLOCK that
created the need for this interim path); 3 NULL-safety PASS (`locked_by`/`locked_at IS NULL`
checked explicitly, no nullable-column equality traps); 4 ordering PASS (both scripts declare all
variables before any executable statement — unlike the sibling artifact Codex just BLOCKed for
exactly this); 5 column order N/A (period-state mutation, not an interface payload); 6 grain PASS
(double-gated to exactly one row per table via pre-check `COUNT(*)=1` and post-check
`@@row_count=1`); 7 distribution N/A; 8 knowledge PASS (consistent with `docs/INPUTS_NEEDED.md` and
`20_SAP_PROGRESS.md`, correctly scoped as the interim path while DDL 075 remains blocked); 9 scope
PASS (touches only `sap_period_state`/`sap_period_lock`, does not deploy or touch the blocked
calendar object); 10 rollback PASS (exact symmetric file, own pre-state asserts, atomic); 11 cost
hygiene PASS (both dry-run 0 bytes, independently reproduced; pre-state check well under cap); 12
honest labelling PASS ("DO NOT RUN WITHOUT ... BOAT DEPLOY APPROVAL" and "No production DML was
executed" are both accurate).

Summary: Checklist 1–12 reviewed; no gap found. Every load-bearing claim (pre-state values, dry-run
byte counts, calendar-table non-existence) was independently re-derived from live objects rather
than trusted, and all matched exactly. Execution still requires Boat's explicit deploy OK on this
artifact and must run through Codex per SINGLE DEPLOYER — both already correctly gated in the files
themselves.
