# SAP Scheduler / Function / Eventarc Inventory

Compiled live 2026-07-27 via `gcloud scheduler jobs list`, `gcloud functions list`,
`gcloud eventarc triggers list`, `gcloud run jobs/services`, `gcloud workflows`, and
`bq ls --transfer_config`/`--transfer_run` — not from memory. Scope: every object that touches
the SAP interface pipeline, in both directions (SAP→BigQuery ingestion, CareOS→SAP export).
Per `TASK_V3_GAP_CLOSURE_v2.md` A0/housekeeping and Boat's 2026-07-27 "away 26-30 July" ask (item 1.1).

**Read this before assuming anything is "just working"** — one real scheduling bug was found
compiling this table (see row 8).

| # | Object | Type | State | Schedule (as configured) | Real TZ / actual fire time | Last run + result | Impact if down | Needs a manual press? |
|---|---|---|---|---|---|---|---|---|
| 1 | `sap-extract-schedule` | Cloud Scheduler | ENABLED but **FAILING** | `30 20 * * *` Asia/Bangkok (20:30 ICT) | 20:30 ICT, correctly configured — but invocation itself fails | Last attempt 2026-07-26T13:30 UTC (20:30 ICT), **status code 2 (fails, 401 UNAUTHENTICATED)** — confirmed ongoing since 2026-07-22, IAM grant still pending (see `docs/INPUTS_NEEDED.md`) | `SAP_LIVE` stops getting fresh nightly data → every downstream layer (legacy interface, V3 diagnostics) works off a stale mirror | **YES — Boat presses EXECUTE by hand every night from Console > Cloud Run > Jobs > `sap-extract-job` > EXECUTE** (per this week's plan) |
| 2 | `sap-extract-job` | Cloud Run Job | Healthy when run manually | Triggered by #1 (broken) or manual `gcloud run jobs execute` | N/A | 3 most recent manual executions all `SUCCESS` (14.6s / 13.0s / 2m3.9s) | Same as #1 — this job itself works fine, it's only the scheduler invocation that's blocked | Same as #1 |
| 3 | `auto_load_sap_data_in_bucket_to_bigquery` | Cloud Scheduler | ENABLED, healthy | `0 1 * * *` Asia/Bangkok (01:00 ICT) | 01:00 ICT, correct | Last attempt 2026-07-26T18:00 UTC (01:00 ICT 07-27), no error code (success) | Publishes the Pub/Sub message that triggers the real loader (#4/#5) — if down, extract's GCS output files never get loaded into `SAP_LIVE` even if extract itself succeeds | No — automatic, currently healthy |
| 4 | `trigger-sap-order-payment-initial-phase` | Eventarc trigger | ACTIVE | Event-driven (Pub/Sub message from #3, or from extract completing) | N/A | Confirmed 3 real successful loads in the last ~5 hours before this check (`Successfully loaded to ...SAP_LIVE` at 13:47, 16:32, 17:55 UTC on 07-26) | Same as #3 | No |
| 5 | `sap-order-payment-initial-phase` | Cloud Run service | ACTIVE | Event-driven | N/A | Same as #4 — this is the actual loader | Same as #3 | No |
| 6 | `sap-order-payment` | Cloud Scheduler | ENABLED, healthy | `30 1 * * *` Asia/Bangkok (01:30 ICT) | 01:30 ICT, correct | Last attempt 2026-07-26T18:30 UTC (01:30 ICT 07-27), no error code | Triggers the **real, legacy RCB Motor interface Cloud Function chain** — this is what SAP actually receives every day for Motor | No — automatic (but see #7's known issue) |
| 7 | `rcb-motor-order-payment-sap-bucket-1` | Cloud Function | Active, last updated 2026-07-26 | Triggered by #6 via Pub/Sub | N/A | **Known open issue (2026-07-26)**: was timing out daily at its 300s ceiling on the RCL Motor newpayment export step for 5+ consecutive days. A 540s fix may or may not be applied — check `gcloud functions describe rcb-motor-order-payment-sap-bucket-1 --region=asia-southeast1 --format="value(timeout)"` each morning. **UNVERIFIED as of this inventory** whether the fix landed | **This IS the real Motor interface file producer** — if it fails/times out, SAP does not get today's Motor create/cancel/change/creditshell/newpayment files | Not user-actionable from mobile if it fails — needs `cloudfunctions.functions.update` permission to fix the timeout, or a code change for the missing Credit-Shell export step (separate known gap) |
| 8 | `sap-order-payment-non-motor` | Cloud Scheduler | ENABLED, healthy | `30 1 * * *` Asia/Bangkok (01:30 ICT) | 01:30 ICT, correct | Last attempt 2026-07-26T18:30 UTC, no error code | Triggers the real legacy RCB NonMotor interface Cloud Function chain | No — automatic |
| 9 | `rcb-nonmotor-order-payment-sap-bucket-1` | Cloud Function | Active | Triggered by #8 | N/A | Historically healthy (60s timeout, ~33-37s typical runtime); one 59s near-miss on 2026-07-25 tied to that day's incident, not chronic | Real NonMotor interface file producer | No |
| 10 | `sap-interface-pipeline` | Cloud Workflow | ACTIVE (deployed) but **NOT scheduled** | None — manual trigger only, deliberately kept parallel, not cut over | N/A | Last (only) execution **FAILED** 2026-07-26T13:05 UTC | None currently — not live production, nothing depends on it yet | N/A — do not schedule or cut over this week (out of scope per this week's "no producer switch" rule) |
| 11 | `sap_state_and_recon_refresh` (BQDTS) | BigQuery scheduled query | ENABLED, succeeding | ~~`every day 21:00`~~ → **FIXED 2026-07-27: `every day 14:00` (explicit UTC = 21:00 ICT)** | Was firing at 21:00 UTC = 04:00 ICT next day (BQDTS defaults to UTC with no timezone suffix; the `Asia/Bangkok`-suffixed schedule string was rejected as invalid syntax by the API — used explicit UTC instead). Confirmed fixed: `nextRunTime` now shows `2026-07-27T14:00:00Z`, exactly 21:00 ICT today | Runs `sp_nightly_state_and_recon_refresh`. Prior 7h-later timing was not currently harmful (still ran well before any morning check) but is now correct per design | No — automatic, now at the intended hour |
| 12 | `sap_dead_mans_switch` (BQDTS) | BigQuery scheduled query | ENABLED, succeeding | Configured as `every day 15:00` (intended: 22:00 ICT) | 15:00 UTC = 22:00 ICT — **this one is actually correct** (whoever configured it did the UTC conversion by hand) | Succeeding daily per transfer config state | RAISEs (triggers `enableFailureEmail`) if the refresh above hasn't run in >26h | No |
| 13 | "SAP Interface Daily Monitor" (RemoteTrigger cloud agent, not a GCP object) | Cloud routine | **DISABLED** (reverted 2026-07-27 per Boat's rule) | Was set to `0 23 * * *` UTC = 06:00 ICT | N/A while disabled | Fired once already (2026-07-26T23:10 UTC) under the pre-guardrail prompt — check that email | None while disabled | Needs Boat's explicit confirmation before re-enabling |

## Summary for the away window
- **Real automatic path working right now**: extract IAM is broken (#1), so freshness depends entirely on Boat's manual nightly EXECUTE press. Everything downstream of a successful extract (#3-#9) is automatic and was healthy at compile time.
- **One real bug found**: #11's schedule fires 7 hours later than designed. Currently harmless (still finishes long before any morning check) but flagged, with the exact fix command ready — needs your go-ahead since scheduler edits are gated.
- **One thing that needs morning verification, can't be automated from data alone**: #7's timeout-fix status — the CLI command to check is in the row above.
