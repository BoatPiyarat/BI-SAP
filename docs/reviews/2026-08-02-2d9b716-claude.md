# Review — 2d9b716 (RULE-21–30 + monthly delta operating model + 21153 evidence)

**Queue entry:** RQ-20260802-monthly-delta-rules-21-30 · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS WITH NOTES** — NOTE 1 is a required one-line canonical fix; NOTE 4 is this
reviewer's own disclosure.

## The five requested checks

1. **Period-transition semantics (25/26):** the state machine is coherent — exactly one open
   period, atomic close, backlog clamped to the new month's first day (RULE-01 generalized
   correctly), new-month rows keep real dates. The explicit period-status/history requirement
   (not inferring from `lock_datetime` alone) is the right hardening of the current 037 guard
   lineage. **NOTE 1 (required):** rule 26's `BatchRunDate = real run date capped to month-end`
   **changes RULE-02** (`= last_day(open_period) เสมอ`). The outcome coincides for the July close
   file (Aug-2 run capped to 31072026) but diverges for normal nightly runs (Aug-5 run →
   `05082026`, not `31082026`). Neither document marks RULE-02 as superseded/generalized — add the
   supersession line in `CURRENT_STATE` §1, or the two canonicals will get cited against each
   other (the R1-retraction lesson).
2. **Paid-before-cancel (21):** hard sequencing gate, and 22's no-manufactured-spine +
   mirror-the-winning-document requirement is consistent with rules 17–19 and the reviewed
   change-order preflight.
3. **NonMotor InsuranceGroup hold (23):** implements RULE-07 with the correct approval semantics
   ("non-empty is not approval"), fail-closed from 2026-08-01 raw dates, separately reported.
4. **Closed payment mappings (28):** directly addresses 21153's 1,889 mojibake PaymentMethod + 31
   PaymentChannel rows; approved sources are the right two (reviewed V2 success mapping,
   SAP-history-accepted values). **NOTE 3:** rule 28 holds the *symptom* — the mojibake is
   confirmed to already exist in the BigQuery payload (Thai text double-encoded, 70 chars/143
   bytes), so the encoding defect upstream needs its own root-cause owner and finding; holding
   rows forever is not the end state.
5. **Exact daily conservation (30):** mutually-exclusive outcome classes, exact conservation,
   daily email — the proven conservation-assert pattern promoted to an operating requirement. The
   delta classification's key line is exactly right: existing Paid + same immutable InvoiceNo =
   no-op; different InvoiceNo = human hold — this closes the 21153 defect at its root.

## 21153 evidence quality

- Arithmetic: 30,245 − 15,843 = 14,402 ✓ exact; 52.38% ✓. **NOTE 2:** error-type counts sum to
  17,760 > 15,843 error rows — multi-error rows or instance-grain counting; state the grain in the
  finding so the numbers can't be misread as disjoint row counts.
- Epistemics excellent: the 14,402 non-error rows are "candidate successes **until a post-import
  SAP extract/mirror proves them**" (rule 27 applied to its own evidence); workbook-over-txt as
  row-error truth (txt undercounts by 31 — notably the same count as the PaymentChannel class);
  no raw PII/mojibake records quoted in the repo; no auto-retry authorized.

## NOTE 4 — reviewer disclosure (recorded in the scorecard as shared rework)

The root defect — export-archive membership used instead of current-SAP-state delta classification
— shipped through this reviewer's multiple passes over 048. The reviewed claims never asserted
"excludes already-in-SAP" and no check requested it, but the population cross-check was measurable
before delivery (eligible ≈30k vs G1's (ก)+(ง) ≈5.4k records) and I did not demand it. SAP's
immutable-InvoiceNo defense contained the damage; the review process should not have needed it to.
Standing lesson proposed for the protocol: **population-changing or population-defining artifacts
must quote the expected magnitude from the latest measured baseline, and the reviewer must
reconcile the two before PASS.**

Queries used: 0 (docs/design/evidence review; mailbox facts verified in the prior turn).
