# Review — 84df583 (CMI cause population and control design)

Verdict: **BLOCK**

1. Traceability — BLOCK: named objects and inline SQL results exist, but exact job IDs/query
   timestamps are absent for stakeholder-level counts and money.
2. Provenance — PASS: the cause query is distinguished from the earlier symptom view.
3. NULL-safety — no specific NULL defect found in the documented predicates.
4. Ordering — N/A for the population; pilot invoice minting remains source-only.
5. Column order — N/A; nothing deployed/exported.
6. Grain — BLOCK: the 401-order result is admitted to mix pure V1 CMI non-deduction with the
   M1/V1 shared-full-payment mechanism. It is not one incident population yet.
7. Distribution — PASS: first-pass noise, cutover split, and heterogeneous sample are exposed.
8. Knowledge consistency — BLOCK: `sap_fa_verification` does not implement D16's required control
   fields (incident/finding ID, period grain, SAP DocEntry/status, JE reference, import LogID and
   success evidence, evidence/capture timestamps, and source link/note).
9. Scope — PASS: source-only SQL and findings; no deploy.
10. Rollback — N/A; table was not created.
11. Cost hygiene — NOTE: query metadata/dry-run/bytes are not retained.
12. Honest labelling — PASS for flagging heterogeneity and no deploy; BLOCK if 401 or
   ฿267,775.28 is treated as final/citable.

Required author response:

1. Split the M1/V1 shared-full-payment shape from pure V1 CMI non-deduction before returning a
   population. `Expected > 0` removes known noise but is not sufficient cause classification.
2. Expand `042` to the full durable evidence contract and add constraints/controlled values;
   seeded prose alone is not the required SAP/FA control.
3. Do not approve `L77828566` as a pilot until it has the full control evidence, including the
   relevant CMI sibling source, SAP key/status, JE/import-success evidence, and FA/Aware decision.

Specific risk: a Method-1 correction based on a heterogeneous population or a partial control
schema can create a second accounting error while appearing to satisfy the arithmetic.

