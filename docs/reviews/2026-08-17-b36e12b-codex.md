# Class-A review — Mo RCL recovery shadow adapter

Verdict: **BLOCK**

Fixed point: `72b804a`  
Reviewed commit: `b36e12b`  
Reviewed diff: `72b804a...b36e12b`  
Artifact: `sql/ddl/076_mo_rcl_recovery_shadow.sql`

No BigQuery object, procedure call, GCS object, or SAP interface file was created by this review.

## Standards axis

- Status spellings are silently normalized even though the canonical gate requires unknown
  spelling or case to block.
- The output checks only a 56-column count and materializes with `SELECT c.*`; it does not prove
  the exact reviewed names, types, and ordinals.
- Mandatory date, numeric, InvoiceNo immutability, exclusion, and successful-charge conservation
  gates are absent.
- A single replaceable ready table is not an immutable request snapshot. A later or concurrent
  run can overwrite the rows between validation and export; the delete/insert evidence paths are
  also non-transactional.
- The final `TO_JSON_STRING` table reference is not a valid row expression and the procedure body
  has no exact execution dry-run evidence.

## Spec axis

- Seeded pairs are not restricted to Phase-1-confirmed `STILL_MISSING_SILENT_DROP` rows, so an
  already-resolved or out-of-scope pair could enter the candidate.
- RCL classification is checked only at the reported period. The complete item spine does not
  prove agreement among source flow, declared flow, payment method, payment channel, BU, and
  operation; checking only for `PaymentChannel LIKE '%RCB%'` is insufficient.
- The adapter does not implement the complete canonical pre-export contract, including required
  nonblank/master checks and zero-silent-drop reconciliation.

## Required correction before another review

Reuse the reviewed V3 event/mapping/InvoiceNo logic rather than treating the dashboard relation as
an export contract. Bind Phase-1 classification, validation evidence, and an explicit 56-column
payload to one immutable request ID; export must read that exact snapshot. Implement every check in
`docs/design/SAP_INTERFACE_PRE_EXPORT_GATE.md`, retain exact dry-run/hash/count evidence, and add
positive and negative fixtures. Until then, deployment, procedure calls, and all GCS writes remain
prohibited.
