# Review — 16cddd8 + 935ac8f (mirror addendum v3 fold, evidence corrections, migration task, review hygiene)

**Class:** A (canonical knowledge + a P0 security finding + accounting-gate CLEARED verdict)
**Reviewer:** Claude Code
**Verdict:** **PASS WITH NOTES**

| # | Result |
|---:|---|
| 1 | PASS — every number carries source table + query timestamp: 8,324,155 rows (`SAP_LIVE`, 2026-07-30 13:53:09 UTC, explicitly labeled a stale-after-21:53 snapshot, not an invariant); overwrite gate (2026-07-30 14:27:28 UTC); daily ratios (2026-07-30 09:10:41 UTC, with the source-side timestamp honestly marked as uncaptured). |
| 2 | PASS — migration surface (569 refs / 80 files) is pinned to commit `19c49cf`; retractions cite the original claims rather than silently rewriting them; `10_SAP_CONTEXT` folds point at `KNOWLEDGE_ADDENDUM_20260730_v3.md` instead of duplicating evidence. |
| 3 | N/A (prose) — but the doc-level analogue is handled correctly: 9,702 `NO_BASELINE` DocEntries are excluded rather than treated as default/POPULATION. |
| 4 | N/A — no new SQL. |
| 5 | N/A — nothing feeding an interface file. |
| 6 | PASS — overwrite-gate grain stated explicitly (per DocEntry, last observation before 07-26 vs first in 07-26..28); arithmetic internally consistent (54,055 + 9,702 = 63,757). |
| 7 | PASS — per-field POPULATION / MUTATION / A→default table and per-day ratios, not a single aggregate; "45×" explicitly killed in favour of daily values. |
| 8 | **NOTE (the substantive one)** — `SECURITY_FINDING_20260730.md`, addendum A11, and `10_SAP_CONTEXT` §A item 11 assert that *"Cloud Run Job/execution metadata returns the credential environment variables"*. The 2026-07-31 gcloud-verified state (`CURRENT_STATE_20260731.md` §3.1, retraction R9) shows the live job binds `SAP_DB_USER`/`SAP_DB_PASSWORD` via `secretKeyRef key=latest` — describing the job returns a secret *reference*, not a value. The source-archive exposure (`deploy.sh`, `envvars.yaml`, `update_env2.sh`) is independently plausible and not contradicted; the metadata-exposure sentence either needs its own observed evidence (which surface, which command) or should be narrowed to the archive claim. Second, status staleness: rotation is CLOSED as of 2026-07-31 (`sap-db-password` v2 enabled 11:32:25, v1 disabled), so a canonical doc still reading "OPEN — P0, rotate the credential" now instructs an action already done. Neither is an authoring error — both commits predate the 07-31 verification — but the reconciliation addendum is required before the next fold. |
| 9 | PASS — all 21 files are `docs/**`: inside Codex's lane under both the old and the 2026-07-31 reallocated assignments. |
| 10 | N/A — nothing deployed. |
| 11 | PASS — no new queries run for these commits; all cited measurements reuse queries already paid for in-session. This review itself used zero queries (artifacts only). |
| 12 | PASS — `[CONFIRMED]`/`[OPEN]`/`[RETRACTED]` tags used throughout; CLEARED verdict explicitly bounded as a LOWER BOUND (states between extracts unrecoverable); `DocEntry 2345730` kept WAITING HUMAN, not resolved by inference; C8's six migration questions preserved as Boat-owned, no inferred answers. Request-hygiene fix checks out: `Opened: 2026-07-30T23:23:02+07:00` matches `935ac8f`'s real commit timestamp exactly. |

**Named risk (anti-rubber-stamp):** if the "Cloud Run metadata returns credential values" sentence
is quoted onward as-is, it revives the exact claim retraction R9 killed (live plaintext env binding)
and could send remediation effort at a surface that is already `secretKeyRef`-backed. Fix is a
short addendum to `SECURITY_FINDING_20260730.md` + A11: (a) narrow or evidence the metadata claim,
(b) record rotation CLOSED 2026-07-31 with v1 disabled, keeping items 4–7 (archive purge, history
scrub, access restriction) as the still-open remainder.
