# Review: `6863dc8` — expected_invoice_no NULL-safe fix

**Class:** A — deployed, money-adjacent  
**Reviewer:** Codex  
**Verdict:** **BLOCK**

The one-line predicate change is locally correct and narrowly scoped, but the committed/session
evidence is not sufficient for a class-A PASS. Author response is required once for the three
blocking evidence/control gaps below.

| # | Checklist result |
|---:|---|
| 1 | **BLOCK:** tables and counts are named, but the exact verification SQL and query timestamps are absent. The reported pre/post figures therefore lack complete traceability. |
| 2 | **PASS:** cites `034`, `6863dc8`, and the same-day session evidence instead of re-deriving established configuration. |
| 3 | **PASS:** changed predicate is NULL-safe and calls canonical `fn_invoice_no`; no new nullable comparison was introduced. |
| 4 | **N/A:** no ordering logic changed. Existing ordering was copied from `034`. |
| 5 | **N/A:** `expected_state` is not an interface file; the output select/schema is unchanged from `034`, and no reorder expression was added. |
| 6 | **PASS:** join and fix are at `(order_item, period)` grain; no order-level fan-out was added. |
| 7 | **PASS WITH NOTE:** author checked field distributions, not only total parity. Exact query evidence is still missing under item 1. |
| 8 | **PASS:** aligns with the canonical NULL-safe-filter and InvoiceNo rules; no D1 formula change is part of this commit. |
| 9 | **PASS:** one SQL file plus authorized deployment; no docs/knowledge edit in the reviewed commit. |
| 10 | **BLOCK:** prior full definition remains in `034`, but no executable rollback command or evidence of a sub-five-minute rollback was stated. |
| 11 | **BLOCK:** session states a targeted query was used, but does not preserve dry-run output or `--maximum_bytes_billed` evidence for the deployed verification. |
| 12 | **PASS WITH NOTE:** the implementation status is honestly labelled deployed; the numeric proof remains incomplete until item 1 is answered. |

Specific risk: a full procedure replacement was deployed with no preserved rollback command and
with verification numbers that cannot be reproduced from the recorded artifact alone. This does
not show that the predicate is wrong; it means the class-A deployment evidence is incomplete.

Targeted reviewer query: one attempt was made to check the live routine predicate and remaining
broken population, but shell quoting produced a parse error before a usable result. Per the
one-query review cap, it was not retried and supplies no evidence.

**Author response requested (one round):**
1. provide the exact pre/post verification SQL plus BigQuery job IDs/timestamps, or mark the old
   counts provenance-incomplete and supply one new provenance-complete targeted verification;
2. state the exact rollback command using the preserved `034` definition and expected duration;
3. attach dry-run bytes and maximum-bytes evidence for the verification/deploy workflow.
