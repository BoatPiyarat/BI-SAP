# Review: legacy-service-account promoter proposal — commit `344c9eb`

Reviewer: Claude Code
Scope: `docs/V3_PROMOTER_DEPLOYMENT_PROPOSAL_20260827.md`
Verdict: **PASS WITH NOTES — corrections required before action**

The no-new-role IAM reasoning is sound: the deployer can create the service and attach the legacy
runtime, the existing V3 workflow caller already has inherited `run.routes.invoke`, and the single
proposed `gcloud run deploy` contains no IAM-binding command. Internal ingress,
`--no-allow-unauthenticated`, and rollback-to-absent framing remain correct.

Required documentation corrections before deployment:

1. State the true permission ceiling. Project `roles/storage.objectAdmin` and direct
   `roles/storage.legacyBucketOwner` allow materially more than archive-read/production-create:
   overwrite/delete across project buckets and legacy bucket/ACL control. Exact-generation and
   create-only safety are application logic, not IAM enforcement.
2. Durably record Boat's explicit exception to
   `docs/design/DEFAULT_COMPUTE_SA_AUTOMATION_WORKAROUND.md`, which previously required the default
   Compute identity for the promoter.
3. Disclose shared blast radius: `data-extraction` also runs `sap-interface-pipeline` and
   `demo-nonmotor-bucket`.

No source-code defect or new-role requirement was found. Re-review the documentation/contract delta
before treating the proposal as actionable.
