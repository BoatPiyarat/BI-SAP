# Review — RQ-20260805-2129-log21183-two-name-binding-gap

Reviewer: Claude Code
Artifact: commit `9e7724c`; `docs/FINDINGS_LOG21183_FILENAME_BINDING_GAP_20260805.md`;
`sql/adhoc/20260805_verify_log21183_filename_binding.sql`.
Verdict: **PASS**

## Checked — live evidence independently reproduced

Re-ran `sql/adhoc/20260805_verify_log21183_filename_binding.sql` myself. Result matches the
finding's table exactly: `export_archive` (558 rows, `DELIVERED`) and `export_file_manifest`
(3,857 rows, `DELIVERED`) both show production basename
`INSURANCE_RCB_06_V3_DAILY_NEWPAYMENT_20260803_V3DAILY-20260803-113257-55042e7c_000000000000.csv`,
which does **not** equal the hardcoded SAP email filename
`RCB_MOTOR_INSURANCE_RCB_...csv` (`production_basename_matches_email = false` in both rows). No
`sap_delivery_manifest_v3` or `sap_import_result_header_v3` row exists for this historical run —
consistent with the finding's own explanation that those tables didn't exist yet when this export
ran. (The email `FileName` value itself is an externally-supplied fact I can't independently
re-derive from BigQuery — I verified everything BigQuery *can* confirm, which is the two recorded
values and their inequality.)

## Checked — diagnostic reasoning traced against the actual pre-fix source

- **Claim 1** ("DDL 062 asserts `p_sap_file_name` equals the basename of `p_production_uri`, then
  stores that value as `sap_delivery_manifest_v3.sap_file_name`")**: checked the DDL 062 revision
  immediately *before* the proposed fix (`git show 30bd693^:sql/ddl/062_...sql`) — confirmed
  `ASSERT REGEXP_EXTRACT(p_production_uri,r'([^/]+)$')=p_sap_file_name` and the same
  `p_sap_file_name` value inserted into `sap_delivery_manifest_v3.sap_file_name` — exactly as
  described.
- **Claim 2** ("the Apps Script ingestor... requires exact equality with
  `sap_delivery_manifest_v3.sap_file_name`; zero matches remain unlabeled and `PENDING_ACK`"):
  confirmed in `workflows/sap_result_ingestion.gs` — `AND sap_file_name = @file_name` bound to the
  parsed email's `FileName`, with `if (manifests.length === 0) return; // ... remain PENDING_ACK
  and do not label.` immediately following. The file's own comment even already states
  "`sap_file_name` is the exact SAP-reported result name, not the production GCS basename" — the
  semantic mismatch this finding identifies (the OLD DDL 062 forced that column to hold the
  production basename instead) is visible in the code itself, not inferred.
- **Claim 4** (a static scheduler can't supply the exact name because run ID/shard suffix are only
  known after the archive export executes): consistent with the promoter/workflow design already
  reviewed this session (the object is discovered dynamically at runtime, not pre-configured).

## Verdict

PASS. Every live-data claim was independently reproduced with an identical result, and the causal
reasoning was checked against the actual pre-fix source rather than taken as asserted — the
pre-fix `DDL 062` really did force a single name into a column the ingestor always intended to hold
a semantically different value. This is a genuine, correctly-diagnosed structural blocker, not a
speculative concern. No manifest, table, GCS object, workflow, scheduler, Gmail label, delivery, or
SAP state was touched in this investigation or in this review.
