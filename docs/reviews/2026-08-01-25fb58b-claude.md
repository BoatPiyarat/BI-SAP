# Review — 25fb58b (legacy definition drift inventory + governance rule + 045 PII correction)

**Queue entry:** RQ-20260801-0040-legacy-definition-governance · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — and the drift result is **independently reproduced**, not just read.

## Independent reproduction (Boat's parallel ask: classify the 12 interface producers)

Re-ran the committed inventory approach restricted to the 12 deployed producer views
(`bq_safe_query.sh` wrapper, dry-run 10,485,760 bytes, well under ceiling; JSON re-fetch same size;
2026-08-01). Same normalization (strip comments/whitespace/CREATE-wrapper). Result, per producer:

| Producer view (sap_view) | Status vs baseline |
|---|---|
| RCB_Motor_process_create · _2_cancel_new · _3_change · _4_creditshell | **MATCH** (4/4) |
| RCL_Motor_process_3_cancel · RCL_Motor_process_4_creditshell | **MATCH** |
| RCB_NonMotor_process_1_create | **MATCH** (sap_view copy) / **DRIFT** (sql/production copy) |
| RCB_NonMotor_process_2_cancel · RCL_NonMotor_process_1_create | **MATCH** |
| RCL_Motor_process_1_create | **DRIFT** |
| RCL_NonMotor_process_2_newpayment | **DRIFT** |
| RCL_Motor_process_2_newpayment | **NO_BASELINE** (no exact-name repo file) |

This matches the finding's 6-drift list exactly (the other three drifted baselines are
non-interface: `SAP_LIVE_FULL` + two dashboards) and confirms all 12 live views exist.

**Operational consequence worth stating for RULE-10/FA work:** the three not-MATCH interface views
are precisely (ก)/(ง)-relevant CREATE/NEWPAYMENT views. `b10a22f`'s D1/D2 membership numbers are
safe (queried live views), but any *repo-file-based* reasoning about those three views is invalid
until fresh captures land.

## Rest of the commit

- Governance rule ("repo files are baseline captures, live inspection required before behavioral
  claims") follows directly from the evidence; the self-correction paragraph about Claude's earlier
  repo-007-based containment reasoning is accurate — conclusion held, reason was invalid, and the
  distinction is recorded rather than blurred.
- Coverage gaps (10 unbaselined live views, 4 unmapped local captures) listed by name; alias
  guesses explicitly forbidden — correct.
- 045 correction: `message_raw` (restricted, BigQuery-only, PII-flagged) + sanitized
  `error_template` with never-quote rules in the header — a deliberate, documented retention
  decision replacing RQ-0000's sanitized-only shape; boundary rules are explicit.
- Archive fail-closed now requires a tested human alert — consistent with `2647350`'s
  monitoring-separation lesson.
- RULE-03 expansion on live `SAP_LIVE_FULL` correctly stays design-only until after 03-Aug
  (UpdateDate-only tie is real: live view lacks UpdateTime in its ordering — same class of gap 024
  just fixed in the mirror).

Queries used: 1 wrapper-gated inventory query (~10 MiB) + JSON re-fetch (~10 MiB) — the reproduction
Boat explicitly requested.
