# Review — RQ-20260805-1956-post-import-activation-rehearsal

Reviewer: Claude Code
Artifact: commit `8b1f2bb`; live inventory/diagnosis evidence `4ed5dd6`, `43bbed3`;
`docs/design/POST_IMPORT_AUTOMATION_DEPLOYMENT.md`.
Verdict: **PASS**

## Checked against the request's own checklist

- **Dependency/review order**: the document's six-step order (DDL 072 → DDL 073 → updated Unit-1
  workflow revision → dispatcher → watchdog → Apps Script publisher config) matches exactly the
  order these artifacts were actually reviewed and passed this session (RQ-1928, RQ-1946, RQ-1939's
  workflow deltas, RQ-1939 dispatcher, RQ-1950 watchdog) — this isn't an aspirational ordering, it
  reflects what's genuinely already been gated.
- **Three dedicated least-privilege identities**: cross-checked against the actual README files —
  dispatcher (BigQuery job creation + `workflows.executions.create` on the one workflow, per
  `infra/post_import_dispatcher/README.md`), watchdog (BigQuery job + `workflows.executions.get`/
  `cancel` + publish on the one existing alert topic, per `infra/post_import_watchdog/README.md`),
  and a Pub/Sub push identity limited to `run.invoker` on the private dispatcher — no identity is
  broader in this plan than what the underlying services actually require.
- **IAM separation, no default service account**: explicit "Do not use the default Compute Engine
  service account. Do not grant project-wide Editor or Owner" — worth noting for context (not a
  defect in this plan): the *existing*, already-deployed `v3-nightly-orchestrator` workflow's own
  `UNITS_2_5_ARCHIVE_FAILED` log entry shows it currently runs as
  `919786098205-compute@developer.gserviceaccount.com` (the default Compute service account) — a
  pre-existing condition on a different, already-live component, outside this plan's own scope
  (which governs the three *new* post-import identities, not a retrofit of the existing workflow's
  identity).
- **Required measured timeout/delivery configuration, not silently defaulted**: matches the watchdog
  code's own enforcement exactly (`CLAIM_TIMEOUT_SECONDS`/`EXECUTION_TIMEOUT_SECONDS`/
  `CANCEL_WAIT_SECONDS` all fail closed if unset, reviewed under RQ-1950) plus adds the two
  additional required-but-not-yet-code-enforced values (watchdog schedule cadence, Pub/Sub delivery
  attempts/ack deadline) as deployment-time inputs.
- **Ten synthetic rehearsal cases — checked each maps to real, already-reviewed behavior, not
  invented properties**: (1)/(2) map to DDL 072's claim/bind idempotency and the dispatcher's
  already-`STARTED`-with-execution no-op check; (3) maps to the workflow's self-bind-before-
  `log_start` ordering; (4) maps to `stop_after_post_import_unit1`'s early `return`; (5)/(6) map to
  DDL 073's zero-residual `SUCCEEDED` path and its error-precedence `REJECTED_BY_SAP` classification;
  (7) maps to `gate_post_import_residual` calling `complete_post_import(..., "HUMAN_ACTION")` then
  `fail_closed` (which publishes to the real alert topic and raises); (8)/(9) map to the watchdog's
  cancel-and-poll-before-`TIMEOUT` and the three-attempt release/alert bound; (10) maps to the
  dispatcher's `ValueError`→400 path, which never reaches `claim()` or `create_execution` for
  malformed data, leaving Pub/Sub's own retry/DLQ policy (configured, not code) to handle it. All
  ten are genuinely testable against artifacts already reviewed this session, not aspirational.
- **Synthetic isolation/no production replay**: "cannot select a production delivery manifest or
  invoke SAP" and "Do not use LogID 21153 or replay any production file" — an explicit named
  real-LogID exclusion, not just a generic instruction.
- **Alert receipt proof, activation order, rollback boundary**: rehearsal step 7 requires the human
  alert to actually "reach Boat," not just be published; activation is sequenced
  (topic → bounded poll → watchdog schedule → one full daily cycle observed) before declaring
  unattended operation; rollback is configuration-first (blank the topic, pause schedule/
  subscription, revert workflow revision) and explicitly forbids deleting evidence
  tables/messages/manifests/execution history — consistent with this project's standing
  never-destroy-evidence practice.
- **Live inventory claims — independently re-verified, not trusted from the commit's own text**:
  - Re-ran the referenced diagnostic (`sql/adhoc/20260805_verify_failed_v3_balance_gate.sql`)
    myself: confirms **559 identities, 1 held, 558 delivery-matched, 0 remaining target
    mismatches** for `V3NIGHTLY-2026-08-03T11:24:27-437934f3`, and
    `sp_build_v3_newpayment_delivery_ready`'s `last_altered` is `2026-08-03 11:30:34`.
  - Checked `pipeline_run_log` directly: `UNITS_2_5_ARCHIVE_FAILED` for that run is timestamped
    `2026-08-03 11:29:40` — 54 seconds before the procedure replacement at second-level log
    precision, consistent with the claimed "52 seconds" at the sub-second precision the live
    Workflows/BigQuery APIs would have shown. The failure is genuinely pre-fix.
  - Ran read-only `gcloud` inventory myself: `v3-nightly-orchestrator` is `ACTIVE` (a newer revision
    than the commit's snapshot, expected since time has passed and this is a moving target);
    `sap-extract-schedule` is `ENABLED` at `30 20 * * *` (20:30 ICT, matching the claim);
    `auto_load_sap_data_in_bucket_to_bigquery` is `ENABLED` at `0 1 * * *` (the "legacy 01:00
    loader"); `v3-orchestrator-alerts` exists in Pub/Sub; no Cloud Run service or Pub/Sub topic
    matching "post-import" exists anywhere in the project. Every inventory claim checked out.
- **No deploy or mutation performed by this artifact**: confirmed — this commit and its two
  evidence commits only add/edit Markdown design docs and one read-only `sql/adhoc/*.sql` file. My
  own verification queries were the same read-only `INFORMATION_SCHEMA`/data-selects the commit
  itself describes, plus read-only `gcloud describe/list` calls — no mutation, deploy, IAM grant,
  trigger, or execution occurred in this review.
- **`git diff --check`**: independently re-ran on all three commits (`8b1f2bb`, `4ed5dd6`,
  `43bbed3`) — clean.

## Verdict

PASS. This is a plan document, not code, but its claims are concrete and checkable — and every one
checked out against live BigQuery and live GCP state, not just against its own prose. The
ten-case rehearsal list is a genuine, complete mapping onto behavior already verified in the four
preceding reviews this session, not a decorative checklist. No deploy, mutation, or production
action is requested or occurred in this review.
