# Review — cb4cb29 (D16 INCIDENT-002a population split)

**Queue entry:** RQ-20260801-1117-d16-002a-population-split · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked: (1) arithmetic — 65+2+86+7+30+1 = 191 records ✓, pure = 67 ✓, boundary alarms = 10 ✓;
shapes are mutually exclusive by CASE precedence. (2) Classification logic read in the committed
SQL: candidate grain as stated (non-CMI Period 1, Paid, real DocEntry, Expected>0, delta = CMI
premium); SHARED (same Actual + same InvoiceNo, NULL-safe TRIM/NULLIF) is checked **before** PURE —
conservative precedence that keeps ambiguous rows out of the pure population; NULL comparisons fall
through to UNCLASSIFIED_REQUIRES_HUMAN_REVIEW — fail-to-human, correct. (3) The id→human_id join
fix is real (CareOS `careos_orders.id` vs SAP `U_OrderID` human ID), and the zero-row failed
diagnostic is disclosed rather than hidden. (4) Full job provenance for the corrected run
(`d16_002a_split_20260801_111420`, 258,022,275 bytes processed, under ceiling). (5) Epistemics:
191 is correctly gated as diagnostic-only, NOT a replacement for the blocked 401 (which is
confirmed to have no recoverable job ID — commit timestamp explicitly not substituted); the 263
label kept as operational, not measured; 002b/224-order populations untouched; credit-shell
membership kept as boundary alarm only. (6) `L77828566` evidence checklist maps one-to-one onto
042's `sp_record_fa_verification` contract fields — correct interlock with the FA register.

Notes (no verdict impact):
1. The candidate set is **current-snapshot-conditioned** (one current SAP key per order). The
   `L80046687` lesson stands: an order whose mirror state changed after posting can drop out of
   this population silently. The finding's own key-level-reconciliation open item covers this;
   just don't read 191 as historically complete.
2. Stated correctly by the author and worth repeating: re-run after reviewed 024 deploys, since
   current-state selection still uses the BatchRunDate/DocEntry order pending UpdateDate columns.

Queries used: 0.
