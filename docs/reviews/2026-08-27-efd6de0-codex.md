# Review — RQ-20260811-1845-daily-completeness-dispatch-delta

Reviewer: Codex
Artifact: exact workflow delta in commit `efd6de0`; `infra/v3_nightly_orchestrator.workflows.yaml`
Fixed point: `ca8ebd1`
Verdict: **BLOCK**

## Standards

**Hard finding — malformed query cells still escape the fail-closed path.** The new guards prove
only `jobComplete` and one returned row, then directly evaluate
`int(count_rows[0].f[0].v)` / `origin_rows[0].f[0].v`. A one-row response with absent or empty
`f`, absent/NULL `v`, or a non-integer count raises outside `fail_closed`. This contradicts the
hunk's claim that malformed results now follow the explicit record→alert→raise contract and fails
`AGENT_RULES.md`'s fail-closed/goal-verification requirements. Add field-cardinality, non-NULL,
and count-convertibility gates before dereference.

The new step labels are distinct and clear; `alert_topic` plumbing is complete; moving
`complete_post_import_success` is surgical and in scope. No actionable baseline smell found.
Later commit `65bd74a` changes delivery-count plumbing only and does not alter these reviewed
hunks.

## Spec

**Hard finding — connector/query exceptions still escape the required contract.** The prior BLOCK
states that API/query and malformed-result failures bypassed `fail_closed`, and requires both
lookup subworkflows to route their completion and result-shape gates through it. Both
`googleapis.bigquery.v2.jobs.query` calls remain outside `try/except`, so a connector or BigQuery
exception terminates without the required failed run-log and alert.

**Hard finding — result-shape gating is partial.** `len(rows) == 1` does not establish a usable
single field or value. This leaves the original malformed-result requirement incomplete.

Both added `fail_closed` calls correctly supply `project`, `run_id`, `alert_topic`, `step`, and
`reason`. Post-import ordering is corrected: `SUCCEEDED` is written only after original-run
resolution and snapshot completion. `HUMAN_ACTION` needs no matching reorder because that branch
intentionally builds no snapshot and immediately raises after recording the residual state.
No scope creep found.

## Checklist 1–12

1. Traceability: **PASS** — exact commit, fixed point, request, and prior BLOCK identified.
2. Provenance: **PASS** — review uses the prior verdict and exact delta, not re-derived counts.
3. NULL-safety: **BLOCK** — nested result values are not presence/NULL gated.
4. Ordering: **PASS** — terminal-success persistence now follows snapshot completion.
5. Column order: **N/A** — no interface schema change in this delta.
6. Grain: **PASS** — original outbound run binding remains unchanged.
7. Distribution: **PASS** — no count-parity correctness claim.
8. Knowledge consistency: **PASS** — terminal-manifest and human-action behavior are preserved.
9. Scope: **PASS** — reviewed workflow delta is limited to the two required corrections.
10. Rollback: **N/A** — source-only, not deployed by this artifact.
11. Cost hygiene: **PASS** — on 2026-08-27 the exact unchanged DDL 067 passed
    `scripts/bq_safe_query.sh --dry-run-only` at 0 bytes; no real query was executed.
12. Honest labelling: **BLOCK** — comments claim malformed results are fully routed through
    `fail_closed`, but malformed nested cells and query exceptions are not.

Required correction: wrap each lookup query exception into its named `fail_closed` path; validate
the single row's `f` cardinality and non-NULL value, plus integer convertibility for the count,
before dereference; then request one exact-commit delta review. The successful DDL 067 dry-run need
not be repeated unless that SQL changes.
