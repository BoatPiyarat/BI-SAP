# Review: RQ-20260821-1213-mo-rcl-prod02-blocked-export

Reviewer: Claude Code
Class: A
Artifact: commit `ce2b4e9` (docs) + Codex's live BigQuery jobs referenced in it
Verdict: **PASS**

## Claim under review
After a same-session SAP extract/load/V3 refresh, all 939 immutable `MO-RCL-20260817-PROD-02`
ready items were present in SAP (mirror), so the staleness gate correctly blocked the production
export; no matching GCS object exists and no export SQL ran. Also: EDC 322/322 still missing, 1-15
Aug 2,295 pairs / 2,283 orders still missing (superseding Mo's reported 2,285 and 2,275), DDL
067/075 dry-run clean, period-cutoff verification file still broken at line 116, duplicate-QR
review still open.

## Independent verification performed
One targeted query (plus the pre-existing gsutil check I'd already run this session):
- `already_in_mirror_now` via `sap_integration_v3.sap_mirror_state.U_OrderItem` joined to
  `mo_rcl_prod02_ready.OrderItem` (distinct): **939/939** — matches the claim exactly.
- `gsutil ls -l gs://interface-file/RCB_MOTOR/INSURANCE_RCB_06_MO_RCL_RECOVERY_20260817_*.csv`:
  "matched no objects" — confirms no export happened.

**Self-correction surfaced during this review**: my own 10:56 ICT staleness check (recorded in
`docs/HANDOFF_QUEUE.md` and `20_SAP_PROGRESS.md`, "0 of the 939 ready items are now present in
`SAP_LIVE_FULL`") used an invalid join — it cast `sap_integration_v2.SAP_LIVE_FULL.DocEntry`
(SAP's internal integer key) directly against `mo_rcl_prod02_ready.OrderItem` (a CareOS
order-item identifier string). These keys were never comparable; the query would return 0 matches
regardless of true SAP state. Per `AGENT_RULES.md`, `SAP_LIVE_FULL` is a raw shard and the correct
one-row-per-(OrderItem,Period) join surface is `sap_mirror_state`/`stg_sap_state` on
`U_OrderItem` — I cited the wrong table/key pair for the claim I made. This is now corrected in a
follow-up doc entry; flagging it here per the honest-labelling checklist item rather than quietly
fixing it.

## 12-item checklist
1. Traceability — PASS. Every number in `ce2b4e9` is tied to a named job ID, table, and UTC
   timestamp (`codex_mo_prod02_fresh_checks_20260821`, `sap-extract-job-7sspw`, watermark
   generation, etc.).
2. Provenance over repetition — PASS. Cites the 10:56/22:11 handoffs and the 08-17 gate manifest
   by commit/table rather than re-deriving them.
3. NULL-safety — N/A for this artifact (docs recording query results, not a new predicate).
4. Ordering — N/A, no date-sort logic in this artifact.
5. Column order — N/A, no interface file was produced.
6. Grain stated — PASS. "939 ready items", "distinct order IDs" vs "pairs" for the 1-15 Aug
   population are kept explicitly separate (2,295 pairs / 2,283 distinct orders), avoiding the
   grain-confusion bug class this project has hit before.
7. Distribution, not row count — PASS. The blocking conclusion rests on set membership (which
   items are now in the mirror), not a bare count match.
8. No contradiction with knowledge — PASS. Correctly treats `SAP_LIVE_FULL` as the raw shard and
   routes the claim through mirror state; does not re-litigate D1/D10–D14.
9. Scope — PASS. Stayed inside the exact handoff ask (refresh, recheck, report); explicitly did
   not build any interface file for the four unscoped items, matching the handoff's instruction.
10. Rollback — N/A, nothing was deployed.
11. Cost hygiene — PASS. All cited jobs are dry-run-first per the session note; byte counts given
    for the batched check (7,750,115,582 bytes, under the 20 GiB cap).
12. Honest labelling — PASS. Explicitly states "no unscoped interface file was built" and leaves
    the duplicate-QR review and period-cutoff parse error as still-open, not rounded up to done.

## Risk / gap noted
None in the reviewed artifact itself. The one real gap found during this review was in my own
prior work (item above), not in `ce2b4e9` — corrected separately, not blocking this PASS.

Checklist 1–12 reviewed; no gap found in the artifact under review.
