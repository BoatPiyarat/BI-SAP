# Session 2026-07-31 — Claude Code (role: reviewer + deploy only, per Boat's 2026-07-31 reallocation)

## 1. Review-debt verification (instruction said 6 OPEN mine — verified against repo, not summaries)

The "8 OPEN / 6 mine" figure was true as of `19c49cf` (the commit `CURRENT_STATE_20260731.md` was
verified against). Ten commits later, `701009d` (2026-07-30 22:35 ICT) already cleared all six:
RQ-1614 → PASS, RQ-1555 → PASS, RQ-1230 → PASS, RQ-1211 → PASS, RQ-292042-06 → PASS,
RQ-292031-07 → PASS WITH NOTES. Verified by reading all six `docs/reviews/2026-07-30-*-claude.md`
files (real 12-point reviews, verdicts match the queue), not by trusting the commit message.

## 2. RQ-20260730-2323 reviewed (the one genuinely-open Claude Code item)

Verdict **PASS WITH NOTES** — `docs/reviews/2026-07-31-935ac8f-claude.md`. Substantive note:
`SECURITY_FINDING_20260730.md`'s "Cloud Run metadata returns credential env values" sentence
conflicts with the 07-31 verified `secretKeyRef key=latest` binding (retraction R9), and its
OPEN-rotate status is stale now that rotation CLOSED 2026-07-31. Reconciliation addendum requested
from Codex (author) — one round, per protocol. Zero reviewer queries used.

## 3. Codex 024/025 work — NOT REVIEWABLE YET

`sql/ddl/024_sap_mirror_doc.sql` and `025_sap_mirror_state.sql` are untouched since `bc760ac`
(2026-07-27); `024` contains zero occurrences of `UpdateDate`/`UpdateTime` and the worktree is
clean. The §2.3 change set does not exist in this repo yet, so checks (a)–(e) and the +191 row
delta cannot be executed. Prepared in advance instead:

**Blast-radius (static, all 8 consumers read — no queries):** no consumer depends on column
POSITION of `sap_mirror_doc`/`sap_mirror_state`. Appending 2 trailing columns in `024` is
additive-safe.
- `002`: reads `SAP_LIVE_FULL`, not the mirror tables; superseded in the nightly chain by `026`
  (⚠️ pre-existing hazard, unrelated to this change: if `sp_refresh_sap_state` were ever re-run it
  would `CREATE OR REPLACE TABLE` over what is now a view — it errors in BigQuery, safe by accident).
- `026`: `stg_sap_state = SELECT * FROM sap_mirror_state` verbatim — schema propagates, but §2.4
  changes only `025`'s tiebreaker, not its projection, so the view is unchanged; `026`'s own header
  records that no downstream consumer does `SELECT *` against `stg_sap_state`.
- `034`/`037`: touch `sap_mirror_state` only via `SELECT DISTINCT U_OrderItem` — named column.
- `038`: `fn_mint_adj_invoice` reads `sap_mirror_doc` by named columns (`U_OrderID`, `U_InvoiceNo`).
- `039`/`041`/`042`: explicit column lists on their own tables; mirror references are named-column.

The one positional surface remains `024`'s own 4-branch `UNION ALL` (checks a–c) — that is where
the review will focus when the artifact lands.

## 4. Deploy

Nothing deployed. Nothing is deploy-eligible: no Boat approval on record for any pending artifact,
and RQ-2230 (`043` MERGE) + RQ-2200 (`bq_safe_query.sh` fix) are still awaiting Codex review.

Review debt after this session: **2 OPEN (mine: 0)** — RQ-2230 and RQ-2200, both Codex's.
