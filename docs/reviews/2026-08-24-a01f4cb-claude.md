# Review: RQ-20260824-1319 v3-normal-rcl-qualifier-delta

Reviewer: Claude Code
Artifact: `a01f4cb`; `sql/adhoc/20260824_qualify_v3_normal_motor_newpayment.sql` (delta after
`cf17399` BLOCK)
Verdict: **PASS**

## Verification performed

Delta review of the two required corrections from `docs/reviews/2026-08-24-5afb5ff-claude.md`
(BLOCK, a different Claude Code session). Per protocol, ran the qualifier live myself rather than
trusting the request text — dry-run 68,640 bytes (matches the cited estimate exactly), real run
reproduced the identical output to the cited job `bqjob_rdd39aad5ae1f368_000001a0326bbce5_1`:
`HOLD_CANCELLED=1`, `HOLD_INCOMPLETE_RCL_SPINE=2`, `READY_NORMAL_RCL_MOTOR_NEWPAYMENT=555`
(552 distinct order_items), with `multi_event_order_items=2` and the exact same two items named —
`L78389133-V1` (periods 4, 5) and `L78525812-V1` (periods 4, 5, 6) — down to matching charge_ids
and invoice identities in the aggregated detail string.

## Required correction 1 — change-order join fanout

**Closed.** Read `sql/adhoc/20260824_qualify_v3_normal_motor_newpayment.sql:36-60` directly:
- `change_link_rows` now unions both relationship directions (`current_human_id` and
  `old_human_id`) into one `(order_id, relationship_role, counterpart_order_id)` shape, then
  `change_link` groups by `order_id` — exactly one row per `order_id`, matching the pattern already
  established in the sibling `081_v3_creditshell_flow_router.sql`.
- The classification's ambiguity check —
  `link_count>1 OR counterpart_count>1 OR (current_link_count>0 AND old_link_count>0)` →
  `HOLD_CHANGE_LINK_AMBIGUOUS` — runs *before* either single-role branch and explicitly catches the
  exact case the prior BLOCK demonstrated live (an OrderID appearing as `current_human_id` in one
  row and `old_human_id` in another). The single-role holds
  (`HOLD_CREDITSHELL_OR_CHANGE_CURRENT` / `HOLD_CHANGE_PREVIOUS`) can only fire once the ambiguous
  case has already been ruled out, so precedence is correct.
- The new `identity_item` join (`JOIN identity_item ii USING (order_item)`, itself grouped by
  `order_item`) adds no fanout of its own — it's a 1:1 lookup per `order_item`, joined for reporting
  the multi-event detail, not for filtering.

## Required correction 2 — headline count and 555-vs-552 gap

**Closed.** My independent rerun confirms the total is 558 (555 + 1 + 2), not 555, matching the
corrected claim text exactly. The gap inside the READY bucket is now explained and independently
verified down to the exact identities: `L78389133-V1` genuinely has two distinct NEWPAYMENT event
identities (periods 4 and 5, different `charge_id`/`invoice_no` each) and `L78525812-V1` has three
(periods 4, 5, 6) — real, multiple ongoing-installment charges on the same item within this one
stale pipeline run, not a query bug. This is disclosed via the new `multi_event_order_items`/
`multi_event_detail` columns rather than left implicit.

## Review focus items

- **One-row-per-OrderID change-link grain** — confirmed by code read (`GROUP BY order_id` in
  `change_link`) and consistent with my live rerun showing no inflated counts.
- **Ambiguous-link precedence** — confirmed: the `HOLD_CHANGE_LINK_AMBIGUOUS` branch is checked
  before the single-role branches in the `CASE` chain.
- **Conservation of 558 identities across mutually exclusive buckets** — the `CASE` expression is a
  single ordered if/else chain (structurally mutually exclusive, no row can match two branches),
  and my independent rerun sums to exactly 558.
- **Multi-event explanation** — independently verified, exact identity match (see above).

## Checklist 1–12

1 traceability PASS (job reproduced independently, not just read); 2 provenance PASS (correctly
scopes to "qualifier-only... DDL 058 was not changed", doesn't re-litigate the already-correct DDL
058 delta); 3 NULL-safety PASS (`IFNULL` used consistently on all `change_link` counts before
comparison); 4 ordering N/A; 5 column order N/A (not an interface file); 6 grain PASS — this is the
item the prior BLOCK failed on, now confirmed closed by both code read and live reproduction; 7
distribution N/A; 8 knowledge PASS (no contradiction with Boat's Motor-template ADDENDUM or any
other confirmed decision); 9 scope PASS (`sql/adhoc/**` read-only evidence only, no DDL/deploy
touched); 10 rollback N/A (read-only qualifier, nothing deployed); 11 cost hygiene PASS (68,640
byte dry-run matches cited estimate exactly, single grouped query); 12 honest labelling PASS — the
"stale run, not a production count" caveat is retained, and the previously-undisclosed 555-vs-552
gap is now explicit and explained rather than silently present.

Summary: Checklist 1–12 reviewed; no gap found. Both required corrections from the prior BLOCK are
independently verified closed — the join-fanout defect is structurally fixed, and the headline
figures now match the job's own output with the internal gap fully explained.
