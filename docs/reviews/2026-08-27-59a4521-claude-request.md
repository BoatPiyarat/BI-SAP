# Claude Code Class-A request — Scenario 1 immutable month-end fallback

Status: **NOT SENT / awaiting exact external-egress approval**

Artifact commit: `59a4521`

Opened from artifact timestamp: `2026-08-27T20:35:50+07:00`

Review only. Do not run BigQuery, deploy DDL, CALL a procedure, export a file, write GCS, touch SAP,
execute a Workflow, mutate Cloud Scheduler, or modify the repository.

## Proposed external review payload

Review these exact artifact files:

- `sql/ddl/103_v3_monthend_onetime_immutable_snapshot.sql` — SHA-256
  `72e18119792fd47e0ebe18bc118d710f7f77a34652c700b7835ac3440e9cc52c`;
- `sql/operator/20260827_monthend_missing_sap_onetime.sql` — SHA-256
  `53b8916236d03907dc9f8e91c8871b487c58de020bb5f3bc8807ea3ca8fe29e6`;
- `scripts/check_v3_monthend_onetime_snapshot.py` — SHA-256
  `57cafc19c439e51570440c597c5a9b69f246305420c4c6464d4a093c0e420dc3`.

Read these exact standards and dependency sources:

- `docs/AGENT_RULES.md`;
- `docs/design/SAP_INTERFACE_PRE_EXPORT_GATE.md`;
- `docs/AGENT_REVIEW_PROTOCOL.md`;
- `sql/ddl/051_v3_unit2_shadow_classifier.sql`;
- `sql/ddl/085_v3_onetime_create_shadow.sql`;
- `sql/ddl/100_v3_scenario3_archive_and_lifecycle.sql`;
- `sql/ddl/101_v3_scenario1_lifecycle_adapter.sql`;
- `docs/reviews/2026-08-27-monthend-onetime-claude.md`.

## Required review

Return two separate axes and one final `PASS` or `BLOCK` verdict.

1. **Standards/Safety:** BigQuery Standard SQL and deployability; new-object-only claims; atomic and
   append-only immutable snapshot behavior; exact 56-column names/types/positions; full final-row
   validation; deterministic identity/hash binding; ICT date handling; partition-bound reads;
   mandatory safe-query wrapper; no GCS/SAP/Workflow/Scheduler side effect; and fail-closed replay,
   source-drift, duplicate, NULL, and lifecycle handling.
2. **Spec:** Every `ONETIME` Unit 2 payment event in the chosen current OPEN ICT month must end in
   exactly one immutable payload row or durable hold. Only ordinary Scenario 1 DDL 085 validated
   `Paid` 1/1 rows absent from SAP and from every archive lifecycle may be released. In particular,
   `DELIVERED`/`PICKED_UP` without terminal SAP row ACK must remain held. The read-only operator must
   return the exact same immutable rows it validates, never a fresh live recomputation.

Check BigQuery syntax closely, including CTAS partitioning, transaction-supported statements,
`STRING_AGG` syntax, correlated subqueries, STRUCT-based hash field ordering, and zero-row builds.
Also check whether the DDL 085 ready/hold/identity binding can silently downgrade or misclassify any
event, whether hold joins can fan out, and whether all partitioned-table reads include an effective
partition filter.

The offline checker reports `V3_MONTHEND_ONETIME_STATIC=PASS` and 56 columns. The mandatory wrapper
self-test passed all 7 fail-closed parser cases. Authenticated dry-run-only validation of exact DDL
103 through `scripts/bq_safe_query.sh` passed at 0 bytes on 2026-08-27 20:38 ICT; the real query was
not executed. The operator cannot be BigQuery-dry-run until the new DDL 103 objects exist, so its
offline gate and explicit source inspection remain required in this review. A source PASS does not
authorize deployment, snapshot build, export, GCS upload, or SAP delivery.
