# Codex session — 2026-07-31

## G1 measurement

Primary batched job `g1_20260731_144401` ran at `2026-07-31 14:44:04 UTC` against
`sap_integration_v3.expected_state`, `sap_mirror_state`, and the four
`sap_integration_v2.SAP_LIVE*` shards. Dry-run/actual bytes were 8,326,924,817; billed bytes were
8,327,790,592 under the 21,474,836,480-byte ceiling. BigQuery job metadata records creation at
`2026-07-31 14:44:03.844 UTC`, start at `14:44:03.995 UTC`, and end at `14:44:11.574 UTC`.
The named source objects were `sap_integration_v3.expected_state`,
`sap_integration_v3.sap_mirror_state`, `sap_integration_v2.SAP_LIVE`,
`SAP_LIVE_2024`, `SAP_LIVE_2025`, and `SAP_LIVE_2026`.

- (ก): 2,404 records / 1,787 orders / THB 16,580,629.20.
- (ค): 58 records / 46 orders / THB 390,761.21.
- (ง): 2,996 records / 2,983 orders / THB 5,498,814.57.
- Q3a changed one `(U_OrderItem,U_Period)` winner and changed zero InvoiceNo values.
- All 496 `Invoice` and 45 `SaleOrder` DocEntry-dedup rows had non-NULL UpdateDate and UpdateTime.
- Four-branch DISTINCT grain changed 2,104,498 → 2,105,816 (+1,318), but final DocEntry grain
  stayed 1,658,776 → 1,658,776 (delta zero). Boat passed Gate 2.5 because only the final grain is
  materialized; the +191 threshold had covered one shard rather than all four.

The population CASE is mutually exclusive at record grain. Targeted follow-up job
`g1_overlap_20260731_145543` at `2026-07-31 14:55:45 UTC` found six orders spanning more than one
group (14 records), with none spanning all three. Dry-run/processed bytes were 51,866,224 and billed
bytes were 52,428,800. Therefore record amounts are not double-counted by group assignment, but
per-group distinct-order counts must not be summed as a unique-order total.

**Correction:** G1 did not repeat E1–E3 predicates in its SQL, but its `expected_state` source is
the post-exclusion output of procedure 037. Therefore THB 22,470,204.98 is a **post-E1–E3** total,
not gross pre-exclusion population. Targeted job `g1_exclusions_20260731_150736` at
`2026-07-31 15:07:38 UTC` counted the current `sap_excluded_records` register:

- `OLD_YEAR_NO_TOUCH`: 780,108 distinct `(order_item,period)` records / 291,012 orders;
- `TEST_CUSTOMER_NAME`: 347 / 156;
- `INSURER_NOT_IN_MASTER`: 3,172 / 953;
- unique across any of those rules: 783,351 records / 292,002 orders.

Per-rule counts can overlap; the `ANY_E1_E3` result is the deduplicated total. These are exclusions
from the current expected-state refresh, not a counterfactual allocation into groups (ก)/(ค)/(ง),
because the exclusion register does not retain PaymentDate or amount. Dry-run/processed bytes were
95,727,367; billed bytes were 96,468,992 under the 21,474,836,480-byte ceiling. Job metadata:
creation `2026-07-31 15:07:38.509 UTC`, start `15:07:38.609`, end `15:07:41.018`.

## Source-only DDL

- `024`: appends native DATE `UpdateDate` and INT64 `UpdateTime` after `BatchRunDate` in all four
  branches; DocEntry dedup orders by UpdateDate, UpdateTime, DocEntry.
- `025`: retains status and non-empty-InvoiceNo priority, replaces BatchRunDate with native recency,
  keeps `docs_considered`, and uses `MULTI_DOC_RESOLVED_BY_RECENCY`.
- `044` creates `sap_period_lock`; current `037` fails closed without an open-period row, clamps
  effective PaymentDate, and emits `payment_date_clamped` without storing an original-date copy.
- Pending incremental replacement `043` now uses the same recency order and propagates the two new
  columns through matched updates and unmatched inserts, preventing a future cutover from nulling
  the recency fields.

Validation: 024 dry-run passed; 044 dry-run passed at 0 bytes; 044→037 combined-order dry-run passed
with a zero-byte lower bound. 025 strict validation is intentionally deferred until 024 has been
reviewed/applied/refreshed because the currently deployed `sap_mirror_doc` lacks UpdateDate and
UpdateTime. The same 025 ranking expression was validated in the successful G1 batch. Nothing was
deployed or written to a production object.

## Query guardrail

Existing wrapper fix from `93e87ea` was inspected against the original BLOCK. Missing or malformed
`totalBytesProcessed` fails closed, explicit zero is accepted, `--force` is absent, and 20 GiB is a
hard ceiling. Offline self-test via Git for Windows bash passed 7/7. No wrapper rewrite was needed.

## P0 InsurerCode exclusion risk

Job `p0_insurer_risk_20260731_152353` at `2026-07-31 15:23:55 UTC` returned 300 records / 294
orders / THB 2,260,768.08 for July PaymentDate exclusions, zero NULL amounts, and normalized codes
`30`, `46`, `48`, `49`. Dry-run/processed bytes were 371,716,873; billed bytes were 372,244,480.
The same batch confirmed 4,810 unique G1 orders and `year_no_touch_max=2024`. Procedure 037's
OrderDate/PolicyDate exclusion basis conflicts with locked PaymentDate scope; no fix was applied.

## RULE-09 source-only implementation

Boat resolved the date-basis conflict with a narrow exception for `OLD_YEAR_NO_TOUCH` only. Updated
live-source 037 (not historical 034) to preserve raw PaymentDate before clamp, use the exact open
calendar-month interval at both the exclusion-register and population gates, and emit
`old_year_rescued`. Static checks found exactly one raw preservation, marker definition, register
guard, population guard, and output marker; 034 SQL had zero changes. Standalone 037 dry-run failed
only because undeployed 044 means `sap_period_lock` does not exist live; combined-order 044→037
dry-run passed at a 0-byte lower bound. Nothing deployed; S6–S10 remain blocked on reviewed apply.

## Export-path verification and deploy preparation

Read-only 30-Jul function logs confirmed the deployed Motor v436 sequence has eight SQL/GCS steps
and NonMotor v400 has four. Each of the 12 writes logged success before SMTP notification caused
the invocations to report crash/timeout. Live INFORMATION_SCHEMA confirmed the shared 56-position
contract; deployed expected_state has 12 internal fields and is not contract-shaped. Only
`INSURANCE_RCB` has SAP-success evidence as ImportType. Added a source-only RULE-09 deployment and
rollback runbook. Function metadata also exposed plaintext SMTP credential configuration; recorded
only the affected resource names in the existing security finding, never the value. No deploy or
legacy-view modification occurred.

## D1/D2 diagnostic and RULE-10

Job `p0_d1_d2_view_membership_20260731_160300` (query timestamp 16:06:59 UTC; dry-run/processed
8,645,545,976 bytes; billed 8,646,557,696) found 1,502/2,404 (ก) records in a relevant CREATE view
and 2,670/2,996 (ง) records in a relevant RCL NEWPAYMENT view. The remaining 902 and 326 records are
absent from the relevant current views. GCS version listing returned only the RCB_MOTOR and
RCB_NONMOTOR folder placeholders, so D3 physical-file membership remains unverified. Updated
runbook/design monitoring to distinguish GCS write, SMTP notification, and SAP pickup/import;
recorded RULE-10 and the intentional last-day-of-open-period BatchRunDate. No export SQL, deploy,
legacy-view change, or GCS write occurred.

## Type-contract and CSV ground-truth follow-up

Live INFORMATION_SCHEMA comparison confirmed type drift at exactly 22 of 56 positions across the
four CREATE and two RCL NEWPAYMENT views, entirely in money/quantity fields. Documented the gap in
028 and proposed WARN-only type monitoring after 03/08. Recursive GCS version listing returned
only placeholders under ADB_MOTOR, RCB_MOTOR, and RCB_NONMOTOR, so physical formatting/header and
a fourth folder remain unproved. Deployed Motor v436 and NonMotor v400 expose no reusable source
location; sourceUploadUrl reads returned 403 and targeted logs did not reveal a serializer. G2 is
therefore explicitly UNKNOWN and manual export SQL remains blocked. No production mutation.

## P0 mirror completeness gate

Found a 169,695,148-byte 31-Jul extract object still in the bronze prefix. Guarded job
`p0_mirror_batch_20260731_162600` proved SAP_LIVE has no 30/31-Jul batch rows; dry-run/processed
133,186,480 bytes, billed 134,217,728. Ran the existing loader scheduler once as directed, but it
and automatic retries failed HTTP 503/OOM above the 1,024 MiB limit. The object remains, so all
July D1/D2 and downstream diagnostics are stale and held. No deployment or repeat manual trigger.

## R1 decisive confirmation

Boat raised the loader to 4Gi; revision 00020's autonomous retry returned 200 and deleted the
bronze object. Post-load evidence found 12 successful LOAD jobs ×61,133 rows on 31-Jul. Per-job
metadata also found 41×60,404 on 27-Jul, 70×60,385 on 28-Jul, and 25×58,619 on 29-Jul, all with
zero bad records. R1 is un-retracted as confirmed leading explanation; interface import is a
contributing factor. Recorded the ineffective one-chunk 61,133-row extract versus threshold
20,000. L3 was not run against materialized sap_mirror_doc/state because both predate the load;
no refresh/CALL was authorized and no production object was changed.

Recorded the exact 31-Jul incident delta: 733,596 committed versus 61,133 expected, adding 672,463
duplicate rows with zero bad records. Verified live SAP_LIVE_FULL contains per-branch DISTINCT and
DocEntry row-number dedup ordered by UpdateDate (not BatchRunDate), containing retry copies before
legacy views. Designed a daily LOAD-count alert and DLQ containment; current Eventarc subscription
has no dead-letter policy and GCP's real minimum maxDeliveryAttempts is 5, best-effort.

Inspected live `sap_import_result`: 0 rows, unpartitioned, seven obsolete nullable fields. Prepared
source-only 045 for sanitized allow-listed audit metadata and an archive-on-write design; neither
was applied. Email parser/ingestion remains blocked until Boat supplies the export and K1/K2/K3 all
pass. Also recorded repo/live SAP_LIVE_FULL ordering drift (BatchRunDate in source files versus
UpdateDate live) without changing either definition.
