# Review — RQ-20260803-2112-unit2-population-magnitude-gate

Reviewer: Claude Code
Artifact: commit `8bea466`; `sql/ddl/063_v3_unit2_population_magnitude_gate.sql`,
`sql/ddl/061_v3_units2_5_nightly_wrapper.sql`, and
`sql/adhoc/20260803_unit2_magnitude_decision_fixture.sql`.
Verdict: **PASS**

## Checklist 1–12

1. **Traceability** — pass. Re-ran the cited wrapper self-test offline
   (`bash scripts/bq_safe_query.sh --self-test`) and independently confirmed 7/7. Live BigQuery
   dry-run bytes are taken on the author's cited evidence (this environment has no `bq`/`gcloud`
   auth to reproduce them; DDL-only dry-runs at 0 bytes is expected and unsurprising for
   CREATE TABLE/CREATE PROCEDURE bodies).
2. **Provenance over repetition** — pass. Cites the fixture and self-test rather than re-describing
   the decision logic in prose only.
3. **NULL-safety** — pass. Amount-breach branch (063 lines 212–219) treats a NULL
   `baseline_amount_satang` as 0 via `IFNULL(...,0)=0` before ever reaching `SAFE_DIVIDE`, and
   `SAFE_DIVIDE` itself is NULL-safe on the denominator; three-valued `TRUE OR NULL` logic means an
   unevaluated-order concern doesn't change the result either way.
4. **Ordering** — pass. Baseline selection (lines 96–110) orders
   `evaluated_at DESC, pipeline_run_id DESC` — a real deterministic tiebreak, not a bare date sort.
5. **Column order** — N/A, no interface-file column list here.
6. **Grain stated** — pass. `PAYMENT_EVENT` vs `SCHEDULE` population_grain is explicit and carried
   through distribution, result, and run tables; the event/schedule split matches the request.
7. **Distribution, not row count** — pass, and this is the artifact's whole point: three conservation
   `ASSERT`s (lines 148–168) tie `v3_unit2_distribution` sums back to the source shadow tables
   exactly (event records, event amount, schedule records) rather than trusting an aggregate count.
8. **No contradiction with knowledge** — pass. Cross-checked the claim "placement before Unit 3" —
   confirmed in `061` (`sp_evaluate_v3_unit2_magnitude` is called at line 16, `sp_build_v3_unit3_mapping_holds`
   at line 18, with no exception handler in between, so a failed `ASSERT` in the magnitude gate
   aborts the script and Unit 3 is never reached). Also traced the baseline-selection dependency on
   `pipeline_run_log.step='UNITS_2_5_ARCHIVE'`: this looked at first read like a step nothing writes
   (no SQL file inserts that literal), but `infra/v3_nightly_orchestrator.workflows.yaml:273` does
   log exactly that step around the `sp_run_v3_units2_5` CALL, and it only logs `SUCCESS` when the
   call — including the magnitude `ASSERT`s inside it — completes without error. So a breached run
   correctly cannot become a future baseline, and a healthy-zero night correctly can. Worth recording
   here since it's a legitimate cross-file dependency a future reader could otherwise misread as broken.
9. **Scope** — pass. File contains `CREATE TABLE IF NOT EXISTS` / `CREATE OR REPLACE PROCEDURE`
   only, all within `sap_integration_v3`; no `INSERT` seeding `v3_unit2_magnitude_config`, matching
   the request's explicit "no configuration seed" claim.
10. **Rollback** — N/A pre-deploy; `CREATE TABLE IF NOT EXISTS` is additive and
    `CREATE OR REPLACE PROCEDURE` on a not-yet-live procedure has no prior definition to preserve.
11. **Cost hygiene** — pass. Fixture is literal-only (no production table reads), matching its own
    "no production table reads or writes" header; self-test reconfirmed offline (see item 1).
12. **Honest labelling** — pass. Header explicitly states "SOURCE ONLY / Class A. No configuration
    seed in this file," and the request body separately discloses that BigQuery dry-runs were
    initially BLOCKED by an auth failure, only later resolved — not silently omitted.

## Fixture-to-implementation match

Independently re-derived the six fixture cases against the actual breach expression in `063`
(lines 204–219: `ABS(delta) > absolute AND (baseline=0 OR percentage_delta > percentage)`) and
confirm they match the fixture's `magnitude_breach` UDF exactly, including the zero-baseline floor
case (`baseline=0, current=25, max_absolute=25` → not a breach, since the comparison is strict `>`)
and the AND-not-OR semantics (`percentage_only_does_not_block` case).

Checklist 1–12 reviewed; no gap found.
