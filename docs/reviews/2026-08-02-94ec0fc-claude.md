# Review — 94ec0fc + 470f684 (July export BLOCK delta: 048/049/runbook)

**Queue entry:** RQ-20260801-2325-july-export-block-delta · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS — both RQ-2241 blocks and both required items are cleared.** RQ-2243's runbook
block clears with it. Deploy/CALL/UAT2/production each remain separate gated steps per the runbook.

## BLOCK 1 — cleared, gate-verified

Both branches now project all 56 columns explicitly with harmonized types (STRING text, FLOAT64
money, INT64 Period/TotalPeriods; expression lists positionally identical). The former
position-22 failure was re-tested with the statement-level typed-literal gate: the fixed
`_payload_source` statement **validates with a true 9,105,294,566-byte semantic estimate** (the
pre-fix version errored). Bonus effect confirmed: the winner fingerprint now hashes normalized
payloads, so cross-view value-equality is no longer defeated by type rendering.

## BLOCK 2 — cleared, and stronger than the minimum asked

049 is now `sp_export_july_payment_to_archive`: it writes **only** to the restricted archive
prefix (`gs://rcb-bronze-zone/sap-interface-archive/...`), never production. The runbook's new
mandatory G3 stage covers every risk the block named: 56 header fields, BillingAddress
comma-quoting round-trip, per-position numeric format on all 22 drift positions, NULL/zero
behavior, DDMMYYYY, Paid required fields, BatchRunDate `31072026`, UAT2 SAP parser result, and
**explicit Boat/Aware acceptance of the exact archive hash/generation** before production.

## Required items — both present

- **Exact-byte promotion**: production delivery is a server-side copy of the *accepted archive
  generation* — no re-serialization — with create-only semantics, generation+hash equality
  verification, and the PREPARED_ARCHIVE → ARCHIVED_PENDING_UAT2 → UAT2_ACCEPTED → DELIVERED →
  ACKNOWLEDGED state machine reconciled by URI/generation/hash, LogID captured into
  `export_archive`. This is the approved archive-on-write design implemented properly
  (archive-first + promote), which is stronger than the post-write copy the block minimally asked for.
- **Paid completeness ASSERT** (InvoiceNo/PaymentDate/PaymentMethod/PaymentChannel non-empty) plus
  PolicyNo ≤50 and the DDMMYYYY UNPIVOT assert — all inside the shadow build, i.e. **before any
  archive/export side effect** (049 CALLs 048 first).

## Follow-up commit `470f684` — correct and honestly reasoned

Direct Order + exact-OrderItem + PURCHASED-lead joins re-applied inside the July export scope,
because 013 is an incremental MERGE and cannot retroactively remove pre-deploy unqualified staging
rows. Right call: the export must not depend on a historical staging cleanup that hasn't happened.

## Notes (non-blocking)

1. The UAT2 stage downloads a PII-bearing file to a local temp directory — add one runbook line to
   **delete the local copy after upload + hash capture**, per the SECURITY_FINDING precedent.
2. `export_archive` gained `archive_uri`/new states since the previous draft — safe only because
   the table has never been created live (confirmed in RQ-2228). After first deployment, schema
   changes to this table need explicit migration steps, not CREATE IF NOT EXISTS drift.

Queries used: 1 statement-level 0-byte dry-run (BLOCK-1 re-verification).
