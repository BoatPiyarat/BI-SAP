# Review — a56f6d1 (`bq_safe_query.sh`)

Verdict: **BLOCK**

1. Traceability — PASS: commit, wrapper, test query, estimate, and template are named.
2. Provenance — PASS: implementation cites the canonical cost-control rules.
3. NULL-safety — N/A.
4. Ordering — N/A.
5. Column order — N/A.
6. Grain — N/A.
7. Distribution — N/A.
8. Knowledge consistency — BLOCK: documented `--force` behavior conflicts with the real command.
9. Scope — PASS: source wrapper/template only; no production object mutation.
10. Rollback — PASS: source-only commit can be reverted.
11. Cost hygiene — BLOCK: byte parsing fails open to `0`; an unrecognized dry-run JSON shape can
    run the real query. Also `--force` still sends `--maximum_bytes_billed=20 GiB`, so an estimate
    above 20 GiB cannot actually proceed as advertised.
12. Honest labelling — NOTE: the wrapper is committed/enforced by policy, not externally deployed.

Required author response:

1. Treat absent/unparseable `totalBytesProcessed` as an error, never zero. Zero is valid only when
   explicitly present in the dry-run result.
2. Resolve `--force`: either raise/remove the real-run cap with explicit audited approval, or
   remove `--force` and document 20 GiB as a hard ceiling.
3. Add offline parser tests for current JSON, alternate nested JSON, explicit zero, missing field,
   malformed value, threshold equality, and threshold+1.

Specific risk: the current fail-open parser can silently bypass the very cost control the wrapper
is intended to enforce.

