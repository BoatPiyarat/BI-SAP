# Review — RQ-20260805-0943-exact-delivery-identity-conservation (delta)

Reviewer: Claude Code
Artifact: commit `70e5671`; delta in `sql/ddl/062_v3_mark_exact_delivery.sql`.
Answers: the sole required note in `docs/reviews/2026-08-05-7d4866a-claude.md`.
Verdict: **PASS — note resolved**

## What changed

Two new set-difference counts are computed and asserted to both be zero:

- `v_identity_unmatched`: non-held payload identities for `p_pipeline_run_id`
  (`file_role='NEWPAYMENT'`, excluding `v3_unit5_balance_hold`) that have **no** matching
  `export_archive` row for `p_export_run_id` on `(order_item, period, charge_id, payload_hash)`.
- `v_archive_unmatched`: `export_archive` rows for `p_export_run_id`
  (`delivery_status='ARCHIVED_PENDING_OBJECT_METADATA'`) that have **no** matching non-held payload
  identity for `p_pipeline_run_id` on the same 4-tuple.

`ASSERT v_identity_unmatched=0 AND v_archive_unmatched=0` now gates delivery alongside the
pre-existing count check.

## Verification

Traced both directions by hand against the exact WHERE/NOT EXISTS clauses:

- `v_identity_unmatched=0` means every eligible identity from the claimed pipeline run has a
  corresponding archived row — catches **missing** archive rows.
- `v_archive_unmatched=0` means every archived row for the claimed export run corresponds to an
  eligible identity from that pipeline run — catches **extraneous/wrong** archive rows.
- Together these are genuine bidirectional set equality on the full 4-tuple, including
  `payload_hash` (not just the natural key), matching the same binding strength already required
  for the Unit 6 completeness snapshot (`docs/reviews/2026-08-04-913ff3b-claude.md`).
- The balance-held exclusion is applied identically to the original `v_identity_rows` definition in
  both new counts — `i.file_role='NEWPAYMENT'` plus the same `v3_unit5_balance_hold` NOT EXISTS,
  applied both directly (`v_identity_unmatched`) and inside the nested subquery
  (`v_archive_unmatched`). No drift between the three definitions.
- No vacuous-pass risk: `v_identity_rows>0` is still asserted first, so if every identity were
  unmatched, `v_identity_unmatched` would equal that same nonzero count and the new assert would
  correctly fail — a completely disjoint pairing cannot pass by both sides being trivially empty.
- The original count-only check (`v_archive_rows=v_identity_rows`) is retained, not removed — but
  it's no longer the only conservation evidence, which is exactly what the review request asked to
  confirm ("no count-only join ... remains as the only conservation evidence"). The new check
  strictly implies the count check (equal sets imply equal counts), so keeping both is harmless
  redundancy, not a conflict.

Independently re-ran `scripts/bq_safe_query.sh --dry-run-only -f sql/ddl/062_v3_mark_exact_delivery.sql`
myself — `0 bytes`, matching the request's own claim. Read the complete current file end-to-end (not
just the diff) to confirm the delta integrates cleanly with the rest of the procedure — filename-
exactness, atomic 3-table write, and all three replay-protection assertions from the base review
are untouched and still present.

## Verdict

PASS. The identity-set conservation gap from the base review is fully closed. No deployment,
procedure CALL, GCS write, Gmail, scheduler, BigQuery table mutation, or SAP action is requested or
occurred in this review.
