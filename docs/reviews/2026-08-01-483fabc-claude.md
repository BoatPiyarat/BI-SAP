# Review — 483fabc (043 watermark-predicate delta fix)

**Queue entry:** RQ-20260801-1448-043-watermark-predicate-delta · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — re-reviewed under the strengthened protocol after this reviewer's prior
miss on this exact file (`_SCORECARD.md` rework entry; addendum in `2026-08-01-6ef690b-claude.md`).

## 1. Diff scope — exact and minimal
`git diff 6ef690b..483fabc` touches only the four WHERE lines (119/143/167/191); each now reads
`DATE(UpdateDate) > wm_date OR (DATE(UpdateDate) = wm_date AND UpdateTime > wm_time)` — both
predicates converted, matching the handoff-specified form. No pruning cost: SAP_LIVE* shards have
no partitioning (verified 2026-08-01 schema pass).

## 2. Whole-file inventory — not just the reported lines
Line-by-line grep of every `UpdateDate` / `UpdateTime` / `wm_*` usage in the full file at
`483fabc`: projections (117/141/165/189) DATE; WHERE (4 lines) DATE-vs-DATE; delta ORDER BY (199)
operates on the DATE projection; MERGE UPDATE/INSERT lists (238/249/262) carry delta's DATE/INT64;
watermark selector (268–272) groups delta's DATE into `new_wm_date DATE`; control table + DECLAREs
+ seed all DATE/INT64. **Zero TIMESTAMP-domain remnants.**

## 3. Statement-level typed-literal dry-run — the new gate, applied and proven both ways
- **Fixed statement** (delta CTAS body extracted standalone, `wm_date`→`DATE '1900-01-01'`,
  `wm_time`→`0`): validates against the four real shards with a true semantic estimate of
  **7,319,841,706 bytes** — this is genuine expression/type analysis, unlike the 0-byte
  CREATE-PROCEDURE dry-run that is blind to body semantics.
- **Negative control** (same extraction from pre-fix `6ef690b`): fails with
  `Unable to find common supertype for templated argument <T1>: {DATE, TIMESTAMP}` at the WHERE
  line — demonstrating this gate would have caught the defect on the first pass, and the
  analytic-in-aggregate defect before it. Proposed as a standing protocol gate
  (`HANDOFF_QUEUE.md` 14:02 ICT entry; spec in the session log).

## 4. Remaining item no dry-run can reach yet (unchanged, explicitly gated)
The MERGE statement itself cannot be semantically validated until the live `sap_mirror_doc` has
`UpdateDate`/`UpdateTime` (chain ② 024 deploy). Static type check passes (delta DATE/INT64 →
post-024 mirror DATE/INT64), and chain ③'s bootstrap → full catch-up → **row-for-row diff vs a
fresh 024 rebuild before repoint** remains the hard gate that will execute it under observation.

Queries used: 2 dry-runs, 0 bytes billed (fixed-statement validation + negative control).
