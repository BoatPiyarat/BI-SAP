# Review — RQ-20260805-2207-numeric-alert-config-inputs

Reviewer: Claude Code
Artifact: commit `81c7f9a`; canonical input/checklist clarification.
Verdict: **PASS**

## Checked — the technical claim independently reproduced

- **DDL 066/068 deliberately seed no numeric threshold rows**: both files define
  `max_record_increase INT64 NOT NULL` / `max_order_increase INT64 NOT NULL` as config-table
  columns and reference them in comparison logic (`... > cfg.max_record_increase OR ... >
  cfg.max_order_increase`), but neither file contains an `INSERT` populating real values — I
  grepped both files directly and confirmed no seed statement exists.
- **No production numeric artifact exists anywhere in the repo**: repo-wide search for
  `max_record_increase`/`max_order_increase` outside the two DDL files finds only design-doc prose
  (`SAP_RUNBOOK_v3.md` naming Boat as the future approver), progress-log references to this same
  gap, and two `sql/adhoc/*_fixture.sql` files that use these names purely as *test* parameters,
  not production config rows. Nothing was missed.
- **The "obsolete global `>60`" reference is real, not a strawman**: confirmed
  `sql/ddl/029_validation_regression_alert.sql:28` literally has `IF total_errors > 60 THEN`, and
  DDL 068's own header explicitly calls this out as "the obsolete hardcoded '>60 total / baseline
  ~22-24' regression heuristic" that it replaces — so the new input's warning against treating that
  retired global value as the new per-check policy is grounded in an actual legacy artifact still
  present in the repo, not a hypothetical.

## Checked — required input shape and new-check onboarding match

The three required rows (DDL 066 effective date + two values for `MISSING`; same for
`STATUS_CONFLICT`; DDL 068 effective date + two values per active `check_name` "after its first
real snapshot is inspected") match both table schemas exactly (`status`/`check_name` keying plus
the two `INT64 NOT NULL` columns) and match the existing onboarding language already present a few
lines above in the same file ("For a newly introduced `check_name`, the first comparison is against
zero: inspect the snapshot before threshold approval... no day-zero history may be silently
seeded").

## Checked — general approval is distinguished from missing values, not overruled

"Boat approved the alert threshold/config approach, but no numeric threshold rows were supplied"
and "General approval does not authorize treating the current backlog, fixture numbers, or the
obsolete global `>60` value as any of these rows" — this correctly separates *process* approval
(already given) from *value* approval (still needed), consistent with the same honest-residual-risk
pattern already used in the post-import IAM input request (RQ-2113) reviewed earlier this session.

## Checked — belongs on the unattended completion checklist

This is exactly the kind of concrete, still-missing human input the
`DAILY_AUTOMATION_COMPLETION_CHECKLIST.md` (reviewed in RQ-2120) already tracks in its "Human and
administrator prerequisites" section — a natural fit, not a new parallel tracking mechanism.

## Checked — no mutation

This commit only edits `docs/INPUTS_NEEDED.md` — no SQL, config, schedule, alert, email, BigQuery,
or SAP state changed. `git diff --check`: clean.

## Verdict

PASS. Every factual claim checks out against the actual DDL and a repo-wide search, the required
input shape matches both the table schemas and the existing onboarding rule, and the wording
correctly distinguishes approved process from missing values without overreaching either way. No
mutation occurred in this review.
