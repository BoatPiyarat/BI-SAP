# Review: RQ-20260822-1212-urgent-refund-cancel-phase2

Reviewer: Claude Code
Class: A
Artifact: commit `bc25b32`; `sql/adhoc/20260822_prepare_urgent_refund_cancel.sql`,
`sql/adhoc/20260822_verify_urgent_refund_payment_precondition.sql`,
`sql/adhoc/20260822_trace_l80545799_payment.sql`
Verdict: **PASS**

## Claim under review
Six plain-cancel items produce an exact gated 47-row complete-spine candidate with no
CareOS-paid/SAP-pending periods and no multi-document winners; hash
`5eef984fcbfba8289d290893d77e5cabd06e5f2abc477150cfbfa35c4a9c4027` for batch date `22082026`. No
persistent object or GCS write occurred.

## Independent verification performed
Re-ran the exact preparation script (temp-table only, no persistent write) with the same literal
scope and batch date. **Reproduced the claimed hash, row count, and item count exactly**:
`5eef984f...c4027`, 47 rows, 6 items — bit-for-bit match against live data one day later, meaning
the underlying SAP/CareOS state for these six items hasn't drifted.

Separately ran one combined query (four metrics, one scan) re-deriving each of the script's own
quarantine gates from scratch rather than trusting its internal ASSERTs: `docs_considered != 1`
(multi-document winner) → 0; scope intersecting `cancelled_change_orders` on either
`old_human_id`/`current_human_id` → 0; CareOS-successful-event-but-SAP-Pending mismatch → 0; CareOS
item not cancelled → 0. All four independently confirm clean. `gsutil ls` against both the
production `RCB_MOTOR` prefix and a `*CANCEL*` wildcard: no matching object, confirming no export
occurred anywhere.

Compared this candidate's field-by-field handling against the full "Aware answers received" list
in `docs/tasks/TASK_URGENT_REFUND_PAID_CANCEL_20260821.md` (11 confirmed rules as of this commit):
InvoiceNo/PaymentDate verbatim-blank on Pending, ActualReceived/ExpectedReceived NULL→0,
ExpectedDate three-level fallback, PaymentMethod/PaymentChannel forced blank on Pending
predecessors (the exact defect the prior BLOCK verdict on `d0229fb` caught), CareOS payment
precondition, mandatory Paid-then-Cancel sequencing, output status always `Cancelled`, and
ascending-Period-within-contiguous-OrderItem ordering (the `ORDER BY OrderItem,Period` in the
script's final `SELECT` matches). Every rule is applied in the script body — none silently skipped.

## 12-item checklist
1. Traceability — PASS. Job IDs, source timestamps, and byte counts named for every claim; I
   reproduced the central hash claim independently rather than trusting it.
2. Provenance over repetition — PASS. Cites the 11 Aware-confirmed rules by date rather than
   re-deriving them, and cites the prior BLOCK verdict's exact defect list implicitly by fixing
   every one of them (forward-fill removed, types not yet addressed — see note below).
3. NULL-safety — PASS. `docs_considered!=1`, `TransactionStatus NOT IN (...)`, and
   `NULLIF(TRIM(...),'')  IS NULL` patterns throughout are NULL-safe; the payment-precondition
   query's `LEFT JOIN ... USING (order_item, period)` correctly treats absent CareOS events as
   zero via `COALESCE(...,0)`, not a silent NULL drop.
4. Ordering — PASS. `ORDER BY sap.order_item, sap.period` in the precondition query and
   `ORDER BY OrderItem, Period` in the final candidate are both integer/string-safe here (Period
   is single-digit-to-two-digit within TotalPeriods≤10 for this exact population — not a general
   solution, but correct for this scope; flagging as a note for any future larger batch).
5. Column order — N/A for this artifact (temp-table preparation only; the final `SELECT` lists all
   56 columns explicitly by name, matching the reviewed physical contract's column set — not
   verified against `INFORMATION_SCHEMA.COLUMNS` ordinal-for-ordinal in this script, since no
   physical table is created from it yet. Worth doing before any real export script is written.
6. Grain stated — PASS. `(OrderItem, Period)` grain is explicit and consistent throughout; the
   payment-precondition trace correctly operates at the same grain, not order-level.
7. Distribution, not row count — PASS. Verdict rests on gate membership (which items pass every
   precondition), not a bare 47-row count; I independently re-derived the same zero-violation
   result via each gate's own predicate.
8. No contradiction with knowledge — PASS. Matches the canonical D1 cancellation definition, the
   RCL/RCB and complete-spine invariants, and does not re-litigate the confirmed decisions list.
9. Scope — PASS. Read-only preparation, no persistent table, no GCS write; explicitly states
   "production export is a separate reviewed step."
10. Rollback — N/A, nothing deployed.
11. Cost hygiene — PASS. Dry-run 0 bytes for the multi-statement temp-table script (confirmed
    independently); the payment-precondition and trace queries are separately dry-run'd per their
    own commit evidence.
12. Honest labelling — PASS. States exactly what was and wasn't done ("No persistent table or GCS
    object was written," "Production export remains gated on Class-A review... followed by a
    [fresh sync]") — nothing rounded up to ready-to-ship.

## Risk / gap noted
Non-blocking, for whoever builds the real export step next: (a) this script has no
`INFORMATION_SCHEMA.COLUMNS` ordinal/type equality assertion against the live physical interface
contract — the prior BLOCK on `d0229fb` flagged an all-STRING type mismatch; this script's
temp-table output has no declared schema to check against, so that risk moves downstream to
whatever persists it. (b) The candidate hash is still batch-date-dependent
(`v_batch_date` embedded in `BatchRunDate`), so a same-day rebuild is stable but a rebuild on a
different day will not reproduce this exact hash — expected and fine for a temporary preparation
artifact, but the eventual immutable export binding needs to persist the exact rows, not just the
hash, before that becomes a problem (same issue noted on the prior BLOCK, not yet re-encountered
here because nothing is persisted).

Checklist 1–12 reviewed; two non-blocking gaps noted above for the next step, not this artifact.
