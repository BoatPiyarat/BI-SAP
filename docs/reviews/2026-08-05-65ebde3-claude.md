# Review — RQ-20260805-2058-canonical-workflow-execution-name

Reviewer: Claude Code
Artifact: commit `65ebde3`; `sql/ddl/072_v3_post_import_refresh_dispatch.sql`;
`infra/v3_nightly_orchestrator.workflows.yaml`.
Verdict: **PASS — resolves the critical finding from RQ-20260805-2044**

## Checked against the request's own checklist

- **Exact canonical construction**: `workflow_execution_name` now uses
  `sys.get_env("GOOGLE_CLOUD_PROJECT_NUMBER")` — a distinct, unambiguous environment variable
  (rather than the confusingly-named `GOOGLE_CLOUD_PROJECT_ID`, which was the source of my
  original concern). This directly resolves the root cause rather than papering over it.
- **Regex acceptance/rejection boundaries — independently tested with real BigQuery queries, not
  just read**: ran `REGEXP_CONTAINS` against five cases myself:
  - `projects/919786098205/...` (the real project number, live-verified in the prior review) →
    **matches**.
  - `projects/pacific-plating-282708/...` (project-ID form) → **matches** (both forms still
    accepted, per the claim of not weakening the existing structure).
  - `projects/12345/...` (5 digits, below the 6-digit floor) → **correctly rejected**.
  - A 21-digit numeric segment (above the 20-digit ceiling) → **correctly rejected**.
  - An empty project segment → **correctly rejected**.
  All five results match the intended boundary exactly.
- **Compatibility with the administrator runbook's execution-scoped IAM condition**: the runbook
  (reviewed in RQ-2044) builds its condition prefix from `$projectNumber = '919786098205'` — the
  same value this delta's `GOOGLE_CLOUD_PROJECT_NUMBER` construction will produce at runtime. The
  two are now consistent: the constructed execution name will both satisfy DDL 072's own format
  assert and match the IAM condition's resource prefix.
- **Neither DDL nor workflow was deployed or executed by this corrective source commit**:
  confirmed — this is a source-only two-file text change. Independently re-ran the mandatory
  dry-run on the corrected DDL 072: 0 bytes. `git diff --check 65ebde3^ 65ebde3`: clean.
- **No weakening of the workflow/location/name/execution structure or delivery state**: the
  `/locations/[a-z0-9-]+/workflows/[A-Za-z0-9_-]+/executions/[A-Za-z0-9_-]+$` suffix of the regex
  is completely unchanged; only the project segment's alternation was added. `delivery_enabled`
  and every other line in the workflow YAML outside this one variable assignment is untouched.

## Verdict

PASS. This closes the critical gap identified in `docs/reviews/2026-08-05-fc048d9-claude.md`:
the workflow now self-constructs its execution name using the correct, unambiguous project-number
variable, and DDL 072's format assertion accepts that real-world format without weakening its
rejection of malformed input. Independently verified the regex's exact accept/reject boundaries
with live BigQuery test queries rather than reasoning about the pattern alone. No deploy, CALL, or
production action occurred in this review — **the admin completion runbook and synthetic
rehearsal (RQ-2044, RQ-1956) may now proceed** as far as this specific blocker is concerned.
