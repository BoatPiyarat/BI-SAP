# Review: RQ-20260824-1319 installment InvoiceNo NULL-safety

Reviewer: Codex  
Artifact: `a3d0305` (`sql/production/sap_dashboard_carepay_installment.sql`)  
Verdict: **BLOCK**

## Standards

1. **BLOCK — validation evidence is incomplete.** The two COALESCE changes are logically
   NULL-safe, but the artifact has no dry-run or before/after execution of the changed definition.
   `AGENT_RULES.md` requires query changes to carry validation evidence; protocol checklist 7/11
   requires distribution and cost evidence. My one permitted live query
   (`bqjob_r495c418663e16e8e_000001a0326f649e_1`, 2026-08-24, dry-run 787,334,840 bytes) confirms
   the current symptom on `sap_data_engineer.sap_dashboard_carepay_installment`: paid period 1 of
   `L77833033-V1` retains `2_chrg_...`, while pending periods 2–6 have SQL NULL InvoiceNo. It does
   not prove the undeployed replacement's output. Author must dry-run the exact changed definition
   and provide a targeted before/after result proving pending becomes `''`, paid values and row
   grain remain unchanged, and no status distribution regresses.
2. **BLOCK — non-surgical formatting churn.** Revert whitespace-only edits in the adjacent
   `InsuredID` and `InsuranceProduct` CASE blocks. `AGENT_RULES.md` “Surgical Changes” prohibits
   unrelated reformatting.
3. **Deployment boundary:** the queue discloses that the live view has an additional outer
   `SELECT * REPLACE` wrapper absent from this baseline file. Any eventual deploy must patch the
   freshly captured live definition and preserve that wrapper; deploying this file wholesale is
   not approved.

No actionable Fowler smell found. The duplicated COALESCE shape is intentional at two existing
normalization boundaries; extraction would enlarge this surgical fix.

## Spec

**PASS.** Both changed predicates correctly resolve BigQuery three-valued logic: NULL status now
makes the blank-InvoiceNo branch true. SUCCESSFUL rows remain unchanged, including the earlier
SUCCESSFUL + NULL `third_party_id` branch. A present nonblank identifier is not erased. No
functional requirement is missing and no semantic scope creep was found. The period-1 precedence
risk is not reachable through a non-successful joined charge because the `charges` CTE itself
contains only `status='SUCCESSFUL'`; a no-charge period has NULL `installment_number`.

## Checklist 1–12

1. **Traceability — PASS:** the review query names object, job, date, item, and periods.
2. **Provenance — PASS:** commit and 06-Aug source history are cited without inventing counts.
3. **NULL-safety — PASS for changed predicates:** `COALESCE(...,'')` is correct.
4. **Ordering — N/A:** no ordering/date change.
5. **Column order — PASS:** expressions changed in place; ordinals unchanged. Live wrapper must be preserved.
6. **Grain — PASS in source:** no join or row-grain change; validation still required.
7. **Distribution — BLOCK:** no changed-output distribution or row-parity evidence.
8. **Knowledge consistency — PASS:** blank pending InvoiceNo matches the confirmed contract.
9. **Scope — BLOCK:** unrelated whitespace formatting violates surgical scope.
10. **Rollback — N/A:** source only, not deployed; future rollback must retain the live definition.
11. **Cost hygiene — BLOCK:** author supplied no dry-run; review query was safely capped.
12. **Honest labelling — PASS:** source-only/unverified/not-deployed status is explicit.

Required correction: revert unrelated formatting, validate the exact changed definition with
dry-run plus targeted before/after distribution/grain evidence, and resubmit once. Do not deploy
the repository baseline over the drifted live wrapper.
