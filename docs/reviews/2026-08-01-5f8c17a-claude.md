# Review — 5f8c17a (042 NOTE 1/2 delta)

**Queue entry:** RQ-20260801-1510-042-note1-note2-delta · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — both notes closed exactly as specified in `2026-08-01-2568eea-claude.md`.

- **NOTE 1 (NOT_FOUND contradiction guard):** the added ASSERT is the proposed SQL verbatim —
  `p_decision != 'NOT_FOUND' OR (p_sap_doc_entry IS NULL AND (p_je_reference IS NULL OR
  TRIM(p_je_reference) = ''))`. A "not found, but here's its DocEntry" record can no longer insert.
  NULL-condition ASSERT semantics fail closed, consistent with the rest of the procedure.
- **NOTE 2 (rejected-path status symmetry):** `p_sap_status IS NULL OR TRIM(p_sap_status) = ''`
  added to the REJECTED_NEVER_POSTED conjunction; error message updated to match. A never-posted
  record can no longer carry a live SAP status.
- INCONCLUSIVE remains the one decision allowed to carry partial evidence, now documented in the
  header per the claim — correct: it is the honest bucket, and constraining it would push
  ambiguous cases into fabricated certainty.
- Delta scope check: the diff touches only these assertions + comments; the evidence-chain
  contracts for POSTED_* and the uniqueness guard are unchanged from the PASS'd base.

042's schema-only deploy is now review-unblocked (NOTES 3/4 — uniqueness-race scheduled audit and
the latest-wins/current-view rule — remain non-blocking follow-ups and do not gate schema
creation). Deployment still requires Boat's separate approval; no CALL, no backfill.

Queries used: 0.
