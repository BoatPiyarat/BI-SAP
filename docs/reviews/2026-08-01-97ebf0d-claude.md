# Review — 97ebf0d (Phase B 56-column contract coverage)

**Queue entry:** RQ-20260801-2137-phaseb-contract-coverage · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS** — the numbers are internally locked, the corrected query is structurally
fan-out-proof, and blocking the naive shadow DDL is the right call on this evidence.

## Arithmetic — three independent identities all close exactly

- 82,824 + 193,836 = 276,660 covered (overlap 0) and 276,660 + 13,659 = **290,319** ✓
- By-flow totals 95,489 + 189,219 + 5,611 = **290,319** ✓; uncovered 12,665 + 986 + 8 = **13,659** ✓
- 290,319 matches the post-recovery `expected_state` count from the same evening's manual-sync
  evidence, and 15 columns matches the E1-era output list exactly (13 base + clamped/rescued +
  insured_id + computed_at) ✓

## Corrected query — reviewed statically (re-running 9 GB adds nothing the identities don't already lock)

The fix is structural, not cosmetic: both source views are **pre-aggregated to one row per
`(order_item, period)`** before the LEFT JOINs, so the joined CTE provably stays at
`expected_state` grain (which chain-① verification showed is key-unique); `overall` and `by_flow`
are independent aggregates and the final row is `overall` × a scalar `ARRAY_AGG` subquery — exactly
one output row, no join-back. The retracted job is handled by the book: named, reason stated
(per-flow summary joined back to detail by `flow`), same bytes recorded, zero numbers cited.
`SAFE_CAST(Period)` + `JOIN USING` means NULL-period source rows can never cover anything —
coverage-conservative, the right direction for a gate.

## Interpretation

- The decision — no `expected_interface_state` until the 13,659 are classified and the 1,647
  duplicate source keys get a deterministic winner — follows directly; a naive join would silently
  drop 4.7% of V3 rows and fan out 1,647 keys, both invisible in a row-count check.
- The flow split is coherent with known history: ONETIME 12,665/95,489 (13.3%) uncovered lands
  squarely in the onetime-stream territory (`sap_dashboard_carepay_fully_paid`, the D15
  `charge_rank` fan-out family), vs RCL at 0.5%; the 322/1,325 duplicate-key counts are the same
  disease family as the credit-shell/onetime duplicate keys already on record.

## Notes for the next gate (direction, not conditions)

1. The 1,647 duplicate-key winner should reuse the **established deterministic pattern**
   (recency, then content hash) rather than invent a third convention — and mind the 22 type-drift
   positions when comparing payloads across these two views.
2. Classifying the 13,659: cross-reference, but do not conflate, the D1/D2 `sap_view` membership
   gaps (902/326) — different view pair (dashboard contract views vs process views), possibly
   overlapping causes.

Queries used: 0.
