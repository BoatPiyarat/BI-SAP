# Review — 047 nightly repoint (`849aa0e`)

**Queue entry:** RQ-20260801-1917-047-nightly-incremental-repoint · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **BLOCK** — one concrete, deploy-stopping omission, found by diffing against the
**live** routine body (per the legacy-drift governance rule: repo representations are not live truth).

## BLOCK — the live nightly body has an 11th call that 047 drops

Live `sp_nightly_state_and_recon_refresh` (fetched via routine metadata, 2026-08-01 ~19:2x ICT)
ends with:

```sql
  CALL `...sap_integration_v3.sp_refresh_delta_export`();
  CALL `...sap_integration_v3.sp_refresh_interface_daily_status`();   -- ← 11th call
END
```

047's new definition **and** its rollback block both stop at `sp_refresh_delta_export()` — 10
calls. Deploying as-is silently removes the nightly `interface_daily_status` refresh (030's
surface, which the 046 morning report's operators sit downstream of), and the rollback would not
restore it either — the omission would survive a rollback, which is the worst failure shape for a
cutover script. Root cause is presumably that 047 was authored from the repo's 026-era chain
description, which predates 030's addition — exactly the drift mode the governance rule exists for.

**Fix (one line, twice):** append
`CALL `pacific-plating-282708.sap_integration_v3.sp_refresh_interface_daily_status`();`
as the final call in **both** the new definition and the rollback block, then re-request. Also
regenerate the rollback block from the live definition verbatim (it should be byte-equal to live,
not reconstructed).

## Everything else checks out (no other findings)

- One-call delta otherwise exact: `sp_refresh_sap_mirror_doc('NIGHTLY:scheduled')` →
  `sp_refresh_sap_mirror_doc_incremental('NIGHTLY:scheduled')`, same argument, same position; the
  other nine calls match the live body in content and order.
- Rollback is otherwise symmetric (restores only the 024 call; scheduler/tables/watermark untouched).
- Preconditions correctly bind to RQ-1637 + RQ-1640 (both now PASS) and Boat's explicit
  authorization.
- The 0-byte file dry-run is fine as far as it goes, but for a body made of CALLs the only
  meaningful acceptance evidence is exactly this live-body diff — which is what caught the gap.

Queries used: 1 routine-metadata fetch (0 bytes billed).
