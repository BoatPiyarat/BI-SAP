# Review — RQ-20260810-1918-period-cutoff-calendar-source

Reviewer: Codex  
Artifact: commit `a82409d`; `sql/ddl/075_v3_period_cutoff_calendar.sql`.  
Verdict: **BLOCK**

## Finding

The procedure implements the requested before/at/after boundary, current calendar/state equality,
missing-next fail-closed behavior, and correct delegation order to `sp_close_open_period`.
Registration is logically insert-only. But the claimed invariant “one immutable row per
period_start” is not enforced under concurrency: the table has no declared key and the
count-then-insert registration is not atomic, so two concurrent calls can both pass and insert.
The governing finding also requires Finance confirmation of the registry/correction policy before
implementation/deployment; that confirmation is still absent.

No executable duplicate, boundary, missing/mismatch, adjacency, timezone, or rerun tests were
provided. The mandatory safe-wrapper dry-run was attempted on 2026-08-11 and failed with
`ReauthUnattendedError` after the wrapper self-test passed 7/7.

## Checklist 1–12

1. Traceability: PASS for source claims; runtime validation is absent.
2. Provenance: PASS.
3. NULL-safety: PASS — required values fail closed through assertions.
4. Ordering: PASS in source; boundary/timezone tests remain missing.
5. Column order: N/A.
6. Grain: BLOCK — monthly grain is stated but not concurrency-safe.
7. Distribution: N/A.
8. Knowledge consistency: BLOCK — Finance correction-policy gate remains unresolved.
9. Scope: PASS — V3 source only.
10. Rollback: N/A — source only.
11. Cost hygiene: BLOCK — no successful live dry-run.
12. Honest labelling: PASS — limitations were disclosed.

Required correction: obtain the Finance policy decision; enforce or explicitly prove safe
single-writer uniqueness; add executable tests covering the finding's gate matrix; obtain a clean
safe-wrapper dry-run; then request one delta review.

