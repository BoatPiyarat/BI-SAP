# Session 2026-08-01 — Claude Code (reviewer + deploy lane, own clone `C:/dev/BI-SAP-review`)

## Process note

Worked entirely from this clone against committed origin refs, per Boat's 2026-07-31 Rule 0
correction. Recorded plainly: before that correction landed, review commit `656a40a` (RQ-2323) was
made directly in Codex's OneDrive working tree — no damage (Codex's `17cc27a` followed cleanly and
the branch was pushed), but it is the incident the rule now prevents. Nothing in that tree has been
touched since.

## Reviews completed this session (5 — queue debt now 0 OPEN for Claude Code)

| RQ | Artifact | Verdict | Review file |
|---|---|---|---|
| RQ-20260731-2201 | `2c96c53` (024/025/037/044) | PASS WITH NOTES | `2026-08-01-2c96c53-claude.md` |
| RQ-20260730-2230 | `043` (`9a3e462`+`2c96c53`) | **BLOCK** | `2026-08-01-043-merge-claude.md` |
| RQ-20260731-2139 | `1a52222` | PASS | `2026-08-01-1a52222-claude.md` |
| RQ-20260731-2225 | `f74f605` | PASS | `2026-08-01-f74f605-claude.md` |
| RQ-20260730-2200 | `93e87ea` | PASS WITH NOTES (self-review caveat) | `2026-08-01-93e87ea-claude.md` |

Key verification methods, all zero billed bytes: mechanical extraction/diff of all four 024 UNION
branch select lists (59 aliases, byte-identical order, UpdateDate/UpdateTime at positions 58/59);
`bq show --schema` on all four SAP_LIVE shards (UpdateDate=TIMESTAMP, UpdateTime=INTEGER); 043
INSERT/UPDATE lists diffed against 024's aliases (59/59, 58/58); static blast-radius read of all 8
mirror consumers plus all `expected_state` consumers (all named-column, no positional dependence);
one standalone 0-byte dry-run reproducing 043's analytic-in-aggregate error; offline re-run of
`bq_safe_query.sh --self-test` (7/7).

## BLOCK summary (043) — the deploy-relevant outcome

Two CONFIRMED CALL-time failures that the 0-byte dry-run structurally cannot catch (procedure
bodies late-bind): (1) watermark-advance `SET` nests an analytic inside an aggregate — BigQuery
rejects it at execution, *after* the MERGE has already mutated the mirror, leaving the watermark
permanently unadvanced; (2) delta branches carry raw TIMESTAMP `UpdateDate` into the post-024 DATE
mirror column. Concrete fixes proposed in the review file; handed to Codex via `HANDOFF_QUEUE.md`.

## Deploy status

Nothing deployed, nothing CALLed. 024/025/037/044 are review-passed and deploy-eligible **only** on
Boat's explicit per-item approval, in order: 044 → Boat inserts period row → 024 → refresh → 025 →
037. 043 must not deploy until its BLOCK clears and a fresh dry-run + fixed-file review pass.

Review debt after batch 1: 0 OPEN (mine: 0).

## Batch 2 — the 8 overnight Codex requests (all Class A, priority-ordered per Boat)

| RQ | Artifact | Verdict |
|---|---|---|
| RQ-2243 | `aba1aad` RULE-09 037 | PASS WITH NOTES — period-lock guard spec issued (lock_datetime never read) |
| RQ-2255 | `1f3e14a` export path + runbook | PASS |
| RQ-2310 | `b10a22f` RULE-10 D1/D2 | PASS — FA wording note on the 902/326 gaps |
| RQ-2330 | `401cc98` 22/56 type drift | PASS — UAT2 known-answer compare noted |
| RQ-2345 | `b00af18` R1 un-retraction | PASS — CURRENT_STATE §4 R1 row needs annotation (Codex lane) |
| RQ-2355 | `2647350` loader guards | PASS WITH NOTES — Guard 1 threshold breaks after chunking fix |
| RQ-0000 | `c6b7b8b` import-log S1 | PASS |
| RQ-0040 | `25fb58b` legacy drift | PASS — independently reproduced |

Boat's parallel task done: live `INFORMATION_SCHEMA.VIEWS` reproduction classified all 12 interface
producers — **9 MATCH, 2 DRIFT (`RCL_Motor_process_1_create`, `RCL_NonMotor_process_2_newpayment`),
1 NO_BASELINE (`RCL_Motor_process_2_newpayment`)**, plus the `sql/production` copy of
`RCB_NonMotor_process_1_create` drifting — identical to Codex's 6-drift finding, and the 3
non-MATCH views are exactly the (ก)/(ง)-relevant CREATE/NEWPAYMENT views. Cost: one wrapper-gated
query (dry-run 10,485,760 bytes) + one JSON re-fetch of the same result (~10 MiB each); everything
else in batch 2 used zero queries.

Review debt after batch 2: 0 OPEN (mine: 0) at time of review — Codex's fix commits arrived while
batch 2 was pushing; reviewed immediately as batch 3.

## Batch 3 — Codex's responses to the BLOCK and the guard spec (both same-night)

| RQ | Artifact | Verdict |
|---|---|---|
| RQ-0153 | `6ef690b` 043 watermark/type fixes | **PASS — RQ-2230 BLOCK cleared in one round** (selector re-verified by 0-byte dry-run; DATE chain complete in all 4 branches) |
| RQ-0156 | `b881fa3` 037 active-period guard | PASS (all 3 failure modes fail closed; exactly-one ASSERT stricter than my LIMIT-1 spec on the overlap case — the better choice) |

## Deploy-eligibility snapshot for Boat (nothing deployed; every step needs per-item approval)

- **RULE-01/02/08/09 chain — review-complete**: 044 → Boat inserts July period row → 037
  (`b881fa3`) → `CALL sp_refresh_expected_state()` → runbook step-9 verification. Runbook:
  `docs/design/RULE09_DEPLOY_RUNBOOK.md` (reviewed PASS).
- **RULE-03 mirror chain — review-complete**: 024 → refresh → 025 (strict validation after 024) →
  037 re-check, per `2026-08-01-2c96c53-claude.md`.
- **043 incremental MERGE — review-complete but sequenced last**: bootstrap + row-for-row diff vs
  fresh 024 rebuild before repointing the nightly chain.
- Still with Codex (non-blocking): CURRENT_STATE §4 R1 un-retraction annotation; SECURITY_FINDING
  reconciliation addendum (HANDOFF item 3); Guard-1 chunk-count threshold; stale 018/030 comments.

## Deploy release (Boat-approved, 2026-08-01) — execution log

**Runbook step 1 — pre-change capture (evidence):**
- Counts (wrapper-gated job, dry-run 35,162,579 bytes, 2026-08-01 ~11:24 ICT):
  `expected_state` = 298,278 · `sap_mirror_doc` = 1,658,776 rows = 1,658,776 distinct DocEntry
  (matches G1 exactly) · `sap_mirror_state` = 1,296,900 · excluded: OLD_YEAR_NO_TOUCH 780,108,
  NO_NEW_PAID_2025 387,899, INSURER_NOT_IN_MASTER 3,172, TEST_CUSTOMER_NAME 347,
  TEST_CUSTOMER_PHONE 341, CANCEL_2025_NOT_IN_SAP 45. Note: **6** rule_code values exist, not the
  runbook's "seven" — POLICYNO_TOO_LONG (F2) writes to `sap_validation_error`, not the register.
- Deployed routine bodies + SHA256 snapshotted pre-change (scratchpad `pre_routines_full.json`):
  `sp_refresh_expected_state` dd202ac3…, `sp_refresh_sap_mirror_doc` f4a411f2…,
  `sp_refresh_sap_mirror_state` 3cb36130….

**Chain ① step 1 (044): table present and verified — with a flagged deviation.**
`sap_period_lock` already existed when my deploy ran: created 2026-08-01 11:25:15 ICT by job
`deploy_044_sap_period_lock_20260801_112510` (user `data@rabbit.co.th`, custom job-ID naming =
the other agent's deploy convention). My job `bqjob_r5492f424612122b8_0000019fbb92b946_1`
(11:26:23 ICT) correctly no-op'd via IF NOT EXISTS ("Skipped"). Verified: schema matches 044
exactly (period, open_period_start, lock_datetime, locked_by, locked_at), 0 rows,
asia-southeast1. Outcome is correct regardless of author; evidence preserved for both jobs.

**Chain ① step 2: STOPPED as instructed — waiting for Boat's July row.**

**🔴 COLLISION FLAG — chains ②③ NOT started.** A second actor is executing the same deploy
release concurrently (evidence above). JOBS_BY_PROJECT since 09:00 ICT shows exactly two
sap_integration_v3 jobs: the parallel `deploy_044_*` and my skipped CREATE — **no
CREATE_PROCEDURE and no CALL by anyone yet**, so nothing has raced and nothing is damaged. But
chain ② onward is CREATE OR REPLACE + full CTAS refreshes: two actors running them concurrently
means last-writer-wins procedure definitions, doubled full-scan cost, and unattributable
verification numbers. Per the release's own rule ("flag immediately if anything deviates — do not
proceed"), chains ② and ③ are held until Boat designates a single deployer. No repo commit from
the other agent records its 044 deploy at flag time.

## Chain ① resumption (Boat priority) — blocked at step 5 by credential reauth

RQ-1200 (E1–E3/F1–F3) closed PASS WITH NOTES (commit `9a23413`); Boat authorized chain ① to
proceed to runbook step 6 after PASS. Execution stopped at the **step-5 verification query**:

- 13:20–13:21 ICT, every `bq` invocation fails with
  `google_reauth.errors.ReauthUnattendedError: Reauthentication challenge could not be answered
  because you are not in an interactive session` (credential `data@rabbit.co.th`; bq's "CLI bug"
  banner is just its wrapper around this error).
- Per the project's own 401-lesson taxonomy this is **authentication, not IAM** — Google's
  session-length reauth policy expired the credential between ~11:28 ICT (last successful call)
  and 13:20 ICT.
- **Human action required on this machine:** interactive `gcloud auth login` (and/or
  `gcloud auth application-default login`) for `data@rabbit.co.th`. No agent can answer the
  challenge non-interactively; nothing was retried in a loop.
- Nothing was deployed in this window. 037 remains at reviewed `1a289cd`, undeployed. Resume point
  is exactly runbook step 5 (verify exactly-one active July period row), then step 6.

## Batch 5 — RQ-1117 (D16 002a split, `cb4cb29`) PASS · RQ-1135 (UpdateDate comparison, `7402acf`) PASS

Both verdicts in the queue; review files `2026-08-01-cb4cb29-claude.md` / `2026-08-01-7402acf-claude.md`.

## PINNED — review criteria for the upcoming E1–E3/F1–F3 implementation (Boat, 2026-08-01)

Boat pre-briefed the highest-risk item before Codex's request lands. Criteria to apply verbatim:
1. **E1 tiered year logic is not a single floor date**: ≤2024 untouched · 2025 cancel-only ·
   ≥2026 normal with `GREATEST(OrderDate, PolicyDate)` as the *processing* date basis.
2. **Tier classification uses `OrderDate` alone** — a separate concern from the GREATEST()
   processing basis for 2026+. Do not let the implementation conflate the two.
   ⚠️ Pre-identified discrepancy to check first: the *currently deployed* 037 derives
   `basis_year` from `date_basis = GREATEST(order_create_time, policy_start_date)` — i.e., today's
   code already classifies tiers by GREATEST, not OrderDate. If the new implementation keeps that,
   it fails criterion 2; if it changes to OrderDate, tier membership of some records will shift —
   demand a before/after tier-count delta as review evidence.
3. **2025 tier requires BOTH conditions** (already in SAP **AND** `is_cancelled_effective=TRUE`).
   Named failure mode: filtering on cancellation alone would wrongly create new 2025 interface
   files. (Current 037's two-predicate pair implements BOTH — the new code must preserve that.)
4. **Every exclusion path writes `sap_excluded_records` with the correct rule_code** — no silent
   drops. ⚠️ Boat's message names `YEAR_OUT_OF_SCOPE` / `YEAR_2025_NON_CANCEL_EXCLUDED` /
   `TEST_CUSTOMER` / `INSURER_NOT_IN_MASTER`; the live register currently uses
   `OLD_YEAR_NO_TOUCH` / `NO_NEW_PAID_2025` / `CANCEL_2025_NOT_IN_SAP` / `TEST_CUSTOMER_NAME` /
   `TEST_CUSTOMER_PHONE` / `INSURER_NOT_IN_MASTER` (pre-change capture, this session). If Codex
   renames codes, existing monitors/counts break silently — the review must reconcile the taxonomy
   explicitly, not accept either set on faith.
5. No deploy without PASS verdict + separate Boat approval, as always.

## Batch 4 — RQ-20260801-0859 (042 FA verification contract, `2568eea`)

**PASS WITH NOTES** — `docs/reviews/2026-08-01-2568eea-claude.md`. Grain confirmed sufficient for
all known consumers; posted/rejected evidence chains bypass-free (ASSERT NULL-semantics fail
closed); append-only verified repo-wide (zero mutation statements reference the table); dry-run
**independently reproduced** (0-byte lower bound, asia-southeast1) and the procedure body manually
re-checked per the 043 late-binding lesson; PII scan clean. Reviewer-caught gap: NOT_FOUND accepts
a contradictory non-NULL DocEntry/JE — one added ASSERT requested pre-deploy (NOTE 1), plus
sap_status symmetry on the rejected path, uniqueness-race backstop, and a latest-wins/current-view
rule before FA reads the table directly. No deploy; Boat approval remains a separate gate.
