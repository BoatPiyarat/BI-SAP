# Review: RQ-20260825-1536 installment-invoiceno-deploy-evidence

Reviewer: Claude Code
Artifact: `5949a3d`; deployment evidence for PASSed exact-live source `ba1ca7d`
Verdict: **PASS**

## Verification performed

Per protocol, metadata-only reads (`bq show -j`, `bq head`, `INFORMATION_SCHEMA.VIEWS`/`COLUMNS`)
plus local hashing — not counted against the one-query budget.

- **Deploy job confirmed exactly.** `bqjob_r7f485d3f96160b3b_000001a0380ddd6a_1`:
  `statementType=CREATE_VIEW`, `totalBytesProcessed=0`/`totalBytesBilled=0`, `state=DONE`, no
  `errorResult`, `creationTime=1787646827379` → **2026-08-25 08:33:47 UTC** — matches the claimed
  timestamp exactly. Query text begins with the same header comment as the reviewed
  `ba1ca7d` candidate.
- **Live definition hash: exact match, and the newline explanation independently confirmed.**
  `INFORMATION_SCHEMA.VIEWS` now returns `view_definition_sha256 =
  2861ea0cd3904c455f544a2f450e9cca377fc5eed43429b2d014905e8bf30f0b`, length 31,373 — matches the
  claimed stored hash exactly. I took my own previously-hashed candidate body (from the prior
  review, hash `148813fb...`) and stripped exactly its one trailing newline, then rehashed:
  **`2861ea0c...` — identical.** This independently proves the terminal-newline explanation is
  correct and that nothing else changed between the reviewed candidate and the deployed view.
- **Post-check job confirmed exactly.** `bqjob_r2c00ef37d4d5120a_000001a0380f2bb2_1`:
  `totalBytesProcessed=776,974,222` (matches claim exactly), `creationTime` → 2026-08-25 08:35:12
  UTC. Fetched its actual result: `total_rows=997734`, `distinct_orderitems=190721`,
  `non_paid_rows=453802`, `baseline_invoiceno_null_count=1`,
  `candidate_invoiceno_null_count=1`, `invoiceno_blank_count=453802` for both baseline and
  candidate, `changed_non_paid_null_to_blank=0`, `paid_invoiceno_changed=0`,
  `residual_paid_invoiceno_null_count=1`, both scoped columns 0 — every figure matches the claim
  exactly. (Baseline/candidate now agreeing and showing 0 further deltas is expected and correct:
  both sides of this post-deploy check now read the same live, already-fixed view.)
- **Schema confirmed live**: `INFORMATION_SCHEMA.COLUMNS` shows exactly 56 columns, with
  `CompanyDB`/`OrderID`/`OrderItem`/`InvoiceNo`/`OrderDate` at ordinal positions 1–5 — matches the
  claim.
- **Scope**: the hash-equality proof above is strictly stronger than a fresh predicate/column grep
  — since the deployed view is byte-for-byte the already-reviewed candidate (modulo one newline),
  every fact already verified in `ba1ca7d`'s PASS review (exactly one voluntary fix, one
  transformation fix, one untouched compulsory predicate, no other statement in the file) carries
  over unchanged. No need to re-derive it.

## Checklist 1–12

1 traceability PASS (every job ID, timestamp, and byte count independently reproduced from live
objects, exact matches); 2 provenance PASS (correctly references the prior PASS on `ba1ca7d` rather
than re-deriving it); 3 NULL-safety N/A (already established in the source review); 4 ordering N/A;
5 column order PASS (56 columns, ordinal 1–5 confirmed live); 6 grain N/A; 7 distribution PASS
(post-check row/item counts independently reproduced exactly, no row-count-as-correctness
overclaim — the actual claim is "0 further deltas," which the byte-identical hash makes provably
true rather than merely observed); 8 knowledge PASS (consistent with the narrowly-scoped
`AGENT_RULES.md` exception and the prior PASS); 9 scope PASS (deploy job's own query targets only
this one view, confirmed via the hash match to the single-statement reviewed file); 10 rollback
PASS (the pre-deploy live definition was already hashed and verified in the prior review — fully
recoverable); 11 cost hygiene PASS (both cited jobs' byte counts verified exactly: 0 bytes deploy,
776,974,222 bytes post-check); 12 honest labelling PASS ("No other production object, GCS, SAP, or
scheduler action" is consistent with the deploy job containing only the one `CREATE_VIEW`
statement).

Summary: Checklist 1–12 reviewed; no gap found. Every cited hash, timestamp, byte count, and
post-check figure was independently reproduced from live objects and matched exactly, including a
direct reproduction of the claimed terminal-newline hash discrepancy. Deployment complete and
correctly scoped to the one authorized view.
