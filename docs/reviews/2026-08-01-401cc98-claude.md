# Review — 401cc98 (22/56 interface type drift + G1/G2 ground-truth limits)

**Queue entry:** RQ-20260731-2330-interface-type-drift-ground-truth · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked: (1) position arithmetic verified — (22–34, 39–40)=15 STRING↔FLOAT64, (41–44, 48)=5
INT64↔FLOAT64, (53–54)=2 three-way = 22 total, all money/quantity fields as claimed, and all inside
the 56-column contract enumerated in `1f3e14a`. (2) The claim is correctly scoped as a **CSV
serialization risk**, not proof of a bad imported file — the finding itself supplies the reason
(no physical CSV retained, serializer unknown). (3) 028's name/position-only design is cited with
its original rationale, and the proposed remedy is deferred post-03/08 and WARN-only until each
accepted coercion has an explicit contract — proportionate, no production change. (4) Ground-truth
limits are the strongest part: three-prefix bucket listing (no fourth BU folder — "must not be
invented"), expired `sourceUploadUrl` + HTTP 403, no serialization marker in logs → deployed
serialization method declared **unknown**, and legacy-writer↔`EXPORT DATA` parity explicitly kept
as a blocker for RULE-10's manual export. This interlocks correctly with `b10a22f`'s "export SQL
prohibited until the physical-file contract gate clears".

Note: when the July manual CSV is produced (RULE-10), the 22 drifting money positions are exactly
where a wrong rendering would be invisible to guard 028 — the UAT2 dry-run (G3) should include a
field-level compare of at least one known-answer row per BU against SAP's accepted format, not just
import success.

Queries used: 0.
