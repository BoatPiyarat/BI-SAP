# Review — RQ-20260815-1323-daily-recon-mtd-report-delta

Reviewer: Codex
Fixed point: `9376cc1`
Artifact: commit `25e6fa0`; daily recon MTD Apps Script, contract tests, and deployment template
Verdict: **BLOCK**

## Standards

The corrective source substantially closes the earlier implementation defects: it uses explicit
Bangkok month-start and current-time query parameters, routes report construction failures to the
fallback channel, polls asynchronous BigQuery jobs, consumes page tokens, rejects unknown statuses,
normalizes recipient comparison, adds a freshness gate, and describes the Apps Script trigger as an
execution window.

The Class-A gate nevertheless remains blocked:

1. The committed contract test is time-dependent. `buildReconMtdReport_()` uses the real clock, while
   the freshness fixture is fixed at 2026-08-15 06:00 UTC. Executing it through the available Node
   runtime on 2026-08-15 produced `3.7h ago`, so the first expected `1.0h ago` assertion failed; the
   claimed 26-assertion pass was not reproduced. Freeze/inject the clock and rerun the full suite.
2. Neither exact parameterized production query has the mandatory successful safe-wrapper dry-run
   evidence. The query is partition-prunable and capped at 20 GiB, but checklist 11 remains open.
3. The deployment document remains a template containing `<APPS_SCRIPT_PROJECT_ID>` and other
   placeholders. There is no verified Apps Script project/manifest, deployed source hash, captured
   trigger ID, or exercised rollback evidence. Acceptance rehearsal and installation remain barred.
4. `git diff --check 9376cc1...25e6fa0` reports trailing whitespace and an extra EOF blank in the
   prior review Markdown added by this commit. This is a formatting defect, not the deployment block.

No actionable Fowler smell or new secret/security issue was found.

## Spec

1. The freshness query applies the current-month `first_paid_time` filter before
   `MAX(recon_checked_at)`. A freshly rebuilt table with no rows yet in the Bangkok calendar month
   therefore returns no freshness evidence and fails closed instead of sending the required valid
   zero-count MTD report. Separate table-refresh evidence from the current-month report population,
   while retaining a cost-bounded/prunable query, and add an empty-month acceptance test.
2. Recipient equality is normalized, but the configuration returns the original untrimmed strings.
   A whitespace-padded address can pass validation and then be handed unchanged to `MailApp`.
   Return the trimmed addresses (preserving or normalizing case as appropriate) and test delivery.
3. The exact live dry-run, concrete deployment target, acceptance rehearsal, trigger capture, and
   rollback evidence explicitly remain outside this delta, so the requested terminal sequence cannot
   advance past review.

## Verification

- Pinned delta inspected: `git diff 9376cc1...25e6fa0`; commit list contains only `25e6fa0`.
- Apps Script syntax parse: PASS in the available embedded Node runtime.
- Contract harness: FAIL before completion because the clock is not deterministic (`3.7h` actual
  versus `1.0h` expected).
- Diff hygiene: FAIL on the review Markdown whitespace noted above.
- BigQuery dry-run: not evidenced; no production query was executed by this review.

Required correction: decouple freshness from an empty MTD population, return sanitized recipient
values, freeze the test clock, repair diff hygiene, rerun all tests, obtain both exact safe-wrapper
dry-runs, and replace deployment placeholders with verified project/manifest/hash/trigger/rollback
evidence. Then request one focused delta review. Do not rehearse or install the trigger before PASS.
