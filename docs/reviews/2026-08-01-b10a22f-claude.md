# Review — b10a22f (RULE-10 + D1/D2 legacy-view membership diagnostic)

**Queue entry:** RQ-20260731-2310-rule10-d1-d3-diagnostic · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked: (1) arithmetic internally consistent — (ก) 1,502+902=2,404 records, (ง) 2,670+326=2,996,
matching G1 job `g1_20260731_144401`'s locked populations exactly; per-view overlap warning stated
(2,663+296 → unique 2,670, must not be summed). (2) Query (committed at
`sql/adhoc/20260731_d1_d2_legacy_view_membership.sql`) joins at the declared `(order_item, period)`
grain and defines (ก)/(ง) the same way G1 did — one guarded job, full byte provenance
(8,645,545,976 dry-run / 8,646,557,696 billed, under ceiling). (3) The two FA-bound gap numbers
(902 records / THB 4,051,711.04 absent for (ก); 326 / THB 640,290.79 for (ง)) are correctly
labelled **BI/view-side filtered**, while the in-view populations are explicitly *not* classified
(present-day view membership ≠ presence in the 30-Jul physical CSV; GCS retains only folder
placeholders even with version listing — D3 correctly declared unavailable rather than inferred).
(4) `keys_columns=0` on all six RULE-10 views is a metadata fact; its absence from the physical
SAP-consumed header is left unverified — right call. (5) RULE-10 records Boat's decision (manual
CSV from V3 for July close; no legacy-view edits; export SQL still prohibited until the
physical-contract gate clears) — decision attributed, not agent-inferred. (6) The
BatchRunDate deviation (legacy `CURRENT_DATE()` vs RULE-02 `LAST_DAY(open_period)` = `31072026`)
is documented as intentional, consistent with RULE-02. (7) SECURITY_FINDING addendum classifies the
SMTP exposure remediation without reproducing values.

Note for the FA report: quote the 902/326 gaps as "excluded by current view logic, disposition
pending", never as "lost" — the finding itself says classification between pickup/rejection vs
timing/view-drift is not yet possible.

Queries used: 0.
