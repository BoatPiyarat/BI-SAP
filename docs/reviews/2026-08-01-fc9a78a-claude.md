# Review — fc9a78a (013/035 BLOCK delta + payment-qualification measurement)

**Queue entry:** RQ-20260801-2233-interface-validation-block-delta · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS — the RQ-2205 BLOCK is cleared.** Both blocks and all four notes closed.

- **BLOCK 1 closed:** array comparison replaced with `TO_JSON_STRING(...) != TO_JSON_STRING(GENERATE_ARRAY(...))`
  (the proposed form), plus `IFNULL(total_periods, -1)` in the reason and a new
  `COUNT(DISTINCT total_periods) != 1` versions check — that last one covers the MAX-masking note
  unprompted. **Statement-level gate applied**: the extracted schedule statement validates with a
  true semantic estimate (8,682,609 bytes) — no longer the blind file-level pass.
- **BLOCK 2 closed:** impact measured with full provenance — 1,127,882 QUALIFIED; 52,828
  LEAD_NOT_PURCHASED; 17,567 NO_ORDER; 0 NO_ORDER_ITEM / 0 EMPTY_ID (70,395 = 52,828+17,567 ✓).
  Dropped charges now persist at charge grain in `sap_payment_qualification_exclusion`
  (partitioned, rule_code-clustered) — the right answer to "cannot enter the
  (order_item, period) register": a separate charge-grain audit surface, not a shoehorn.
  The classification CASE is a sensible resolution hierarchy, and the CMI-last `item_rank`
  matches the canonical CMI-identification rule.
- **Doc notes closed:** RULE-08 wording now names CareOS as the raw-date source with
  `payment_date_clamped` as the only durable marker; the winner-selection bullet now scopes the
  content-hash tiebreak to per-DocEntry mirror dedup; rule 13 cites Boat's item 16.

Deploy of 013/035 remains subject to the July runbook's own gates and Boat approval as usual.

Queries used: 1 statement-level 0-byte dry-run.
