# Review: RQ-20260825-1558 empty-installment-details finding and Phase 1/2 source

Reviewer: Codex  
Artifacts: `57f9a30` + `00ce8ed` + `b7aba5e`  
Verdict: **BLOCK**

## Standards

The root-cause chain and V3 ONETIME/RCL split are coherent, and the Phase 2 `NOT EXISTS` is scoped
to the held `order_item` within the same pipeline run. Three Class-A evidence/scope problems remain:

1. The corrected RCL count of 2, the snapshot sequence for `L73472003-1`, and both negative-control
   results have saved SQL but no execution job IDs or exact source timestamps. These are production
   quantifications and cannot pass traceability/cost-hygiene checklists without their actual jobs.
2. `20260825_verify_082_negative_controls.sql` selects its 20 controls with `LIMIT 20` and no
   `ORDER BY`. The sample is nondeterministic, so “zero false positives” is not reproducible against
   a stable named population. Pin the controls or order them deterministically, rerun, and cite the
   job/timestamp.
3. Deployable DDL 082 calls itself `Class B` and still states 3 affected RCL items after the finding
   corrected the count to 2. A view/table creation plus production behavioral conclusion is Class A.
   Correct the classification and stale header before deployment.

The Phase 2 procedure body also cannot be accepted for deployment until 082 exists and the exact
full file receives a real dry-run/validation against those live objects, as the author already
disclosed. Do not deploy the prepared 058 delta as part of Phase 1.

## Spec

**PASS WITH REQUIRED EVIDENCE CORRECTIONS.** The view uses the true latest snapshot per transaction,
restricts to `flow='RCL'` and `total_periods>1`, names the hold explicitly, and excludes the held
item rather than its siblings. `L78753909-V1` is correctly outside the RCL hold by design because
the existing V3 ONETIME source independently resolves it.

## Checklist 1–12

1 traceability **BLOCK** (job IDs/timestamps absent); 2 provenance PASS; 3 NULL-safety PASS;
4 ordering **BLOCK** (nondeterministic negative-control sample); 5 column order N/A; 6 grain PASS
(latest transaction snapshot, distinct order item); 7 distribution **BLOCK** (corrected count and
controls lack executable provenance); 8 knowledge PASS; 9 scope **BLOCK** (Class B label and stale
count in deployable DDL); 10 rollback PASS for additive Phase 1, Phase 2 not deploy-ready;
11 cost hygiene **BLOCK** (dry-run estimate cited, execution jobs absent); 12 honest labelling PASS
for Phase 2's disclosed limitation, BLOCK for DDL 082's stale Class-B/3-item header.

No live query was needed for this verdict: the review blocks on the submitted artifact's own
missing provenance and deterministic-test defect. No deployment, CALL, GCS, SAP, or scheduler
action was performed.
