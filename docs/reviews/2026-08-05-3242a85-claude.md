# Review — RQ-20260805-2051-post-import-rehearsal-fixtures

Reviewer: Claude Code
Artifact: commit `3242a85`; `sql/ddl/074_post_import_rehearsal_fixtures.sql`.
Verdict: **PASS**

## Checked against the request's own checklist

- **Exact live column contracts**: pulled `INFORMATION_SCHEMA.COLUMNS` for all six target tables
  (`export_archive`, `export_file_manifest`, `sap_delivery_manifest_v3`,
  `sap_import_result_header_v3`, `sap_import_error_detail_v3`,
  `v3_post_import_refresh_outbox`) and compared column-for-column, in order, against every
  `INSERT INTO (...)` column list in this file — all six match exactly, no missing/extra/misordered
  column.
- **Transaction/row-count conservation**: all five inserts sit inside one `BEGIN TRANSACTION`/
  `COMMIT TRANSACTION` block, each followed by an `ASSERT @@row_count` (`=3` for the four
  per-case tables, `=1` for the error-detail insert, correctly scoped to only the REJECT case via
  `WHERE has_exact_error`). The claim that the outbox seed was moved *into* this same transaction
  is consistent with what I read — it is the last insert before `COMMIT TRANSACTION`, not a
  separate statement outside it.
- **Nonce and dispatcher-shape guards — independently diffed against the real dispatcher**:
  `p_nonce` must be `^[0-9]{14}$`. The three generated identifiers are checked against
  `^[0-9]{1,20}$` (log_id), `^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$` (export_run_id), and
  `^[A-Za-z0-9][A-Za-z0-9._-]*[.]csv$` (sap_file_name) — I compared these character-for-character
  against `infra/post_import_dispatcher/main.py`'s `_LOG_ID`, `_RUN_ID`, `_FILE_NAME` patterns
  (reviewed in RQ-1939): identical (the file-name pattern uses `[.]csv$` here vs `\.csv$` there —
  functionally the same literal-dot match). A fixture that wouldn't pass the real dispatcher's own
  validation could never be seeded.
- **Mirror seed stability**: the single stable identity is the globally most-recently-updated
  `paid`/`cancelled` row in `sap_mirror_doc` (`QUALIFY ROW_NUMBER() OVER (ORDER BY UpdateDate DESC,
  UpdateTime DESC, DocEntry DESC) = 1`, no partition needed since only one anchor row is required);
  `ASSERT COUNT(*)=1` on the temp table correctly fails closed if the source table were ever empty
  of qualifying rows rather than silently proceeding with no anchor.
- **Error precedence — traced against DDL 073's actual logic, not just described**: ACK and REJECT
  share the *exact same* `(order_item, period, invoice_no, status)` — the only differences are
  `charge_id`/`log_id`/`export_run_id`. This is a deliberate, correct test: without error
  precedence, REJECT's identity would mirror-match exactly like ACK's does. Because each case gets
  its own distinct `log_id`, DDL 073's `_exact_errors` lookup (scoped `WHERE log_id=p_log_id`,
  reviewed in RQ-1946) only ever sees REJECT's own error row when reconciling REJECT — the
  coarse `(order_item, period)`-only grain I flagged as a non-blocking observation in that review
  cannot cross-contaminate ACK and REJECT here, since they are reconciled under different log_ids.
- **Residual non-match guarantee**: RESIDUAL's `order_item`
  (`SYNTHETIC-NO-MIRROR-<nonce>`) and `invoice_no` (`SYNTHETIC-NO-MATCH-<nonce>`) are
  nonce-suffixed synthetic strings that cannot exist in real `sap_mirror_doc` data — guaranteeing
  `_mirror_match` never matches it, so it can only land `PENDING_ACK`.
- **Shadow URI/no-object boundary**: every URI is built under
  `gs://rcb-bronze-zone/post_import_rehearsal/...` — clearly namespaced apart from production
  paths — and the file contains no GCS client call, `EXPORT DATA`, or any statement that could
  create an actual object; it only writes string values into BigQuery rows.
- **Retained-evidence behavior**: no `DELETE` targeting any of these tables appears anywhere in the
  file — fixture rows are permanent once seeded, consistent with "no cleanup/delete path."
- **Outbox initial state**: all three cases seed as `request_status='PENDING'`,
  `claim_token=NULL`, `attempt_count=0` — the correct starting point for the dispatcher's normal
  claim flow to pick them up during rehearsal.
- **Expected-result claims match DDL 073/the workflow's own reviewed behavior**: ACK →
  `ACKNOWLEDGED`/outbox `SUCCEEDED`; REJECT → `REJECTED_BY_SAP` (error precedence) / outbox
  `SUCCEEDED`; RESIDUAL → `PENDING_ACK` / outbox `HUMAN_ACTION` and the workflow's
  `gate_post_import_residual` → `fail_closed`, which does `raise` (reviewed in RQ-1946) — so
  "workflow FAILED" for the RESIDUAL case is the *correct*, expected outcome, not a bug.
- **Not deployed or called**: this file is `CREATE OR REPLACE PROCEDURE` only — no `CALL`
  statement anywhere. Independently re-ran the mandatory dry-run: 0 bytes, matching the claim.
- **`git diff --check`**: clean.

## Verdict

PASS. Every table this procedure writes to was checked column-for-column against live BigQuery
schema, the three synthetic identifiers were verified against the real dispatcher's own validation
patterns (not just described as compatible), and the ACK/REJECT/RESIDUAL cases are constructed to
genuinely and specifically exercise DDL 073's error-precedence and non-match logic rather than
just superficially resembling three different outcomes. No deploy, CALL, GCS write, workflow
execution, delivery, or SAP action occurred in this review.
