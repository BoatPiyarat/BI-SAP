# Review — 7402acf (Boat-supplied SAP_LIVE vs [@INSURANCE] UpdateDate count comparison)

**Queue entry:** RQ-20260801-1135-sap-updatedate-count-comparison · **Class:** A · **Reviewer:** Claude Code
**Verdict:** **PASS**

Checked: (1) arithmetic — every delta recomputed from the table: spot checks 59,280−2,261=+57,019;
981−1,045=−64; 2−42=−40; 15,836−6,527=+9,309 all correct; the two negatives (07-06, 07-07) are
indeed the only comparable dates where BQ is lower; 06-30 correctly marked not comparable
(source not supplied). 08-01 exact match at 60,118 stated as count-level only, with the DocEntry
anti-join still required — correct. (2) Grain discipline: BQ side declared `COUNT(DISTINCT
DocEntry)`; source count explicitly NOT proven distinct, and the missing source query
timestamp/text is declared unsupplied rather than invented — complies with the
no-fabricated-provenance rule. (3) Interpretation is the right semantics for an append-only
snapshot store: positive deltas explained by retained historical observations (a DocEntry can
persist under an old UpdateDate bucket after SAP's current row advances) and correctly given zero
evidentiary weight for completeness. (4) The UpdateDate-vs-BatchRunDate non-conflation warning is
explicit. (5) Cross-checks against known figures hold: 07-29 = 58,619 and 07-28 = 60,385 match the
established batch counts.

Note (sharpens, doesn't change, the conclusion): because BigQuery never deletes, a document whose
*current* SAP UpdateDate is 07-06/07-07 but which is absent from that BQ bucket was necessarily
never extracted after its last change — so the two negative deltas are a stronger capture-gap
signal than any positive delta is of anything. The prescribed source distinct-DocEntry anti-join
should target those two dates first; treat alongside (not merged with) the separate 22–24 Jul
no-POST window already flagged in `b00af18`.

Queries used: 0.
