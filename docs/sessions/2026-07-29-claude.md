# Session 2026-07-29 (Claude) — SQL-domain only, no docs/knowledge touched

Scope this session: verify+commit prior SQL (Rule 4, done separately), then 4 items from Boat.
**Nothing in this session was deployed except the Rule-4 commits already reported** (032-034,
already live from the prior session; 035 below is source-only, NOT run). Codex owns
`docs/knowledge/**`, `INPUTS_NEEDED.md`, `30_SAP_CHANGELOG.md`, `20_SAP_PROGRESS.md` — fold from
here, don't ask me to write those directly this session.

All numbers below are live queries against BigQuery on 2026-07-29, re-run fresh for this doc (not
copied from memory). Read-only throughout except the two `git commit`s (SQL files only).

---

## 1. ⚠️ CANCEL PATH 2025 — investigated, real gap confirmed, but smaller and more specific than
   the raw NO_NEW_PAID_2025 number suggested. **Needs Boat's call before Phase C, not a fix I made.**

### The question: for 2025 orders that are cancelled in CareOS and already exist in SAP, does the
pipeline generate anything a future cancel-export step could use as a baseline?

**Short answer: currently 0, and the fix isn't "make expected_state carry a Cancelled status" —
it's more layered than that. Full breakdown:**

Total 2025 order_items (`stg_order_dim`, `is_cancelled = TRUE`, year via
`GREATEST(OrderDate,PolicyDate)` basis): **18,249**

| Split | order_items | Meaning |
|---|---:|---|
| Not yet in SAP at all | 109 | Correctly excluded already, logged as `CANCEL_2025_NOT_IN_SAP` (45, have a schedule row so they made it into `sap_excluded_records`) or invisible for the same reason as the row below (64, no schedule row — see next section) |
| **Already in SAP, fully Cancelled there already** | **10,153** (8,187 + 1,966) | SAP already reflects the cancellation. No action needed for these — not a gap. |
| **Already in SAP, SAP has NOT caught up yet (some period still Pending/Paid in the mirror)** | **9,953** | **These are the real "should get a cancel document" population. Pipeline generates a cancel row for 0 of them today.** |

Breaking the 9,953 down further — this is the part that changes the picture:

| Sub-group | order_items | periods | Why it's invisible / mislabeled |
|---|---:|---:|---|
| Genuine standalone cancellation, **has a `stg_schedule` row** (flows through the engine) | **328** | 790 | Enters `expected_state` today, but `expected_state.expected_status` only ever computes `Paid`/`Pending` — there is no `Cancelled` value in the schema at all. These 328 are silently mislabeled, not silently dropped. |
| **Credit-Shell change-order supersession** (`old_human_id` in `careos.cancelled_change_orders`), **no `stg_schedule` row** | **9,625** | 21,770 | `stg_schedule`'s own source query deliberately excludes `old_human_id` rows (`WHERE o.human_id NOT IN (SELECT old_human_id FROM cancelled_change_orders)`) so the superseding new order isn't double-scheduled. Verified via 3 samples (L77238827, L79216708, L78513502): all 3 are confirmed superseded orders with a real linked transaction — this is the *existing*, deliberate change-order exclusion working as designed, not a new bug. |

**Verified, not assumed**: sampled 3 order_items from the 9,625 group directly against
`careos.careos_orders`/`carepay_transactions`/`cancelled_change_orders` — confirmed all 3 have
`matching_txn_count = 1` (a transaction genuinely exists) and `is_superseded_old_order = 1`. Then
re-ran the full split with `COUNT(DISTINCT order_item)` (first pass double-counted via a
fan-out join against `cancelled_change_orders` — caught before reporting, since that table can have
>1 row per `old_human_id`).

### What this means, plainly:
- The 328 genuine-cancel group is a real, clean, small gap: expected_state has no way to represent
  "this should be Cancelled." That's a schema/design gap in `expected_state`, confirmed with 0
  currently generated — matches Boat's own "if 0, fix the design" criterion.
- The 9,625 change-order group is **not a new problem** — it's the same open question already
  parked with Aware (Q3a, `docs_considered > 1`, `SAP_CANCEL_IMPORT_SPEC_INFERRED_v0.9.md`,
  per `INPUTS_NEEDED.md`). Whether a superseded old order needs its own explicit SAP cancel
  document, or whether the replacement order's own document supersedes it in SAP's own picking
  logic, is exactly what that open question is about. I did not attempt to resolve it — flagging
  the overlap so whoever builds Phase C's cancel branch knows these ~9,625 items are the SAME
  unresolved question, not a separate one.
- **Downstream impact, quantified**: of `interface_daily_status`'s current 9,372 distinct
  `STATUS_CONFLICT` items, **6,497 (69%) are this exact 2025-cancel-mislabeling population**
  (23,222 of 34,758 STATUS_CONFLICT rows). This is likely most of what's left in STATUS_CONFLICT
  today. Not fixed this session (no design call to make it on my own) but now quantified instead of
  unexplained.

**Nothing here was written to `sap_excluded_records` or `expected_state` differently than what's
already live — this section is pure investigation, no new deploy.**

---

## 2. Backlog — new numbers, stop citing 373,044

`373,044` was `MISSING_NO_ROW_IN_SAP` before the exclusion engine (032-034) existed. Live numbers
now, freshly queried:

### `expected_state` (the "real work" set, post-exclusion)
**293,188 rows / 132,631 distinct order_items** — this is what should be cited as the working
backlog going forward, not 373,044.

### `sap_excluded_records` — excluded, not deleted, by rule_code
| rule_code | rows | distinct order_items |
|---|---:|---:|
| OLD_YEAR_NO_TOUCH | 780,108 | 394,074 |
| NO_NEW_PAID_2025 | 387,939 | 200,666 |
| INSURER_NOT_IN_MASTER | 3,119 | 938 |
| TEST_CUSTOMER_NAME | 347 | 199 |
| TEST_CUSTOMER_PHONE | 341 | 225 *(report-only, still counted in `expected_state` today — see below)* |
| CANCEL_2025_NOT_IN_SAP | 45 | 45 |
| DATE_BASIS_MISSING | 0 | 0 |

### `delta_export` (expected_state vs SAP, after all of the above)
| delta_type | rows | distinct items |
|---|---:|---:|
| OK | 291,567 | 131,978 |
| **MISSING_NO_ROW_IN_SAP** | **749** | **598** |
| NEEDS_PAID_UPDATE | 724 | 711 |
| UNEXPECTED_ALREADY_PAID | 148 | 128 |

**This is the number that replaces 373,044: real MISSING backlog is 749 rows / 598 distinct
order_items**, not 373,044. (Note `TEST_CUSTOMER_PHONE` is still report-only per Boat's instruction
— those 225 items are *not* removed from this count yet, pending Boat's hard-filter decision.)

### `interface_daily_status` (for context, not this session's focus)
| status | rows | distinct items |
|---|---:|---:|
| OK | 257,340 | 123,158 |
| STATUS_CONFLICT | 34,758 | 9,372 (69% explained by §1's finding, see above) |
| MISSING | 576 | 565 |
| PENDING_ACK | 514 | 514 |

---

## 3. F2 — PolicyNo > 50 chars = BLOCK

**Written, NOT deployed**: `sql/ddl/035_policyno_too_long_validation.sql` — full
`sp_run_validation()` procedure (BigQuery routines have no ALTER-to-add-a-check; every versioned
file carries the complete body, same convention as 017/034), existing 4 checks unchanged, new
`POLICYNO_TOO_LONG` check appended. Source is `stg_order_dim.policy_no` (from 033, verbatim
`careos_order_items.policy_number`, no transform). Committed to git (Rule 4) but **the CREATE OR
REPLACE PROCEDURE has not been run against BigQuery** — `sp_run_validation` in production is still
the 4-check version from 017.

**Verified read-only against current `expected_state`** (so these numbers hold regardless of
whether/when 035 gets deployed): **28 rows / 11 distinct order_items** have `policy_no` over 50
chars. All 3 sampled examples are the same pattern — upstream `policy_number` holds an operational
status note in Thai (e.g. `"แจ้งงานแล้ว/ต่ออายุ/ไม่รวม พรบ./กรมธรรม์ 260901/M003006545"`, meaning
roughly "notified/renewal/excl. compulsory/policy 260901/M003006545") instead of, or in addition
to, a real short policy number — not a case of a policy number that's genuinely 51-58 characters.
Truncating would silently corrupt the field; blocking for manual review is the right call per
Boat's rule.

**Morning report line** (content only — not wired into the RemoteTrigger routine's live prompt
this session, since that's outside `sql/` and this session stayed SQL-domain; Codex/next session
should add this line):
> **PolicyNo blocked (>50 chars): 11 order_items, 28 rows.** Examples: L79444694-V1 (51 chars),
> L78245871-V1 (58 chars), L78095272-V1 (55 chars).

---

## 4. Git status — clean under `sql/`

```
git status --short -- sql/
(no output — clean)
```

Commits this session (SQL only, nothing under `docs/knowledge/**` touched):
- `9825e97` — 034 (source verified against live BigQuery in the prior turn, matches exactly)
- `bad4737` — 035 (F2 validation check, source only, not deployed)

---

## Summary table for Codex to fold into PROGRESS/CHANGELOG

| Item | Status |
|---|---|
| 1. Cancel-path 2025 investigation | **Done.** Real gap confirmed (328 items, engine-visible-but-mislabeled) + separately, 9,625 items are the pre-existing Aware Q3a change-order question, not new. Needs Boat's design call before Phase C builds the cancel branch — not fixed, by design (no unilateral fix on a money-adjacent design gap). |
| 2. Backlog re-numbering | **Done.** New real MISSING = 749 rows / 598 items (was 373,044). Full breakdown above. |
| 3. F2 POLICYNO_TOO_LONG | **SQL written, verified read-only (11 items / 28 rows), NOT deployed** per this session's no-deploy instruction. Morning-report line drafted, not wired in (outside `sql/`). |
| 4. Git status | **Done.** Clean under `sql/`, 2 commits this session. |

## Waiting on Boat
- Design call on the 328-item Cancelled-status gap in `expected_state` (does it get a `Cancelled`
  expected_status value, or does Phase C compute cancel candidates separately?).
- Whether the 9,625 change-order items need an explicit SAP cancel document — this is the same
  open question as Aware Q3a, just now quantified against the cancel-2025 lens too.
- Deploy approval for `035_policyno_too_long_validation.sql` (per DEPLOY GATE — replaces
  `sp_run_validation`, existing consumers read `sap_validation_error`).
- Whether/when to hard-filter `TEST_CUSTOMER_PHONE` (still report-only, 225 items untouched in
  `expected_state`).

---

## D1 REVISED — `is_cancelled_effective` diagnostic (read-only, steps 2-5 NOT started, waiting on Boat)

**First diagnostic pass was wrong and was corrected same-turn**: initially compared
`careos_order_items.is_cancelled` against `careos_order_items.cancel_time` (both on the same
table, near-redundant — only 1 item apart) instead of the actual 2-table comparison Boat specified.
Corrected diagnostic (`careos_orders.is_cancelled` vs `careos_order_items.cancel_time`, joined via
`order_id`) found a real, substantial **3,352-item "added group"** (items with `cancel_time` set
whose *order* isn't flagged `is_cancelled`), not the 1-item edge case first reported.

### Full 3-field definition (per Boat, all three confirmed distinct sources/grains)
```sql
is_cancelled_effective =
     careos_order_items.is_cancelled IS TRUE       -- item level, careos.careos_order_items
  OR careos_order_items.cancel_time IS NOT NULL    -- item level, careos.careos_order_items
  OR careos_orders.is_cancelled IS TRUE            -- order level, careos.careos_orders (order cancelled = every item cancelled)
```

### S1 — Partial-cancel-recreate hypothesis: **confirmed as the dominant pattern**
Of the 3,352-item added group: **3,302 (98.5%) have a sibling item on the same order that is still
active** (`cancel_time IS NULL` and item-level `is_cancelled` not TRUE). This matches
`10_SAP_CONTEXT`'s documented normal practice (cancel one item, recreate/keep another under the
same order) — **not a CareOS data inconsistency** for the vast majority of this population. Only
**50 items have no active sibling** — these are the ones that might be genuine
full-order-should-have-been-cancelled cases the order-level flag missed; worth a closer look later,
not chased further this pass.

### S2 — Real SAP status of the 2,254 already-in-`sap_mirror_state` items: **confirms Boat's hypothesis**
| Bucket | items | Σ ActualReceived (THB) |
|---|---:|---:|
| `NOT_IN_SAP` (of the 3,352 total, not part of the 2,254) | 1,098 | — |
| **`ALL_CANCELLED`** (every period already Cancelled in SAP — no action needed) | **1,835** | 24,409,183.85 |
| `HAS_PAID` (at least one period still Paid/invoiced, not Cancelled in SAP) | 370 | 4,879,773.77 |
| `HAS_PENDING_ONLY` (no paid period, not Cancelled in SAP) | 49 | 801,287.23 |

**Confirmed**: real remaining work is **419 items / ≈฿5.68M** (`HAS_PAID` + `HAS_PENDING_ONLY`), not
2,254 items / ฿30M. Most of the ฿30M (81%, ฿24.4M) is already correctly reflected as Cancelled in
SAP — nothing to do there.

### S3/S4 — Overlap with the earlier 9,625 credit-shell-supersession population: **small, mostly a different population**
Only **249 of 3,352 (7.4%)** are `old_human_id` entries in `cancelled_change_orders` (the same
mechanism as the earlier 9,625 finding). **3,103 (92.6%) are NOT** — this added group is
predominantly a *different* phenomenon (ordinary partial cancel-recreate per S1) than the
change-order-supersession population found investigating the 2025 cancel path earlier. Not double
counting: these are two largely distinct populations that happen to share a small overlap.

### S5 — Year split (date basis = `GREATEST(order_create_time, policy_start_date)`, same as E1)
| Year bucket | items |
|---|---:|
| ≤2024 (year-scope excludes entirely, `OLD_YEAR_NO_TOUCH` — irrelevant to any of this regardless of cancel status) | 2,091 |
| 2025 (cancel-only-if-already-in-SAP gate applies) | 808 |
| 2026+ (normal) | 453 |
| Unknown (no date basis) | 0 |

Only **1,261 of 3,352** (2025 + 2026+) are even reachable by the current pipeline once year scope
is applied — the 2,091 pre-2025 items are excluded regardless of any cancel-status fix.

### S6 — 5 sample items (deterministic pick, first 5 by `order_item` ascending)
**Caveat**: alphabetical-first landed all 5 in the ≤2024 / not-in-SAP-mirror slice — good for
illustrating the partial-cancel-recreate shape, not representative of the 2025/2026+ actionable
subset. Every one of the 5 is a clean single-sibling-still-active case:

| order_item | order_id | item cancel_time | 1 sibling (item, cancel_time, is_cancelled) | latest SUCCESSFUL charge | SAP status |
|---|---|---|---|---|---|
| L72396772-1 | L72396772 | 2024-01-26 15:24:12 | L72396772-2, NULL, false | 2023-04-27, ฿2,507.61 | (not in SAP mirror) |
| L72397292-1 | L72397292 | 2024-10-03 13:58:03 | L72397292-2, NULL, false | 2023-03-13, ฿10,110.00 | (not in SAP mirror) |
| L72403320-1 | L72403320 | 2023-08-23 11:30:49 | L72403320-2, NULL, false | 2023-06-10, ฿2,275.12 | (not in SAP mirror) |
| L72413296-1 | L72413296 | 2023-06-02 10:15:46 | L72413296-2, NULL, false | 2023-05-08, ฿2,222.84 | (not in SAP mirror) |
| L72449874-1 | L72449874 | 2024-10-03 11:52:44 | L72449874-2, NULL, false | 2023-05-19, ฿15,780.21 | (not in SAP mirror) |

Every sample: one item cancelled, its sibling item on the same order untouched and still active —
textbook partial cancel-recreate. Can pull a second, stratified sample from the 2025/2026+
`HAS_PAID`/`HAS_PENDING_ONLY` buckets (the actual actionable 419) if useful.

### Design constraint recorded now (per Boat, before steps 2-5 exist)
**Cancel file/status must be emitted per-item only.** An order-level cancel signal must NEVER cause
sibling items on the same order to be pulled in — partial cancel-recreate (S1: 98.5% of this
population) is normal practice, and dragging active siblings into a cancel branch would be a real,
new, money-adjacent bug, not a fix. Any future `is_cancelled_effective` computation must stay at
`order_item` grain, never fan out to "if any item on this order is cancelled, treat the whole order
as cancelled."

**Boat confirmed 2026-07-29 → proceeding to steps 2-5.** 4 extra diagnostics run first (batched,
one query, `--maximum_bytes_billed`), with one real bug caught and fixed mid-analysis before
reporting - see below.

### Bug caught during extra diagnostics: `sap_bucket` undercounted "already cancelled"
First pass classified SAP status via `TransactionStatus = 'Cancelled'` (exact match only). Row-level
sampling (item 3's first sample) showed a period with status `"Cancelled (Change order / Rejected)"`
bucketed as `HAS_PENDING_ONLY` (still-needs-action) when it was actually already resolved in SAP.
Confirmed via `GROUP BY TransactionStatus`: exactly two cancelled variants exist in
`sap_mirror_state` - `Cancelled` (91,112 rows) and `Cancelled (Change order / Rejected)` (38,036
rows) - matching `030_interface_daily_status.sql`'s own `STATUS_CONFLICT` check, which already
treats both as cancelled. Fixed (`TransactionStatus IN ('Cancelled', 'Cancelled (Change order /
Rejected)')`) and every number below is post-fix. **This also corrects the S2 numbers reported
earlier this session** (was: ALL_CANCELLED 1,835/HAS_PAID 370/HAS_PENDING_ONLY 49; corrected below).

### Corrected S2 headline (3,352 added-group items)
| Bucket | items | Σ ActualReceived (THB) |
|---|---:|---:|
| NOT_IN_SAP | 1,098 | — |
| ALL_CANCELLED (corrected, was 1,835) | **1,958** | 26,198,988.32 |
| HAS_PAID (corrected, was 370) | **247** | 3,089,969.30 |
| HAS_PENDING_ONLY (unchanged) | 49 | 801,287.23 |

**Real actionable population is 296 items (247+49) / ≈฿3.89M, not 419/฿5.68M as first reported.**

### Extra item 1 — year breakdown of the 296 actionable (for the FA conversation)
| Year bucket | items | Σ ActualReceived (THB) |
|---|---:|---:|
| 2025 (cancel-only-if-in-SAP gate applies - all 296 are already in `sap_mirror_state` by construction, so all pass that gate) | 25 | 449,435.07 |
| 2026+ (normal) | 16 | 270,872.24 |
| ≤2024 or unknown date basis (year scope excludes these regardless - `OLD_YEAR_NO_TOUCH` - stays untouched even though SAP genuinely hasn't caught up) | 255 | 3,170,949.22 |

**"Real work sendable in the first round" = 41 items (25+16) / ≈฿720,307.31** - this is the number
for the FA conversation, not 296/฿3.89M and not the original 419/฿5.68M. The other 255 items
(86% of the actionable population, ฿3.17M) are genuinely stale SAP/CareOS mismatches for pre-2025
orders that current year-scope policy leaves untouched - flagging this as a real, separate open
question (year scope currently has no cancel-specific carve-out for old years) rather than silently
folding it into "handled."

### Extra item 2 — the 50 no-active-sibling items: genuine order-level flag gap, not random noise
**Exactly 25 distinct orders, each contributing exactly 2 items** (both items on the order were
individually cancelled, so neither sees the other as an "active" sibling) - not scattered
single-item orders. Breakdown:
- Only **2 of 50** are `cancelled_change_orders` supersessions - most of this group is NOT explained
  by the credit-shell mechanism.
- Year: 36 ≤2024, 4 2025, 10 2026+.
- SAP bucket (corrected): 18 `NOT_IN_SAP`, 30 `ALL_CANCELLED` (already resolved, nothing to do), 1
  `HAS_PAID`, 1 `HAS_PENDING_ONLY` (only these 2 of 50 need actual action).

**Conclusion: confirmed CareOS data-inconsistency, not a pipeline bug.** These 25 orders have every
item individually cancelled (`cancel_time` set on both), but the parent order's `is_cancelled` flag
was never set to TRUE. Per Boat's instruction, logging as an IT question rather than fixing/chasing
further:

> **Draft for `INPUTS_NEEDED.md` (Codex: please fold in under a new "Boat/IT" section) —
> `careos_orders.is_cancelled` not set when every child item is individually cancelled**
> Found 2026-07-29 investigating the cancel-status gap: 25 orders where **all** their
> `careos_order_items` rows have `cancel_time` set (i.e. every item was individually cancelled),
> but `careos_orders.is_cancelled` is still not TRUE for the parent order. Examples:
> L73414015, L73708431, L73949419, L74117246, L74219698 (5 of the 25; full list of 50 order_items
> in `docs/sessions/2026-07-29-claude.md`). Low financial urgency (30 of these 50 items are already
> `Cancelled`/`Cancelled (Change order / Rejected)` in SAP regardless - only 2 of 50 need any real
> action), but worth asking IT whether the order-level cancel flag should auto-set when its last
> active item is cancelled, since this project is about to start reading that flag as part of
> `is_cancelled_effective`. Not fixed, not chased further - per instruction, this is a question, not
> a blocker.

### Extra item 3 — stratified sample of 5 (from the corrected 296 actionable, highest Σ ActualReceived first)
| order_item | order_id | cancel_time | sibling (still active?) | SAP periods | in year scope? |
|---|---|---|---|---|---|
| L77340194-V1 | L77340194 | 2025-11-01 | M1, yes | 1 period, Paid, ฿36,499.75 | Yes (2025, in SAP) - genuinely actionable now |
| L73475856-V1 | L73475856 | 2024-07-06 | M1, yes | 1 period, Paid, ฿53,619.81 | No (≤2024) - real gap, outside current policy |
| L77050989-V1 | L77050989 | 2024-06-26 | M1, yes | 6 periods: 3 Paid, 3 Pending | No (≤2024) - real gap, outside current policy |
| L73914132-V1 | L73914132 | 2024-09-04 | M1, yes | 1 period, Paid, ฿43,100.67 | No (≤2024) - real gap, outside current policy |
| L73808892-V1 | L73808892 | 2026-02-12 | M1, yes | 10 periods: 1 Cancelled, 7 Paid, 2 Pending | Yes (2026+) - genuinely actionable now |

Every sample: exactly one sibling, still fully active (`cancel_time` NULL, `is_cancelled` false) -
the per-item-only design constraint (recorded earlier) is exactly what these cases need: cancel
`L77340194-V1` without touching `L77340194-M1`.

**Proceeding to steps 2-5 now** (source-only, no deploy until acceptance is reported and confirmed).

<details>
<summary>Appendix: full 50-item / 25-order list (order_item | basis_year | sap_bucket | in_cancelled_change_orders)</summary>

```
L73414015-M1|2024|ALL_CANCELLED|false     L73414015-V1|2024|ALL_CANCELLED|false
L73685174-M1|2023|NOT_IN_SAP|false        L73685174-V1|2023|NOT_IN_SAP|false
L73708431-M1|2024|ALL_CANCELLED|false     L73708431-V1|2024|ALL_CANCELLED|false
L73949419-M1|2024|ALL_CANCELLED|false     L73949419-V1|2024|ALL_CANCELLED|false
L74117246-M1|2024|ALL_CANCELLED|false     L74117246-V1|2024|ALL_CANCELLED|false
L74165224-M1|2023|NOT_IN_SAP|false        L74165224-V1|2023|ALL_CANCELLED|false
L74219698-M1|2024|ALL_CANCELLED|false     L74219698-V1|2024|ALL_CANCELLED|false
L74249480-M2|2023|NOT_IN_SAP|false        L74249480-V1|2023|NOT_IN_SAP|false
L74670275-M1|2023|NOT_IN_SAP|false        L74670275-V1|2023|NOT_IN_SAP|false
L74671168-M1|2023|NOT_IN_SAP|false        L74671168-V1|2023|NOT_IN_SAP|false
L74784468-M1|2024|NOT_IN_SAP|false        L74784468-V1|2024|NOT_IN_SAP|false
L74798352-M1|2023|NOT_IN_SAP|false        L74798352-V1|2023|NOT_IN_SAP|false
L75296607-M1|2023|NOT_IN_SAP|false        L75296607-V1|2023|NOT_IN_SAP|false
L76948140-M1|2024|NOT_IN_SAP|false        L76948140-V1|2024|HAS_PENDING_ONLY|false
L77033749-M1|2024|ALL_CANCELLED|false     L77033749-V1|2024|ALL_CANCELLED|false
L77066429-M1|2024|ALL_CANCELLED|false     L77066429-V1|2024|HAS_PAID|false
L77169545-M1|2024|ALL_CANCELLED|false     L77169545-V1|2024|ALL_CANCELLED|false
L77321246-M1|2024|ALL_CANCELLED|false     L77321246-V1|2024|ALL_CANCELLED|false
L77561619-M1|2026|ALL_CANCELLED|true      L77561619-V1|2026|ALL_CANCELLED|true
L77683928-M1|2025|ALL_CANCELLED|false     L77683928-V1|2025|ALL_CANCELLED|false
L77895993-M1|2026|ALL_CANCELLED|false     L77895993-V1|2026|ALL_CANCELLED|false
L78395967-M1|2026|ALL_CANCELLED|false     L78395967-V1|2026|ALL_CANCELLED|false
L78651685-M1|2025|ALL_CANCELLED|false     L78651685-V1|2025|ALL_CANCELLED|false
L79802363-M1|2026|ALL_CANCELLED|false     L79802363-V1|2026|ALL_CANCELLED|false
L80088749-M1|2026|NOT_IN_SAP|false        L80088749-V1|2026|NOT_IN_SAP|false
```
(`L77561619` is the one pair also explained by `cancelled_change_orders` - the other 24 orders are
not.)
</details>
